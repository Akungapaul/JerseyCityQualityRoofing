---
phase: 06-commercial-service-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/commercial-related-services-row.tsx
autonomous: true
requirements: [COMM-01, COMM-02, COMM-03, COMM-04]

must_haves:
  truths:
    - "CommercialRelatedServicesRow links to /services/commercial/ URLs not /services/residential/"
    - "Component uses commercial-specific Lucide icons (Layers, ClipboardCheck, Wrench, Building2)"
    - "Heading says 'Related Commercial Services' not 'Related Services'"
    - "Component is a Server Component (no 'use client' directive)"
  artifacts:
    - path: "src/components/sections/commercial-related-services-row.tsx"
      provides: "Commercial related services navigation component"
      exports: ["CommercialRelatedServicesRow"]
      min_lines: 50
  key_links:
    - from: "src/components/sections/commercial-related-services-row.tsx"
      to: "src/data/services.ts"
      via: "import { getService }"
      pattern: "getService"
    - from: "src/components/sections/commercial-related-services-row.tsx"
      to: "/services/commercial/"
      via: "Link href"
      pattern: "/services/commercial/"
---

<objective>
Create the CommercialRelatedServicesRow component -- the only new component in Phase 6.

Purpose: Commercial service pages need related service links that point to `/services/commercial/[slug]` instead of `/services/residential/[slug]`. A separate component prevents conditional logic pollution in the proven residential component.

Output: One new component file matching the exact visual structure of RelatedServicesRow with commercial URLs and icons.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-commercial-service-pages/06-UI-SPEC.md

<interfaces>
<!-- Exact source component to mirror. Extracted from codebase. -->

From src/components/sections/related-services-row.tsx (74 lines, the full component):
```typescript
import Link from "next/link";
import {
  Wrench,
  Home,
  Search,
  AlertTriangle,
  Hammer,
  ArrowRight,
} from "lucide-react";
import type { LucideIcon } from "lucide-react";
import { getService } from "@/data/services";

const SERVICE_ICON_MAP: Record<string, LucideIcon> = {
  "roof-repair": Wrench,
  "roof-replacement": Home,
  "roof-inspection": Search,
  "emergency-roofing": AlertTriangle,
};

interface RelatedServicesRowProps {
  serviceSlugs: readonly string[];
}

export function RelatedServicesRow({ serviceSlugs }: RelatedServicesRowProps) {
  const services = serviceSlugs
    .map((slug) => {
      const service = getService(slug);
      if (!service) return null;
      return { ...service, Icon: SERVICE_ICON_MAP[slug] ?? Hammer };
    })
    .filter(
      (s): s is NonNullable<typeof s> => s !== null,
    );

  if (services.length === 0) return null;

  return (
    <div>
      <h2 className="text-[1.75rem] font-heading font-bold text-text-primary">
        Related Services
      </h2>

      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 mt-6">
        {services.map((service) => (
          <Link
            key={service.slug}
            href={`/services/residential/${service.slug}`}
            className="bg-secondary rounded-lg p-6 group hover:bg-secondary-lighter transition-colors duration-[150ms]"
          >
            <service.Icon
              size={24}
              strokeWidth={1.5}
              className="text-accent mb-2"
              aria-hidden="true"
            />
            <h3 className="text-lg font-heading font-bold text-text-primary">
              {service.name}
            </h3>
            <p className="text-lg text-text-secondary mt-1 line-clamp-2">
              {service.shortDescription}
            </p>
            <span className="mt-4 flex items-center gap-1 text-accent text-sm">
              View {service.name}
              <ArrowRight
                size={16}
                strokeWidth={1.5}
                aria-hidden="true"
                className="group-hover:translate-x-1 transition-transform duration-[150ms]"
              />
            </span>
          </Link>
        ))}
      </div>
    </div>
  );
}
```

Commercial icon map (from UI-SPEC):
| Service Slug | Lucide Icon |
|-------------|-------------|
| flat-roof-systems | Layers |
| roof-maintenance | ClipboardCheck |
| commercial-repair | Wrench |
| commercial-replacement | Building2 |
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CommercialRelatedServicesRow component</name>
  <files>src/components/sections/commercial-related-services-row.tsx</files>
  <read_first>
    - src/components/sections/related-services-row.tsx
  </read_first>
  <action>
Create `src/components/sections/commercial-related-services-row.tsx` as a direct parallel of the existing `RelatedServicesRow` component with these 3 specific changes:

**Change 1 -- Icon map:** Replace the SERVICE_ICON_MAP with COMMERCIAL_ICON_MAP:
```typescript
import {
  Layers,
  ClipboardCheck,
  Wrench,
  Building2,
  Hammer,
  ArrowRight,
} from "lucide-react";

const COMMERCIAL_ICON_MAP: Record<string, LucideIcon> = {
  "flat-roof-systems": Layers,
  "roof-maintenance": ClipboardCheck,
  "commercial-repair": Wrench,
  "commercial-replacement": Building2,
};
```

**Change 2 -- Link URLs:** Replace `/services/residential/` with `/services/commercial/`:
```typescript
href={`/services/commercial/${service.slug}`}
```

**Change 3 -- Heading text:** Replace "Related Services" with "Related Commercial Services":
```typescript
<h2 className="text-[1.75rem] font-heading font-bold text-text-primary">
  Related Commercial Services
</h2>
```

Everything else is identical to RelatedServicesRow:
- Same props interface: `{ serviceSlugs: readonly string[] }`
- Same getService lookup, null filtering, and empty-state return
- Same grid layout: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 mt-6`
- Same card styling: `bg-secondary rounded-lg p-6 group hover:bg-secondary-lighter transition-colors duration-[150ms]`
- Same icon rendering: `size={24} strokeWidth={1.5} className="text-accent mb-2" aria-hidden="true"`
- Same arrow CTA: `"View {service.name}"` with ArrowRight, group-hover:translate-x-1
- Same h3 service name and p short description with line-clamp-2
- Fallback icon: `Hammer` (same as residential)

The component is a Server Component -- do NOT add `"use client"` directive. Named export: `CommercialRelatedServicesRow`.
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/JerseyCityQualityRoofing && pnpm type-check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/sections/commercial-related-services-row.tsx exists
    - File does NOT contain `"use client"` or `'use client'`
    - File contains `export function CommercialRelatedServicesRow`
    - File contains `const COMMERCIAL_ICON_MAP: Record<string, LucideIcon>` with entries for flat-roof-systems, roof-maintenance, commercial-repair, commercial-replacement
    - File contains `Layers` import from lucide-react (for flat-roof-systems icon)
    - File contains `ClipboardCheck` import from lucide-react (for roof-maintenance icon)
    - File contains `Building2` import from lucide-react (for commercial-replacement icon)
    - File contains `href={` followed by `/services/commercial/` (NOT `/services/residential/`)
    - File contains `Related Commercial Services` heading text
    - File contains `import { getService } from "@/data/services"` or equivalent
    - File contains `import Link from "next/link"`
    - `pnpm type-check` exits 0 with no errors
  </acceptance_criteria>
  <done>CommercialRelatedServicesRow component created as a Server Component with commercial icon map, /services/commercial/ link prefix, and "Related Commercial Services" heading. TypeScript compilation passes.</done>
</task>

</tasks>

<verification>
After task completes:
1. `pnpm type-check` -- no TypeScript errors
2. Grep for `/services/residential/` in the new file -- must return 0 matches
3. Grep for `/services/commercial/` in the new file -- must return at least 1 match
4. Grep for `"use client"` in the new file -- must return 0 matches
</verification>

<success_criteria>
- CommercialRelatedServicesRow component exists at src/components/sections/commercial-related-services-row.tsx
- Component links to /services/commercial/ URLs
- Component uses Layers, ClipboardCheck, Wrench, Building2 icon map
- Component heading says "Related Commercial Services"
- Component is a Server Component (no "use client")
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/06-commercial-service-pages/06-02-SUMMARY.md`
</output>
