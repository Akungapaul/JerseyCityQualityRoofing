---
phase: 04-core-marketing-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/hero-section.tsx
  - src/components/sections/badge-strip.tsx
  - src/components/sections/services-grid.tsx
  - src/components/sections/why-choose-us.tsx
  - src/components/sections/testimonial-carousel.tsx
  - src/components/sections/star-rating.tsx
  - src/components/sections/service-areas-overview.tsx
  - src/components/sections/faq-accordion.tsx
  - src/components/sections/google-map-embed.tsx
  - src/components/sections/city-card.tsx
  - src/components/sections/city-card-grid.tsx
  - src/components/sections/business-hours-table.tsx
  - src/components/sections/contact-info-column.tsx
  - src/components/sections/about-company-story.tsx
  - src/components/sections/about-team-section.tsx
  - src/components/sections/about-certifications.tsx
autonomous: true
requirements: [CORE-01, CORE-02, CORE-03, CORE-04, CRO-06, CRO-07, SEO-15]

must_haves:
  truths:
    - "HeroSection renders split layout with headline, subtext, and dual CTA buttons (phone + quote form scroll)"
    - "BadgeStrip renders all 5 certification badges with Lucide icons"
    - "ServicesGrid renders 8 service cards in a 2-column mobile / 4-column desktop grid grouped by Residential and Commercial"
    - "TestimonialCarousel renders cards with star ratings using Embla Carousel with autoplay and loop"
    - "FaqAccordion is keyboard-accessible with aria-expanded, aria-controls, and single-open behavior"
    - "GoogleMapEmbed renders a lazy-loaded iframe with fallback text"
    - "CityCardGrid renders tiered layout with Tier 1 cards visually larger than Tier 2/3 cards"
    - "ContactInfoColumn displays NAP data from BUSINESS_INFO, business hours, and license number"
  artifacts:
    - path: "src/components/sections/hero-section.tsx"
      provides: "Split hero with dual CTAs"
      contains: "HeroSection"
    - path: "src/components/sections/badge-strip.tsx"
      provides: "Certification badge horizontal strip"
      contains: "BadgeStrip"
    - path: "src/components/sections/services-grid.tsx"
      provides: "Icon card grid from SERVICES data"
      contains: "ServicesGrid"
    - path: "src/components/sections/testimonial-carousel.tsx"
      provides: "Embla Carousel with autoplay for testimonials"
      contains: "useEmblaCarousel"
    - path: "src/components/sections/faq-accordion.tsx"
      provides: "Accessible accordion with ARIA attributes"
      contains: "aria-expanded"
    - path: "src/components/sections/google-map-embed.tsx"
      provides: "Lazy-loaded Google Maps iframe"
      contains: "loading=\"lazy\""
    - path: "src/components/sections/city-card-grid.tsx"
      provides: "Tiered city card layout"
      contains: "getMunicipalitiesByTier"
    - path: "src/components/sections/contact-info-column.tsx"
      provides: "NAP data column for contact page"
      contains: "BUSINESS_INFO"
  key_links:
    - from: "src/components/sections/services-grid.tsx"
      to: "src/data/services.ts"
      via: "import getServicesByCategory"
      pattern: "getServicesByCategory"
    - from: "src/components/sections/city-card-grid.tsx"
      to: "src/data/municipalities.ts"
      via: "import getMunicipalitiesByTier"
      pattern: "getMunicipalitiesByTier"
    - from: "src/components/sections/testimonial-carousel.tsx"
      to: "src/data/testimonials.ts"
      via: "TESTIMONIALS prop or import"
      pattern: "Testimonial"
    - from: "src/components/sections/contact-info-column.tsx"
      to: "src/data/business-info.ts"
      via: "import BUSINESS_INFO"
      pattern: "BUSINESS_INFO"
---

<objective>
Build all 16 reusable section components for Phase 4: HeroSection, BadgeStrip, ServicesGrid, WhyChooseUs, TestimonialCarousel, StarRating, ServiceAreasOverview, FaqAccordion, GoogleMapEmbed, CityCard, CityCardGrid, BusinessHoursTable, ContactInfoColumn, AboutCompanyStory, AboutTeamSection, AboutCertifications.

Purpose: Create the full component library that Plan 03 assembles into the four marketing pages. Each component is self-contained, data-driven, and follows the established cva+cn() pattern.
Output: 16 new component files in src/components/sections/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-core-marketing-pages/04-CONTEXT.md
@.planning/phases/04-core-marketing-pages/04-RESEARCH.md
@.planning/phases/04-core-marketing-pages/04-UI-SPEC.md

<interfaces>
<!-- Existing components to compose with -->

From src/components/sections/section-wrapper.tsx:
```typescript
interface SectionWrapperProps {
  children: React.ReactNode;
  tone?: "dominant" | "secondary";
  id?: string;
  className?: string;
}
export function SectionWrapper({ children, tone, id, className }: SectionWrapperProps): JSX.Element;
```

From src/components/sections/scroll-reveal.tsx:
```typescript
// "use client" component
interface ScrollRevealProps {
  children: React.ReactNode;
  className?: string;
  delay?: number;
}
export function ScrollReveal({ children, className, delay }: ScrollRevealProps): JSX.Element;
```

From src/components/sections/cta-banner.tsx:
```typescript
interface CTABannerProps {
  heading?: string;
  subtext?: string;
  className?: string;
}
export function CTABanner({ heading, subtext, className }: CTABannerProps): JSX.Element;
```

From src/components/ui/button-variants.ts:
```typescript
export const buttonVariants: (props?: { variant?: "primary" | "secondary" | "ghost" | "phone"; size?: "default" | "compact" }) => string;
```

From src/data/types.ts:
```typescript
export interface FAQ { question: string; answer: string; }
export interface Testimonial { id: string; name: string; citySlug: string; serviceSlug: string; rating: 4 | 5; text: string; date: string; projectType: string; }
export interface Municipality { name: string; slug: string; population: number; description: string; tier: 1 | 2 | 3; /* ... */ }
export interface Service { name: string; slug: string; category: ServiceCategory; shortDescription: string; /* ... */ }
export type ServiceCategory = "residential" | "commercial";
```

From src/data/services.ts:
```typescript
export const SERVICES: Record<string, Service>;
export function getServicesByCategory(category: ServiceCategory): Service[];
```

From src/data/municipalities.ts:
```typescript
export const MUNICIPALITIES: Record<string, Municipality>;
export function getMunicipalitiesByTier(tier: 1 | 2 | 3): Municipality[];
```

From src/data/testimonials.ts:
```typescript
export const TESTIMONIALS: readonly Testimonial[]; // 48 entries
```

From src/data/business-info.ts:
```typescript
export const BUSINESS_INFO: BusinessInfo;
```

From src/lib/constants.ts:
```typescript
export const PHONE_NUMBER = "(201) 555-0123";
export const PHONE_HREF = "tel:+12015550123";
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build interactive client components (TestimonialCarousel, FaqAccordion, GoogleMapEmbed) and install Embla</name>
  <files>
    src/components/sections/testimonial-carousel.tsx,
    src/components/sections/star-rating.tsx,
    src/components/sections/faq-accordion.tsx,
    src/components/sections/google-map-embed.tsx
  </files>
  <read_first>
    - src/components/sections/section-wrapper.tsx (SectionWrapper tone prop and className pattern)
    - src/components/sections/scroll-reveal.tsx (ScrollReveal wrapping pattern)
    - src/data/testimonials.ts (TESTIMONIALS array shape, 48 entries)
    - src/data/types.ts (Testimonial and FAQ interfaces)
    - src/lib/utils.ts (cn() utility)
    - src/lib/constants.ts (PHONE_NUMBER, PHONE_HREF for carousel link)
    - .planning/phases/04-core-marketing-pages/04-UI-SPEC.md (Interaction Contracts section for exact specs)
    - .planning/phases/04-core-marketing-pages/04-RESEARCH.md (Embla Carousel patterns, FAQ ARIA patterns)
    - package.json (check if embla already installed)
  </read_first>
  <action>
    **Step 0: Install Embla Carousel**
    ```bash
    pnpm add embla-carousel-react@8.6.0 embla-carousel-autoplay@8.6.0
    ```

    **Create src/components/sections/star-rating.tsx** (Server Component, no "use client"):
    - Props: `{ rating: number; className?: string }`
    - Import `Star` from `lucide-react`
    - Render 5 Star icons (size 16). For stars <= rating: `fill="currentColor"` with `className="text-accent"`. For stars > rating: no fill, `className="text-secondary-lighter"`.
    - Wrap in `<div className="flex items-center gap-0.5" aria-label={\`${rating} out of 5 stars\`}>` with `role="img"`

    **Create src/components/sections/testimonial-carousel.tsx** ("use client"):
    Per D-07 and UI-SPEC Interaction Contracts:
    - Import `useEmblaCarousel` from `embla-carousel-react` and `Autoplay` from `embla-carousel-autoplay`
    - Import `useCallback` from `react`
    - Import `ChevronLeft, ChevronRight` from `lucide-react`
    - Import `StarRating` from `./star-rating`
    - Import `SectionWrapper` from `./section-wrapper`
    - Import `ScrollReveal` from `./scroll-reveal`
    - Import `TESTIMONIALS` from `@/data/testimonials`
    - Import `type Testimonial` from `@/data/types`

    Props: `{ testimonials?: readonly Testimonial[] }` (defaults to TESTIMONIALS)

    Embla config:
    ```typescript
    const [emblaRef, emblaApi] = useEmblaCarousel(
      { loop: true, align: 'start', slidesToScroll: 1 },
      [Autoplay({ delay: 5000, stopOnInteraction: false, stopOnMouseEnter: true })]
    );
    ```

    Navigation:
    ```typescript
    const scrollPrev = useCallback(() => emblaApi?.scrollPrev(), [emblaApi]);
    const scrollNext = useCallback(() => emblaApi?.scrollNext(), [emblaApi]);
    ```

    Layout:
    - Outer: `<SectionWrapper tone="secondary">` wrapping `<ScrollReveal>`
    - Heading: `<h2 className="font-heading font-bold text-[1.75rem] text-text-primary mb-8">What Our Customers Say</h2>`
    - Carousel container: `<div role="region" aria-roledescription="carousel" aria-label="Customer testimonials">`
    - Embla wrapper: `<div className="overflow-hidden" ref={emblaRef}>`
    - Flex container: `<div className="flex -ml-4">`
    - Each slide: `<div className="flex-[0_0_100%] lg:flex-[0_0_33.333%] min-w-0 pl-4" role="group" aria-roledescription="slide">`
    - Card inside slide: `<div className="bg-dominant rounded-lg p-6 h-full flex flex-col">` with StarRating, quote text (text-text-primary text-lg), customer name (font-bold text-text-primary), city (text-text-secondary)
    - Prev/next buttons below carousel: `<div className="flex items-center justify-center gap-4 mt-6">` with `<button onClick={scrollPrev} aria-label="Previous testimonial" className="min-h-[44px] min-w-[44px] ...">` using ChevronLeft/ChevronRight icons (size 24)
    - Button styling: `bg-dominant rounded-full flex items-center justify-center hover:bg-secondary-lighter transition-colors duration-[150ms] focus-visible:ring-2 focus-visible:ring-accent focus-visible:ring-offset-2 focus-visible:ring-offset-secondary`

    **Create src/components/sections/faq-accordion.tsx** ("use client"):
    Per D-04 and UI-SPEC Interaction Contracts:
    - Import `useState` from `react`
    - Import `AnimatePresence, motion` from `motion/react`
    - Import `ChevronDown` from `lucide-react`
    - Import `type FAQ` from `@/data/types`
    - Import `cn` from `@/lib/utils`

    Props: `{ faqs: readonly FAQ[]; defaultOpenIndex?: number; className?: string }`

    State: `const [openIndex, setOpenIndex] = useState<number | null>(defaultOpenIndex ?? 0);`

    Layout:
    - Outer: `<div role="region" aria-label="Frequently Asked Questions" className={cn("divide-y divide-secondary-lighter", className)}>`
    - Each FAQ item wrapped in `<div key={index}>`:
      - `<h3>` wrapping `<button>` with:
        - `id={`faq-heading-${index}`}`
        - `aria-expanded={isOpen}`
        - `aria-controls={`faq-panel-${index}`}`
        - `onClick={() => setOpenIndex(isOpen ? null : index)}`
        - `className="w-full text-left flex items-center justify-between py-4 min-h-[44px] text-text-primary font-heading font-bold text-lg focus-visible:ring-2 focus-visible:ring-accent focus-visible:ring-offset-2 focus-visible:ring-offset-dominant outline-none"`
        - Content: question text + ChevronDown icon with `className={cn("transition-transform duration-300 text-accent shrink-0", isOpen && "rotate-180")}` (size 20)
      - Panel: `<AnimatePresence initial={false}>` wrapping conditional `<motion.div>` with height animation:
        ```
        <motion.div
          id={`faq-panel-${index}`}
          role="region"
          aria-labelledby={`faq-heading-${index}`}
          initial={{ height: 0, opacity: 0 }}
          animate={{ height: "auto", opacity: 1 }}
          exit={{ height: 0, opacity: 0 }}
          transition={{ duration: 0.3, ease: "easeOut" }}
          className="overflow-hidden"
        >
          <p className="pb-4 text-text-secondary text-lg leading-relaxed">{faq.answer}</p>
        </motion.div>
        ```

    Single-open behavior: clicking an open item closes it (sets openIndex to null). Clicking a closed item opens it and closes the previously open one.

    **Create src/components/sections/google-map-embed.tsx** ("use client"):
    Per D-11, D-16 and UI-SPEC:
    - Props: `{ query: string; title: string; className?: string; zoom?: number }`
    - Default zoom: 14

    Build src URL: If `process.env.NEXT_PUBLIC_GOOGLE_MAPS_API_KEY` exists, use Maps Embed API URL `https://www.google.com/maps/embed/v1/place?key=${apiKey}&q=${encodeURIComponent(query)}&zoom=${zoom}`. Otherwise, fallback to `https://www.google.com/maps?q=${encodeURIComponent(query)}&output=embed`.

    Layout:
    - Outer: `<div className={cn("relative w-full aspect-video rounded-lg overflow-hidden bg-dominant", className)}>`
    - iframe: `<iframe src={src} title={title} loading="lazy" referrerPolicy="no-referrer-when-downgrade" allowFullScreen className="absolute inset-0 w-full h-full border-0" />`
    - Below iframe: `<noscript><p className="text-text-secondary text-lg p-4">Map could not be loaded. Visit us at 123 Summit Avenue, Jersey City, NJ 07304.</p></noscript>`
  </action>
  <verify>
    <automated>pnpm type-check && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "embla-carousel-react" and "embla-carousel-autoplay"
    - src/components/sections/star-rating.tsx contains `export function StarRating`
    - src/components/sections/star-rating.tsx contains `role="img"`
    - src/components/sections/star-rating.tsx does NOT contain "use client"
    - src/components/sections/testimonial-carousel.tsx contains `"use client"`
    - src/components/sections/testimonial-carousel.tsx contains `useEmblaCarousel`
    - src/components/sections/testimonial-carousel.tsx contains `Autoplay`
    - src/components/sections/testimonial-carousel.tsx contains `aria-roledescription="carousel"`
    - src/components/sections/testimonial-carousel.tsx contains `aria-roledescription="slide"`
    - src/components/sections/testimonial-carousel.tsx contains `stopOnMouseEnter: true`
    - src/components/sections/faq-accordion.tsx contains `"use client"`
    - src/components/sections/faq-accordion.tsx contains `aria-expanded`
    - src/components/sections/faq-accordion.tsx contains `aria-controls`
    - src/components/sections/faq-accordion.tsx contains `role="region"`
    - src/components/sections/faq-accordion.tsx contains `AnimatePresence`
    - src/components/sections/google-map-embed.tsx contains `"use client"`
    - src/components/sections/google-map-embed.tsx contains `loading="lazy"`
    - src/components/sections/google-map-embed.tsx contains `NEXT_PUBLIC_GOOGLE_MAPS_API_KEY`
    - pnpm type-check exits 0
  </acceptance_criteria>
  <done>Embla Carousel installed. TestimonialCarousel renders cards with star ratings, autoplay, loop, prev/next navigation, and full ARIA attributes. FaqAccordion is keyboard-accessible with single-open behavior and Motion height animations. GoogleMapEmbed lazy-loads iframe with API key fallback. StarRating renders visual star display.</done>
</task>

<task type="auto">
  <name>Task 2: Build static server components (HeroSection, BadgeStrip, ServicesGrid, WhyChooseUs, ServiceAreasOverview, CityCard, CityCardGrid, BusinessHoursTable, ContactInfoColumn, About sections)</name>
  <files>
    src/components/sections/hero-section.tsx,
    src/components/sections/badge-strip.tsx,
    src/components/sections/services-grid.tsx,
    src/components/sections/why-choose-us.tsx,
    src/components/sections/service-areas-overview.tsx,
    src/components/sections/city-card.tsx,
    src/components/sections/city-card-grid.tsx,
    src/components/sections/business-hours-table.tsx,
    src/components/sections/contact-info-column.tsx,
    src/components/sections/about-company-story.tsx,
    src/components/sections/about-team-section.tsx,
    src/components/sections/about-certifications.tsx
  </files>
  <read_first>
    - src/components/sections/section-wrapper.tsx (SectionWrapper tone prop)
    - src/components/sections/scroll-reveal.tsx (ScrollReveal wrapping pattern)
    - src/components/sections/cta-banner.tsx (existing CTA pattern for consistency)
    - src/components/ui/button-variants.ts (buttonVariants for CTA buttons)
    - src/data/services.ts (first 80 lines -- SERVICES shape, getServicesByCategory)
    - src/data/municipalities.ts (first 60 lines -- MUNICIPALITIES shape, getMunicipalitiesByTier)
    - src/data/business-info.ts (BUSINESS_INFO for NAP data)
    - src/data/types.ts (Municipality, Service, ServiceCategory types)
    - src/lib/constants.ts (PHONE_NUMBER, PHONE_HREF)
    - .planning/phases/04-core-marketing-pages/04-UI-SPEC.md (component inventory, page layouts, interaction contracts)
    - .planning/phases/04-core-marketing-pages/04-CONTEXT.md (D-01 through D-17)
  </read_first>
  <action>
    All components below are **Server Components** (NO "use client" directive). All use `SectionWrapper` and `ScrollReveal` where they are top-level page sections. All import data from registries -- NO hardcoded NAP, service names, or city names.

    **1. src/components/sections/hero-section.tsx** (per D-01, UI-SPEC Hero):
    - Import `Link` from `next/link`, `Phone` from `lucide-react`, `buttonVariants` from `@/components/ui/button-variants`, `PHONE_NUMBER`, `PHONE_HREF` from constants, `cn` from utils
    - NO SectionWrapper (hero has custom padding)
    - Layout: `<section className="bg-dominant py-16 sm:py-20 lg:py-24">` with max-w-[1280px] container
    - Inner: `<div className="flex flex-col lg:flex-row items-center gap-8 lg:gap-12">`
    - Left (60%): `<div className="lg:w-[60%] text-center lg:text-left">`
      - h1: `"Hudson County's Most Trusted Roofing Experts"` with `className="font-heading font-bold text-[2.5rem] lg:text-[3rem] leading-[1.1] text-text-primary"`
      - Subtext: `"Professional residential and commercial roofing services across all 12 Hudson County municipalities. Licensed, insured, and locally trusted since 2003."` with `className="text-text-secondary text-lg mt-4 max-w-xl lg:max-w-none"`
      - CTA buttons row: `<div className="flex flex-col sm:flex-row items-center gap-3 mt-8">`
        - Primary: `<a href="#quote-form" className={cn(buttonVariants({ variant: "primary", size: "default" }))}>Get Your Free Quote</a>`
        - Phone: `<a href={PHONE_HREF} className={cn(buttonVariants({ variant: "phone", size: "default" }), "gap-2")}><Phone size={20} strokeWidth={1.5} aria-hidden="true" />{`Call ${PHONE_NUMBER}`}</a>`
    - Right (40%): `<div className="lg:w-[40%] flex items-center justify-center">`
      - Placeholder: `<div data-placeholder="hero-image" className="w-full max-w-md aspect-[4/3] rounded-lg bg-gradient-to-br from-secondary to-dominant-lighter flex items-center justify-center">` with a subtle roof-themed SVG or text "Hero Image Placeholder" in text-text-secondary
    - The anchor href="#quote-form" links to the CompactQuoteForm's SectionWrapper id

    **2. src/components/sections/badge-strip.tsx** (per D-06, UI-SPEC BadgeStrip):
    - Import icons: `Award, Shield, Star, Clock, CheckCircle` from `lucide-react` (or other suitable ones)
    - NO SectionWrapper -- custom background `bg-[#33382b]` (dominant-lighter per D-06)
    - Layout: `<section className="bg-[#33382b] py-4 sm:py-6">` with max-w container
    - Inner: `<div className="flex flex-wrap items-center justify-center gap-x-6 gap-y-3 lg:gap-x-8">`
    - 5 badge items, each: `<div className="flex items-center gap-2 text-text-primary">` with icon (size 20, className="text-accent") + `<span className="text-lg font-body whitespace-nowrap">`
    - Items (per D-06):
      1. Award icon + "GAF Master Elite"
      2. Shield icon + "Licensed & Insured"
      3. Star icon + "BBB A+ Rated"
      4. Clock icon + "20+ Years Experience"
      5. CheckCircle icon + "CertainTeed SELECT"
    - Between items (desktop only): subtle dividers via `divide-x divide-secondary-lighter` or CSS borders. Use `border-r border-secondary-lighter last:border-r-0` on each item for desktop, hidden on mobile.

    **3. src/components/sections/services-grid.tsx** (per D-03, UI-SPEC Service Cards):
    - Import `getServicesByCategory` from `@/data/services`, `Link` from `next/link`, `ArrowRight` from `lucide-react`
    - Import service-specific Lucide icons: `Hammer, RefreshCw, Search, Siren, Layers, Wrench, Building2, HardHat` (map one icon per service slug)
    - Create icon mapping: `const SERVICE_ICONS: Record<string, LucideIcon> = { 'roof-repair': Hammer, 'roof-replacement': RefreshCw, 'roof-inspection': Search, 'emergency-roofing': Siren, 'flat-roof-systems': Layers, 'roof-maintenance': Wrench, 'commercial-repair': Building2, 'commercial-replacement': HardHat }`
    - Uses SectionWrapper tone="secondary"
    - Heading: `<h2>Our Roofing Services</h2>` (font-heading font-bold text-[1.75rem])
    - Two sub-sections: "Residential Services" (h3) and "Commercial Services" (h3)
    - Grid: `<div className="grid grid-cols-2 lg:grid-cols-4 gap-4">`
    - Each card: `<Link href={`/services/${service.category}/${service.slug}`} className="group bg-dominant rounded-lg p-5 flex flex-col gap-3 hover:bg-secondary-lighter transition-colors duration-[150ms]">`
      - Icon: `<ServiceIcon size={24} className="text-accent" />`
      - Name: `<span className="font-heading font-bold text-lg text-text-primary">{service.name}</span>`
      - Description: `<span className="text-text-secondary text-lg line-clamp-2">{service.shortDescription.split('.')[0]}.</span>` (first sentence only)
      - Arrow: `<ArrowRight size={16} className="text-accent mt-auto group-hover:translate-x-1 transition-transform duration-[150ms]" />`

    **4. src/components/sections/why-choose-us.tsx** (Claude's discretion):
    - SectionWrapper tone="dominant"
    - Heading: `<h2>Why Choose Jersey City Quality Roofing</h2>`
    - 4 stat cards in a grid (2-col mobile, 4-col desktop):
      1. "20+ Years" / "Serving Hudson County since 2003"
      2. "2,500+" / "Roofing projects completed"
      3. "12 Cities" / "Every Hudson County municipality"
      4. "4.8 Stars" / "Average customer rating"
    - Each card: `<div className="bg-secondary rounded-lg p-6 text-center">` with large stat number (font-heading font-bold text-[2rem] text-accent) and description below (text-text-secondary text-lg)

    **5. src/components/sections/service-areas-overview.tsx** (for homepage):
    - Import `getMunicipalitiesByTier` from `@/data/municipalities`, `Link` from `next/link`, `MapPin` from `lucide-react`
    - SectionWrapper tone="dominant"
    - Heading: `<h2>Serving All of Hudson County</h2>`
    - Render Tier 1 cities as a featured row with larger text, then Tier 2 and Tier 3 as a compact grid
    - Each city: `<Link href={`/service-areas/${city.slug}`} className="group flex items-center gap-2 text-text-primary hover:text-accent transition-colors">`
    - MapPin icon + city name
    - Final row: `<Link href="/service-areas" className={cn(buttonVariants({ variant: "secondary" }))}>View All Service Areas</Link>`

    **6. src/components/sections/city-card.tsx** (per D-15, UI-SPEC City Cards):
    - Props: `{ city: Municipality; featured?: boolean }`
    - Import `Link` from `next/link`, `ArrowRight` from `lucide-react`
    - Card: `<Link href={`/service-areas/${city.slug}`} className={cn("group block bg-secondary rounded-lg p-5 hover:border-accent border border-secondary-lighter transition-colors duration-[150ms]", featured && "p-6")}>`
      - City name: `<h3 className={cn("font-heading font-bold text-text-primary", featured ? "text-[1.5rem]" : "text-lg")}>{city.name}</h3>`
      - Tagline: `<p className="text-text-secondary text-lg mt-1">{city.description.split('.')[0]}.</p>` (first sentence as tagline)
      - Stat: `<p className="text-text-secondary text-lg mt-2">Population: {city.population.toLocaleString()}</p>`
      - Link text: `<span className="inline-flex items-center gap-1 text-accent font-bold text-lg mt-3 group-hover:gap-2 transition-all duration-[150ms]">View Services <ArrowRight size={16} /></span>`

    **7. src/components/sections/city-card-grid.tsx** (per D-14, UI-SPEC CityCardGrid):
    - Import `getMunicipalitiesByTier` from `@/data/municipalities`, `CityCard` from `./city-card`
    - SectionWrapper tone="dominant"
    - Heading: `<h2>Our Service Areas</h2>`
    - Tier 1 grid: `<div className="grid grid-cols-2 lg:grid-cols-4 gap-4 mb-8">` with `<CityCard city={city} featured />`
    - Tier 2+3 grid: `<div className="grid grid-cols-2 lg:grid-cols-3 gap-4">` with `<CityCard city={city} />`
    - Get data: `const tier1 = getMunicipalitiesByTier(1); const tier2 = getMunicipalitiesByTier(2); const tier3 = getMunicipalitiesByTier(3);`

    **8. src/components/sections/business-hours-table.tsx** (per D-12):
    - Server Component, no SectionWrapper (used inside ContactInfoColumn)
    - Heading: `<h3 className="font-heading font-bold text-lg text-text-primary mb-3">Business Hours</h3>`
    - Table with rows:
      - Monday-Friday: 7:00 AM - 6:00 PM
      - Saturday: 8:00 AM - 2:00 PM
      - Sunday: Closed
    - Each row: `<div className="flex justify-between py-2 border-b border-secondary-lighter last:border-b-0">` with day (text-text-primary) and hours (text-text-secondary)
    - Below table: Emergency callout: `<div className="mt-4 bg-dominant rounded-lg p-4 flex items-center gap-3">` with `<Siren size={20} className="text-accent shrink-0" />` and `<p className="text-lg"><span className="font-bold text-accent">24/7 Emergency Service Available</span><br /><a href={PHONE_HREF} className="text-accent hover:underline underline-offset-4">{PHONE_NUMBER}</a></p>`

    **9. src/components/sections/contact-info-column.tsx** (per D-10, D-17):
    - Import BUSINESS_INFO, PHONE_NUMBER, PHONE_HREF, BusinessHoursTable
    - ALL data from BUSINESS_INFO -- zero hardcoded values
    - Heading: `<h2 className="font-heading font-bold text-[1.75rem] text-text-primary mb-6">Contact Information</h2>`
    - NAP block: Address (with MapPin icon), phone (with Phone icon, tel: link), email (with Mail icon, mailto: link)
    - License: `<p className="text-text-secondary text-lg mt-4">NJ HIC License: {BUSINESS_INFO.licenseNumber}</p>`
    - BusinessHoursTable component rendered below

    **10. src/components/sections/about-company-story.tsx** (per D-09):
    - Import `ABOUT_CONTENT` from `@/data/about-content` (created in Plan 01)
    - SectionWrapper tone="secondary"
    - Heading: `<h2>{ABOUT_CONTENT.companyStory.heading}</h2>`
    - Render each paragraph in ABOUT_CONTENT.companyStory.paragraphs as `<p className="text-text-secondary text-lg mt-4 leading-relaxed">`

    **11. src/components/sections/about-team-section.tsx** (per D-09):
    - Import `ABOUT_CONTENT` from `@/data/about-content`
    - SectionWrapper tone="dominant"
    - Heading: `<h2>Our Team</h2>`
    - Grid of team member cards (1-col mobile, 3-col desktop)
    - Each card: `<div className="bg-secondary rounded-lg p-6">` with placeholder image area (`<div className="w-20 h-20 rounded-full bg-dominant-lighter mx-auto mb-4" data-placeholder="team-photo" />`), name (h3, font-bold), role (text-accent), bio (text-text-secondary)

    **12. src/components/sections/about-certifications.tsx** (per D-09):
    - Import `BUSINESS_INFO` from `@/data/business-info`
    - SectionWrapper tone="secondary"
    - Heading: `<h2>Certifications & Credentials</h2>`
    - Grid of certification cards from BUSINESS_INFO.certifications
    - Each: icon + cert name (font-bold) + issuer + year
    - Include insurance info: provider, coverage types, policy note
    - Include license number: BUSINESS_INFO.licenseNumber

    NO component should hardcode phone numbers, addresses, or business names. ALL come from imports.
  </action>
  <verify>
    <automated>pnpm type-check && pnpm lint</automated>
  </verify>
  <acceptance_criteria>
    - src/components/sections/hero-section.tsx contains `export function HeroSection`
    - src/components/sections/hero-section.tsx contains `PHONE_HREF` (not hardcoded phone)
    - src/components/sections/hero-section.tsx contains `data-placeholder="hero-image"`
    - src/components/sections/hero-section.tsx contains `#quote-form`
    - src/components/sections/hero-section.tsx does NOT contain "use client"
    - src/components/sections/badge-strip.tsx contains `export function BadgeStrip`
    - src/components/sections/badge-strip.tsx contains `GAF Master Elite`
    - src/components/sections/badge-strip.tsx contains `bg-[#33382b]`
    - src/components/sections/badge-strip.tsx does NOT contain "use client"
    - src/components/sections/services-grid.tsx contains `getServicesByCategory`
    - src/components/sections/services-grid.tsx contains `grid-cols-2 lg:grid-cols-4`
    - src/components/sections/services-grid.tsx does NOT contain "use client"
    - src/components/sections/why-choose-us.tsx contains `export function WhyChooseUs`
    - src/components/sections/city-card.tsx contains `export function CityCard`
    - src/components/sections/city-card-grid.tsx contains `getMunicipalitiesByTier`
    - src/components/sections/business-hours-table.tsx contains `7:00 AM`
    - src/components/sections/business-hours-table.tsx contains `24/7 Emergency`
    - src/components/sections/contact-info-column.tsx contains `BUSINESS_INFO`
    - src/components/sections/contact-info-column.tsx does NOT contain hardcoded phone pattern "555-0123" outside of import
    - src/components/sections/about-company-story.tsx contains `ABOUT_CONTENT`
    - src/components/sections/about-team-section.tsx contains `ABOUT_CONTENT`
    - src/components/sections/about-certifications.tsx contains `BUSINESS_INFO`
    - pnpm type-check exits 0
  </acceptance_criteria>
  <done>All 12 server components render correctly with TypeScript strict mode. Every component pulls data from registries. No hardcoded NAP data. Hero has split layout with dual CTAs. Badge strip has 5 certification items on #33382b background. Services grid shows 8 cards in 2x4 layout. City card grid shows tiered layout. Contact info column shows full NAP from BUSINESS_INFO.</done>
</task>

</tasks>

<verification>
All Phase 4 section components verified:
- `pnpm type-check` passes with zero errors
- `pnpm lint` passes with zero errors
- All 16 component files exist in src/components/sections/
- No hardcoded NAP values in any component (verified by grep)
- Client components ("use client") limited to: testimonial-carousel, faq-accordion, google-map-embed
</verification>

<success_criteria>
1. Embla Carousel installed (embla-carousel-react, embla-carousel-autoplay in package.json)
2. TestimonialCarousel renders with Embla, autoplay, loop, ARIA attributes, prev/next navigation
3. FaqAccordion has aria-expanded, aria-controls, single-open, Motion height animation
4. GoogleMapEmbed lazy-loads iframe with API key fallback
5. All 12 server components export named functions, use data imports, zero hardcoded NAP
6. pnpm type-check and pnpm lint both pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-marketing-pages/04-02-SUMMARY.md`
</output>
