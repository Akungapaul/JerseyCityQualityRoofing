---
phase: 7
slug: location-hub-pages
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-25
---

# Phase 7 — UI Design Contract

> Visual and interaction contract for the 12 city hub pages. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (custom cva+cn pattern established Phase 2) |
| Preset | not applicable |
| Component library | none (custom primitives with cva + class-variance-authority) |
| Icon library | Lucide React 0.477.x |
| Font | Cormorant (headings, 700 weight), Cormorant Garamond (body, 500 weight) |

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding, divider spacing |
| sm | 8px | Compact element spacing, badge gaps |
| md | 16px | Default element spacing, card inner padding |
| lg | 24px | Section heading to content gap, card grid gaps |
| xl | 32px | Layout gaps between major content blocks |
| 2xl | 48px | Section vertical padding (mobile: py-12) |
| 3xl | 64px | Section vertical padding (sm: py-16), page-level hero padding |

Exceptions:
- 44px minimum touch target for all interactive elements (buttons, accordion triggers, phone links) -- per WCAG AA and established Phase 2 pattern (`min-h-[44px]`)
- 80px (`lg:py-20`) -- established hero/section desktop padding pattern carried forward from Phase 2 SectionWrapper and used consistently across Phase 5 residential and Phase 6 commercial service pages
- 96px (`lg:py-24`) -- hero large-desktop top/bottom padding, completing the mobile-to-desktop responsive ramp py-16 (64px) to py-20 (80px) to py-24 (96px) carried forward from existing hero section patterns in hero-section.tsx, service-hero.tsx, and about page
- 1280px max content width (`max-w-[1280px]`) -- established in SectionWrapper and all page templates

---

## Typography

| Role | Size | Weight | Line Height | Font Family |
|------|------|--------|-------------|-------------|
| Badge / Detail | 14px (0.875rem) | 500 (medium) or 700 (bold) | 1.5 | Cormorant Garamond |
| Body | 18px (1.125rem) | 500 (medium) | 1.7 | Cormorant Garamond |
| Section Heading (h2) | 28px (1.75rem) | 700 (bold) | 1.2 | Cormorant |
| Page Title (h1) | 40px mobile / 48px desktop (2.5rem / 3rem) | 700 (bold) | 1.1 | Cormorant |

Source: Established in Phase 2 globals.css and confirmed across all service page components. The 14px (0.875rem) size is used exclusively for badge text in compact visual elements (neighborhood roof type badges, key challenge labels, landmark significance tags).

Notes:
- All heading sizes use `font-heading font-bold` classes
- Body text uses `text-lg text-text-secondary` for secondary paragraphs, `text-text-primary` for primary
- Badge / Detail (14px) is reserved for compact inline badges and uppercase tracking labels only -- never for body copy or paragraph text
- Neighborhood subheadings (h3) use `text-lg font-heading font-bold text-text-primary` (same as FAQ accordion headings)

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | #2a2e22 (`bg-dominant`) | Page background, hero sections, odd-numbered content sections |
| Secondary (30%) | #3e4435 (`bg-secondary`) | Alternating section backgrounds, cards, CTA banners, mid-page CTA |
| Accent (10%) | #c89640 (`text-accent`, `bg-accent`) | See reserved-for list below |
| Destructive | #ef4444 (`text-destructive`) | Form validation error messages only |

Supporting colors (already defined in globals.css):
- `#33382b` (dominant-lighter) -- BadgeStrip background, gradient subtle layers
- `#4a5040` (secondary-lighter) -- Card borders, accordion dividers (`divide-[#4a5040]`, `border-[#4a5040]`)
- `#f0ede6` (text-primary) -- Headings, primary content text
- `#b0ae9e` (text-secondary) -- Supporting paragraphs, meta text, descriptions
- `#d8aa56` (accent-hover) -- Hover state for accent elements
- `#34d399` (success) -- Form submission success state

Accent reserved for:
- Primary CTA buttons ("Get Your Free Quote", "Get Free Estimate") -- `bg-accent text-dominant`
- Phone number links -- `text-accent`
- Accordion chevron icons -- `text-accent`
- Focus rings -- `ring-accent`
- "View Services" link arrows and hover states on cards -- `text-accent`
- Service card link hover borders -- `hover:border-accent`
- Stat highlight numbers in housing stock and weather data cards

---

## Component Inventory

### Reuse from Previous Phases (DO NOT rebuild)

| Component | Source | Props for Phase 7 |
|-----------|--------|-------------------|
| `SectionWrapper` | Phase 2 | `tone="dominant" \| "secondary"`, alternating per section |
| `ScrollReveal` | Phase 2 | Wrap each section's inner content |
| `BadgeStrip` | Phase 2 | No props needed, renders certification badges |
| `CTABanner` | Phase 2 | `heading`: city-specific, `subtext`: city-specific |
| `MidPageCTA` | Phase 5 | `heading`: city-specific (e.g., "Ready to Protect Your {cityName} Roof?") |
| `TestimonialCarousel` | Phase 4 | `testimonials`: pass result of `getTestimonialsByCity(slug)` |
| `FaqAccordion` | Phase 4 | `faqs`: city-specific FAQs from CityHubContent, `defaultOpenIndex={0}` |
| `QuoteForm` | Phase 3 | `defaultServiceType`: "Roofing Services" |
| `GoogleMapEmbed` | Phase 4 | `query`: "{cityName}, NJ", `title`: "Map of {cityName}", `zoom={13}` |
| `ServiceContentSection` | Phase 5 | `heading` + `content` for long narrative sections |

### New Components for Phase 7

| Component | File | Type | Purpose |
|-----------|------|------|---------|
| `CityHubHero` | `src/components/sections/city-hub-hero.tsx` | Server Component | City-specific hero with h1, population stat, county label, tagline, dual CTA (quote + phone) |
| `LocalExpertiseSection` | `src/components/sections/local-expertise-section.tsx` | Server Component | ~500 word first-person narrative about roofing expertise in this city |
| `HousingStockSection` | `src/components/sections/housing-stock-section.tsx` | Server Component | Data-rich section with stat cards (median age, total units, owner/renter split, dominant types) |
| `WeatherClimateSection` | `src/components/sections/weather-climate-section.tsx` | Server Component | Weather stats with roofing implications (snowfall, rainfall, hurricane risk, nor'easter frequency) |
| `NeighborhoodBreakdown` | `src/components/sections/neighborhood-breakdown.tsx` | Server Component | Grid of neighborhood cards with per-neighborhood roofing concerns, common roof types, key challenge |
| `ServicesInCityGrid` | `src/components/sections/services-in-city-grid.tsx` | Server Component | 2x4 grid of service cards with forward links to `/services/[category]/[service]/[city]` |
| `CityLandmarksSection` | `src/components/sections/city-landmarks-section.tsx` | Server Component | Landmarks list connecting local landmarks to roofing significance |

---

## New Component Visual Contracts

### CityHubHero

```
Layout: Full-width dominant background, py-16 sm:py-20 lg:py-24
Structure: Two-column on lg+ (60/40 split), single column on mobile
Left column:
  - County badge: inline-flex, bg-secondary, rounded-full, px-3 py-1, text-text-secondary, text-lg
    Copy: "Hudson County, NJ"
  - H1: font-heading font-bold text-[2.5rem] lg:text-[3rem] leading-[1.1] text-text-primary
    Copy: "{heroHeadline}" from CityHubContent
  - Subtitle: text-lg text-text-secondary mt-4 leading-relaxed
    Copy: "{heroSubtitle}" from CityHubContent
  - Population stat: text-lg text-text-secondary mt-2
    Copy: "Serving {population} residents"
  - CTA row: flex flex-wrap items-center gap-4 mt-8
    - Primary: anchor to #quote-form, bg-accent hover:bg-accent-hover text-dominant font-bold px-6 py-3 rounded-lg min-h-[44px]
      Copy: "Get Your Free Quote"
    - Phone: anchor with tel: href, text-accent flex items-center gap-2 text-lg font-bold min-h-[44px]
      Copy: Phone icon + PHONE_NUMBER
Right column:
  - CompactQuoteForm with defaultServiceType="Roofing Services"
```

### HousingStockSection

```
Layout: SectionWrapper tone="secondary"
Structure:
  - H2: "Housing & Architecture in {cityName}"
  - Narrative paragraph: text-lg text-text-secondary, ~400 words from housingStockNarrative
  - Stat cards grid: grid grid-cols-2 lg:grid-cols-4 gap-4 mt-8
    Each card:
      - bg-dominant rounded-lg p-5 border border-[#4a5040]
      - Stat number: text-[1.75rem] font-heading font-bold text-accent
      - Stat label: text-lg text-text-secondary mt-1
    Cards:
      1. "{medianAge} Years" / "Median Home Age"
      2. "{totalUnits}" / "Housing Units"
      3. "{ownerOccupied}%" / "Owner Occupied"
      4. "{renterOccupied}%" / "Renter Occupied"
  - Dominant types list: flex flex-wrap gap-2 mt-6
    Each type: inline-flex badge, bg-secondary rounded-full px-3 py-1 text-lg text-text-secondary
```

### WeatherClimateSection

```
Layout: SectionWrapper tone="dominant"
Structure:
  - H2: "Weather & Climate Impact on {cityName} Roofs"
  - Narrative paragraph: text-lg text-text-secondary, ~400 words from weatherClimateNarrative
  - Weather stat cards: grid grid-cols-2 lg:grid-cols-3 gap-4 mt-8
    Each card:
      - bg-secondary rounded-lg p-5
      - Icon: Lucide icon, size={24}, text-accent, aria-hidden="true"
      - Value: text-[1.5rem] font-heading font-bold text-text-primary mt-2
      - Label: text-lg text-text-secondary mt-1
    Cards:
      1. Snowflake icon / "{annualSnowfall} inches" / "Annual Snowfall"
      2. CloudRain icon / "{annualRainfall} inches" / "Annual Rainfall"
      3. Thermometer icon / "{avgSummerHigh}F" / "Average Summer High"
      4. Wind icon / "{avgWinterLow}F" / "Average Winter Low"
      5. AlertTriangle icon / "{hurricaneRisk}" / "Hurricane Risk"
      6. Cloud icon / "{norEasterFrequency}" / "Nor'easter Frequency"
  - Common concerns: ul, mt-6, list-disc list-inside
    Each: text-lg text-text-secondary, from commonWeatherConcerns array
```

### NeighborhoodBreakdown

```
Layout: SectionWrapper tone="dominant"
Structure:
  - H2: "Neighborhood Roofing Guide: {cityName}"
  - Grid: grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mt-8
    Each card:
      - bg-secondary rounded-lg p-6 border border-[#4a5040]
      - H3: text-lg font-heading font-bold text-text-primary
        Copy: "{neighborhood.name}"
      - Description: text-lg text-text-secondary mt-2, ~100 words
      - Common roof types: flex flex-wrap gap-2 mt-3
        Each: inline-flex badge, bg-dominant rounded-full px-3 py-1 text-text-secondary text-[0.875rem]
        (Badge / Detail size per typography table)
      - Key challenge: mt-3
        Label: text-text-secondary text-[0.875rem] font-bold uppercase tracking-wider
          Copy: "Key Challenge"
          (Badge / Detail size per typography table)
        Value: text-lg text-accent mt-1
          Copy: "{keyChallenge}"
```

### ServicesInCityGrid

```
Layout: SectionWrapper tone="secondary"
Structure:
  - H2: "Roofing Services in {cityName}"
  - Subheading: text-lg text-text-secondary mt-2
    Copy: "From emergency repairs to full replacements, we provide every roofing service {cityName} homeowners and businesses need."
  - Two sub-sections:
    - H3: "Residential Services" -- text-lg font-heading font-bold text-text-primary mt-8
    - Grid: grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 mt-4
    - H3: "Commercial Services" -- text-lg font-heading font-bold text-text-primary mt-8
    - Grid: grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 mt-4
  Each card:
    - Link component (next/link) with prefetch={false}
    - bg-dominant rounded-lg p-5 border border-[#4a5040] hover:border-accent transition-colors duration-[150ms]
    - Lucide icon: size={24}, text-accent, aria-hidden="true"
    - Service name: text-lg font-heading font-bold text-text-primary mt-3
    - "in {cityName}": text-text-secondary text-lg mt-1
    - Arrow: inline-flex items-center gap-1 text-accent font-bold text-lg mt-3 group-hover:gap-2 transition-all
      Copy: "Learn More" + ArrowRight icon size={16}
```

### CityLandmarksSection

```
Layout: SectionWrapper tone="secondary"
Structure:
  - H2: "Local Landmarks & Roofing Heritage in {cityName}"
  - Narrative paragraph: text-lg text-text-secondary, ~300 words from landmarksNarrative
  - Landmark cards: grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 mt-8
    Each card (from Municipality.landmarks array):
      - bg-dominant rounded-lg p-5 border border-[#4a5040]
      - H3: text-lg font-heading font-bold text-text-primary
        Copy: "{landmark.name}"
      - Description: text-lg text-text-secondary mt-2
        Copy: "{landmark.description}"
      - Significance badge: mt-3
        - inline-flex, bg-secondary rounded-full px-3 py-1, text-accent text-[0.875rem] font-bold
        Copy: "{landmark.significance}"
        (Badge / Detail size per typography table)
```

---

## Page Template: Section Order

City hub pages follow 14 sections in this order, alternating dominant/secondary tone per established pattern.

| # | Section | Tone | Component | Scroll Reveal |
|---|---------|------|-----------|---------------|
| 1 | City Hero | dominant | `CityHubHero` | No (above fold) |
| 2 | Badge Strip | secondary (custom bg) | `BadgeStrip` | No |
| 3 | Local Expertise Narrative | dominant | `ServiceContentSection` | Yes |
| 4 | Housing Stock & Architecture | secondary | `HousingStockSection` | Yes |
| 5 | Weather & Climate Impact | dominant | `WeatherClimateSection` | Yes |
| 6 | City Landmarks | secondary | `CityLandmarksSection` | Yes |
| 7 | Neighborhood Breakdown | dominant | `NeighborhoodBreakdown` | Yes |
| 8 | Mid-Page CTA | secondary (inherent) | `MidPageCTA` | No |
| 9 | Services in City Grid | secondary | `ServicesInCityGrid` | Yes |
| 10 | Building Codes & Permits | dominant | `ServiceContentSection` | Yes |
| 11 | City Testimonials | dominant (inherent) | `TestimonialCarousel` | No (has own) |
| 12 | FAQ Accordion | secondary | `FaqAccordion` | Yes |
| 13 | Quote Form | secondary (inherent) | `QuoteForm` | No |
| 14 | CTA Banner | secondary (inherent) | `CTABanner` | No |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (hero) | "Get Your Free Quote" |
| Secondary CTA (hero phone) | "Call {PHONE_NUMBER}" |
| Mid-page CTA heading | "Ready to Protect Your {cityName} Roof?" |
| Mid-page CTA subtext | "Free estimates with no obligation." |
| CTA Banner heading | "Your Trusted {cityName} Roofing Partner" |
| CTA Banner subtext | "Call now for a free inspection or request your no-obligation quote." |
| Services grid subheading | "From emergency repairs to full replacements, we provide every roofing service {cityName} homeowners and businesses need." |
| Neighborhood key challenge label | "Key Challenge" |
| Housing stat labels | "Median Home Age", "Housing Units", "Owner Occupied", "Renter Occupied" |
| Weather stat labels | "Annual Snowfall", "Annual Rainfall", "Average Summer High", "Average Winter Low", "Hurricane Risk", "Nor'easter Frequency" |
| Forward link text | "Learn More" (with ArrowRight icon) |
| Empty state (no city found) | Heading: "City Not Found" / Body: "The roofing service area you're looking for doesn't exist. Browse our service areas to find your city." / Action: Link to /service-areas |
| Error state (build/render) | Not applicable -- pages are statically generated. 404 handled by `notFound()` which uses Next.js default or custom not-found page. |
| FAQ section heading | "Frequently Asked Questions About Roofing in {cityName}" |
| Building codes section heading | "Building Codes & Permit Requirements in {cityName}" |
| Testimonials section heading | "What {cityName} Homeowners Say About Us" |

Destructive actions: None in this phase. City hub pages are read-only content pages with no user-modifiable state.

---

## Interaction Contracts

### Accordion (FAQ)
- Reuse existing `FaqAccordion` component
- First item open by default (`defaultOpenIndex={0}`)
- Single-open mode (opening one closes the previous)
- Keyboard: Enter/Space toggles, focus-visible ring on trigger
- Animation: height auto with 300ms ease-out via Motion library

### Testimonial Carousel
- Reuse existing `TestimonialCarousel` component
- Auto-play with Embla Carousel Autoplay plugin
- Left/right navigation arrows (ChevronLeft, ChevronRight)
- Keyboard accessible navigation buttons
- Previous arrow: `aria-label="Previous testimonial"`
- Next arrow: `aria-label="Next testimonial"`

### Service Card Links (Forward Links)
- `prefetch={false}` on all service-in-city links (pages do not exist until Phase 8)
- Hover: border color transitions from `border-[#4a5040]` to `border-accent` at 150ms
- Arrow gap expands on hover (`group-hover:gap-2`) for directional affordance

### Scroll Reveal
- Applied to sections 3-7, 9-10, 12 (content sections below the fold)
- NOT applied to hero, badge strip, mid-page CTA, testimonial carousel, quote form, CTA banner

### Google Map Embed
- One map per city page, zoomed to that municipality
- `query`: "{cityName}, NJ" (e.g., "Jersey City, NJ")
- `zoom`: 13 (city-level, wider than contact page's 14)
- Placed within the Local Expertise section or as a visual element in the hero area

### Phone Links
- All phone numbers wrapped in `tel:` links
- Minimum 44px touch target
- `text-accent` color with Phone icon from Lucide

---

## Responsive Behavior

| Breakpoint | Layout Changes |
|------------|----------------|
| Mobile (< 640px) | Single column everything. Hero stacks vertically (headline above form). Stat cards 2-column grid. Neighborhood cards single column. Service cards single column. Section padding py-12. |
| Tablet (640px-1023px) | Stat cards 2-column. Neighborhood cards 2-column. Service cards 2-column. Section padding py-16. |
| Desktop (1024px+) | Hero 60/40 split. Housing stats 4-column. Weather stats 3-column. Neighborhoods 3-column. Service cards 4-column. Section padding py-20. Max content width 1280px centered. |

---

## Accessibility Contracts

| Requirement | Implementation |
|-------------|----------------|
| One h1 per page | CityHubHero renders the single h1 |
| Heading hierarchy | h1 (hero) > h2 (section headings) > h3 (neighborhood names, landmark names, FAQ questions) |
| Semantic HTML | `<section>` via SectionWrapper, `<main>` wrapper in layout, `<nav>` for breadcrumbs |
| Focus visible | 2px accent ring with 2px dominant offset on all interactive elements |
| Touch targets | 44px minimum height on all buttons, links, accordion triggers |
| Alt text | Decorative icons use `aria-hidden="true"`. No content images in this phase (placeholder SVGs if needed use aria-hidden). |
| Color contrast | text-primary (#f0ede6) on dominant (#2a2e22) = 11.2:1 ratio (AAA). text-secondary (#b0ae9e) on dominant = 5.8:1 ratio (AA). accent (#c89640) on dominant = 5.1:1 ratio (AA). |
| Reduced motion | Global `prefers-reduced-motion: reduce` media query disables all animations |
| Keyboard navigation | Tab order follows visual order. Accordion triggers focusable. Carousel arrows focusable. All links focusable. |
| Carousel navigation aria | Previous arrow button: `aria-label="Previous testimonial"`. Next arrow button: `aria-label="Next testimonial"`. |

---

## SEO Visual Elements

| Element | Specification |
|---------|---------------|
| JSON-LD blocks | Two `<script type="application/ld+json">` blocks per page: RoofingContractor + BreadcrumbList. Rendered via existing `JsonLd` component. Not visible to users. |
| Breadcrumbs | Rendered by existing `Breadcrumbs` component in layout. Visual path: Home > Service Areas > {City Name} |
| OG metadata | `generateMetadata()` with title, description, canonical, openGraph per established pattern |
| Internal links | All service-in-city forward links use `next/link` with `prefetch={false}`. All CTA links to /contact or #quote-form use `next/link` or anchor. |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable -- project does not use shadcn |
| Third-party | none | not applicable |

Project uses custom cva+cn() component pattern without shadcn. No registry dependencies.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
