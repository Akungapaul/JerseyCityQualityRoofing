---
phase: 02-design-system-layout-shell
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - src/components/sections/section-wrapper.tsx
  - src/components/sections/cta-banner.tsx
  - src/components/sections/scroll-reveal.tsx
  - src/app/(marketing)/layout.tsx
  - src/app/(marketing)/page.tsx
autonomous: false
requirements:
  - FNDN-05
  - FNDN-07
  - UX-03
  - UX-04
  - UX-06
  - UX-08

must_haves:
  truths:
    - "Every marketing page renders within the layout shell: Header at top, Breadcrumbs below header, page content in main, Footer at bottom"
    - "SectionWrapper component applies consistent spacing and alternating dark tone backgrounds for long content pages"
    - "CTABanner component renders a full-width conversion strip with phone number and quote button, usable at mid-page and above footer"
    - "ScrollReveal component wraps content with fade-in-up animation triggered at 20% viewport visibility, respecting prefers-reduced-motion"
    - "The homepage renders a test layout proving header, breadcrumbs, sections, and footer work together end-to-end"
    - "User has visually verified the full layout shell renders correctly in browser"
  artifacts:
    - path: "src/components/sections/section-wrapper.tsx"
      provides: "Section spacing with alternating tone backgrounds"
      exports: ["SectionWrapper"]
    - path: "src/components/sections/cta-banner.tsx"
      provides: "Full-width conversion strip component"
      exports: ["CTABanner"]
    - path: "src/components/sections/scroll-reveal.tsx"
      provides: "Motion-powered scroll animation wrapper"
      exports: ["ScrollReveal"]
    - path: "src/app/(marketing)/layout.tsx"
      provides: "Composed marketing layout with Header + Breadcrumbs + main + Footer"
      min_lines: 10
  key_links:
    - from: "src/app/(marketing)/layout.tsx"
      to: "src/components/layout/header.tsx"
      via: "Header import and render"
      pattern: "import.*Header.*from.*layout/header"
    - from: "src/app/(marketing)/layout.tsx"
      to: "src/components/layout/footer.tsx"
      via: "Footer import and render"
      pattern: "import.*Footer.*from.*layout/footer"
    - from: "src/app/(marketing)/layout.tsx"
      to: "src/components/layout/breadcrumbs.tsx"
      via: "Breadcrumbs import and render"
      pattern: "import.*Breadcrumbs.*from.*layout/breadcrumbs"
    - from: "src/components/sections/scroll-reveal.tsx"
      to: "motion/react"
      via: "whileInView for scroll-triggered animation"
      pattern: "import.*motion.*from.*motion/react"
---

<objective>
Build section utility components (SectionWrapper, CTABanner, ScrollReveal), wire the complete marketing layout in (marketing)/layout.tsx composing Header + Breadcrumbs + main + Footer, update the homepage with a test layout proving all components work, and get user visual verification.

Purpose: This plan completes the layout shell by connecting all components into the page render pipeline and providing section-level utilities that content phases (4-10) will use for every page. The visual verification checkpoint confirms the design system and layout work correctly before moving to content phases.

Output: Complete marketing layout shell rendering on every page, section utility components, verified in browser.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-design-system-layout-shell/02-CONTEXT.md
@.planning/phases/02-design-system-layout-shell/02-UI-SPEC.md
@.planning/phases/02-design-system-layout-shell/02-01-SUMMARY.md
@.planning/phases/02-design-system-layout-shell/02-02-SUMMARY.md

@src/styles/globals.css
@src/lib/utils.ts
@src/lib/constants.ts
@src/app/(marketing)/layout.tsx
@src/app/(marketing)/page.tsx
@src/components/layout/header.tsx
@src/components/layout/footer.tsx
@src/components/layout/breadcrumbs.tsx
@src/components/ui/button.tsx

<interfaces>
From src/components/layout/header.tsx (Plan 02):
```typescript
export function Header(): JSX.Element
// Renders: skip-to-content link + sticky header with MegaMenu, phone, CTA, hamburger
```

From src/components/layout/footer.tsx (Plan 02):
```typescript
export function Footer(): JSX.Element
// Renders: CTA banner + multi-column sitemap + certification badges + copyright
```

From src/components/layout/breadcrumbs.tsx (Plan 02):
```typescript
export function Breadcrumbs(): JSX.Element
// Renders: route-aware breadcrumb trail + BreadcrumbList JSON-LD. Skips homepage.
```

From src/components/ui/button.tsx (Plan 01):
```typescript
export function Button(props: ButtonProps): JSX.Element
export const buttonVariants: CVA
// Variants: primary, secondary, phone. Sizes: default, compact.
```

From src/lib/constants.ts:
```typescript
export const SITE_NAME = "Jersey City Quality Roofing";
export const PHONE_NUMBER = "(201) 555-0123";
export const PHONE_HREF = "tel:+12015550123";
```

From globals.css @theme (Plan 01):
Colors: dominant, dominant-lighter, secondary, secondary-lighter, accent, text-primary, text-secondary.
Spacing: xs(4) sm(8) md(16) lg(24) xl(32) 2xl(48) 3xl(64) 4xl(80) 5xl(120).
Durations: fast(150ms) base(300ms) slow(600ms).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build section utility components and wire marketing layout</name>
  <files>src/components/sections/section-wrapper.tsx, src/components/sections/cta-banner.tsx, src/components/sections/scroll-reveal.tsx, src/app/(marketing)/layout.tsx, src/app/(marketing)/page.tsx</files>
  <read_first>
    - src/app/(marketing)/layout.tsx (current pass-through layout to be replaced)
    - src/app/(marketing)/page.tsx (current homepage stub to be updated)
    - src/styles/globals.css (color and spacing tokens)
    - src/components/layout/header.tsx (Header export to compose)
    - src/components/layout/footer.tsx (Footer export to compose)
    - src/components/layout/breadcrumbs.tsx (Breadcrumbs export to compose)
    - src/components/ui/button.tsx (Button for CTA banner)
    - src/lib/constants.ts (PHONE_NUMBER, PHONE_HREF)
    - .planning/phases/02-design-system-layout-shell/02-UI-SPEC.md (section wrapper, CTA banner, scroll reveal specs)
  </read_first>
  <action>
Create 3 section components, then update 2 existing files.

**FILE 1: src/components/sections/section-wrapper.tsx**

Server Component (no interactivity, just layout/styling wrapper).

Props: children (React.ReactNode), tone ("dominant" | "secondary", default "dominant"), id (string, optional for anchor links), className (string, optional).

Implementation: Wraps children in a section element (semantic HTML per UX-04). tone="dominant" applies bg-dominant, tone="secondary" applies bg-secondary. Responsive vertical padding: py-12 (48px mobile), sm:py-16 (64px tablet), lg:py-20 (80px desktop). Inner container: max-w-[1280px] mx-auto px-4 sm:px-6 lg:px-8. Merges additional className via cn() utility. If id provided, set as id attribute.

```typescript
import { cn } from "@/lib/utils";

interface SectionWrapperProps {
  children: React.ReactNode;
  tone?: "dominant" | "secondary";
  id?: string;
  className?: string;
}

export function SectionWrapper({ children, tone = "dominant", id, className }: SectionWrapperProps) {
  return (
    <section id={id} className={cn("py-12 sm:py-16 lg:py-20", tone === "dominant" ? "bg-dominant" : "bg-secondary", className)}>
      <div className="max-w-[1280px] mx-auto px-4 sm:px-6 lg:px-8">{children}</div>
    </section>
  );
}
```

**FILE 2: src/components/sections/cta-banner.tsx**

Server Component (static conversion strip, no interactivity beyond links).

Imports: next/link (Link), lucide-react (Phone), @/components/ui/button (Button), @/lib/constants (PHONE_NUMBER, PHONE_HREF), @/lib/utils (cn).

Props: heading (string, default "Ready to Protect Your Roof?"), subtext (string, default "Call now for a free inspection or request your no-obligation quote."), className (string, optional).

Structure: div with bg-secondary py-8 lg:py-10. Container max-w-[1280px] mx-auto px-4 sm:px-6 lg:px-8. Flex layout: col on mobile, row on sm+ (flex-col sm:flex-row items-center justify-between gap-4 sm:gap-6). Left side: p element with heading text (font-heading font-bold text-[1.75rem] text-text-primary leading-[1.2]) + p element with subtext (text-text-secondary text-lg mt-1). Right side: flex items-center gap-3 shrink-0 with Button variant="phone" wrapping a href={PHONE_HREF} (Phone icon + PHONE_NUMBER) and Button variant="primary" wrapping Link href="/contact" ("Get Free Quote").

Key rules: Uses p for heading text (NOT h2) because this component may appear mid-page where heading hierarchy would break. The footer.tsx has its own CTA banner with proper h2. Min height 96px achieved via py-8 padding + content. Phone uses PHONE_HREF (tel: per UX-08). Quote links to /contact via next/link (UX-08). All text 18px minimum.

**FILE 3: src/components/sections/scroll-reveal.tsx**

"use client" component (needs Motion whileInView).

Imports: motion/react (motion), @/lib/utils (cn).

Props: children (React.ReactNode), className (string, optional), delay (number, default 0 for stagger support).

Implementation using Motion library:

```typescript
"use client";
import { motion } from "motion/react";
import { cn } from "@/lib/utils";

interface ScrollRevealProps {
  children: React.ReactNode;
  className?: string;
  delay?: number;
}

const variants = { hidden: { opacity: 0, y: 24 }, visible: { opacity: 1, y: 0 } };

export function ScrollReveal({ children, className, delay = 0 }: ScrollRevealProps) {
  return (
    <motion.div
      initial="hidden"
      whileInView="visible"
      viewport={{ once: true, amount: 0.2 }}
      variants={variants}
      transition={{ duration: 0.6, ease: "easeOut", delay }}
      className={cn(className)}
    >
      {children}
    </motion.div>
  );
}
```

Animation per UI-SPEC: fade-in-up (opacity 0->1, y 24->0). Duration 600ms. Easing easeOut. Trigger at 20% viewport visibility (amount: 0.2). Fires once (once: true). delay prop enables stagger (pass 0, 0.1, 0.2 for sibling items). Reduced motion: Motion library respects prefers-reduced-motion natively; globals.css also has blanket 0ms override (D-18).

**FILE 4: Update src/app/(marketing)/layout.tsx**

Replace the current pass-through layout. Server Component (Header is client but rendered as child).

```typescript
import { Header } from "@/components/layout/header";
import { Footer } from "@/components/layout/footer";
import { Breadcrumbs } from "@/components/layout/breadcrumbs";

export default function MarketingLayout({ children }: { children: React.ReactNode }) {
  return (
    <>
      <Header />
      <Breadcrumbs />
      <main id="main-content">{children}</main>
      <Footer />
    </>
  );
}
```

main id="main-content" is the skip-to-content target. No h1 in layout (each page provides its own). Breadcrumbs between header and main (auto-skip on homepage). Semantic structure: header + main + footer per UX-04.

**FILE 5: Update src/app/(marketing)/page.tsx**

Update homepage stub with test content exercising all section components. This is NOT the final homepage (Phase 4). This verifies the layout shell works.

IMPORTANT: Per CLAUDE.md, every page MUST export metadata with title, description, and openGraph. Add a static metadata export at the top of the file.

```typescript
import type { Metadata } from "next";
import { SectionWrapper } from "@/components/sections/section-wrapper";
import { CTABanner } from "@/components/sections/cta-banner";
import { ScrollReveal } from "@/components/sections/scroll-reveal";

export const metadata: Metadata = {
  title: "Jersey City Quality Roofing | Hudson County's Trusted Roofing Experts",
  description:
    "Professional residential and commercial roofing services across all 12 Hudson County municipalities. Licensed, insured, and locally trusted since 2003.",
  openGraph: {
    title: "Jersey City Quality Roofing | Hudson County's Trusted Roofing Experts",
    description:
      "Professional residential and commercial roofing services across all 12 Hudson County municipalities. Licensed, insured, and locally trusted since 2003.",
    type: "website",
  },
};

export default function HomePage() {
  return (
    <>
      <SectionWrapper tone="dominant" className="lg:py-[120px]">
        <ScrollReveal>
          <h1 className="font-heading font-medium text-[2.25rem] sm:text-[2.75rem] lg:text-[3.5rem] leading-[1.1] text-text-primary max-w-3xl">
            Hudson County&rsquo;s Trusted Roofing Experts
          </h1>
          <p className="text-text-secondary text-lg mt-6 max-w-2xl">
            Professional residential and commercial roofing services across all 12 Hudson County municipalities. Licensed, insured, and locally trusted since 2003.
          </p>
        </ScrollReveal>
      </SectionWrapper>

      <SectionWrapper tone="secondary">
        <ScrollReveal>
          <h2 className="font-heading font-bold text-[1.75rem] text-text-primary">Our Services</h2>
          <p className="text-text-secondary text-lg mt-3">
            From emergency repairs to full commercial roof replacements, we handle every roofing need in Hudson County.
          </p>
        </ScrollReveal>
      </SectionWrapper>

      <CTABanner />

      <SectionWrapper tone="dominant">
        <ScrollReveal>
          <h2 className="font-heading font-bold text-[1.75rem] text-text-primary">Serving All 12 Hudson County Municipalities</h2>
          <p className="text-text-secondary text-lg mt-3">
            Jersey City, Hoboken, Bayonne, North Bergen, Union City, West New York, Secaucus, Kearny, Harrison, East Newark, Guttenberg, and Weehawken.
          </p>
        </ScrollReveal>
      </SectionWrapper>

      <SectionWrapper tone="secondary">
        <ScrollReveal>
          <h2 className="font-heading font-bold text-[1.75rem] text-text-primary">Why Choose Us</h2>
          <p className="text-text-secondary text-lg mt-3">
            GAF Master Elite Contractor. CertainTeed SELECT ShingleMaster. Over 20 years serving Hudson County homeowners and property managers.
          </p>
        </ScrollReveal>
      </SectionWrapper>
    </>
  );
}
```

This test page verifies: Header renders at top with sticky behavior. Breadcrumbs skip on homepage. SectionWrapper alternates dominant/secondary tones. ScrollReveal animates on scroll. CTABanner renders mid-page. Footer renders at bottom. Typography uses correct fonts and sizes (h1 hero 56px responsive, h2 section 28px, body 18px). Metadata export satisfies CLAUDE.md SEO requirement.
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/JerseyCityQualityRoofing && pnpm type-check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/sections/section-wrapper.tsx exports SectionWrapper
    - src/components/sections/section-wrapper.tsx contains tone prop with "dominant" and "secondary"
    - src/components/sections/section-wrapper.tsx contains section element
    - src/components/sections/section-wrapper.tsx contains max-w-[1280px]
    - src/components/sections/section-wrapper.tsx contains py-12 sm:py-16 lg:py-20
    - src/components/sections/cta-banner.tsx exports CTABanner
    - src/components/sections/cta-banner.tsx contains "Ready to Protect Your Roof?"
    - src/components/sections/cta-banner.tsx contains PHONE_HREF import
    - src/components/sections/cta-banner.tsx contains "Get Free Quote"
    - src/components/sections/scroll-reveal.tsx contains "use client"
    - src/components/sections/scroll-reveal.tsx exports ScrollReveal
    - src/components/sections/scroll-reveal.tsx contains whileInView
    - src/components/sections/scroll-reveal.tsx contains once true
    - src/components/sections/scroll-reveal.tsx contains amount 0.2
    - src/components/sections/scroll-reveal.tsx contains duration 0.6
    - src/app/(marketing)/layout.tsx contains import Header from layout/header
    - src/app/(marketing)/layout.tsx contains import Footer from layout/footer
    - src/app/(marketing)/layout.tsx contains import Breadcrumbs from layout/breadcrumbs
    - src/app/(marketing)/layout.tsx contains main id="main-content"
    - src/app/(marketing)/layout.tsx does NOT contain h1
    - src/app/(marketing)/page.tsx exports metadata with title, description, and openGraph
    - src/app/(marketing)/page.tsx contains exactly one h1
    - src/app/(marketing)/page.tsx contains SectionWrapper
    - src/app/(marketing)/page.tsx contains ScrollReveal
    - src/app/(marketing)/page.tsx contains CTABanner
    - pnpm type-check passes with zero errors
  </acceptance_criteria>
  <done>Three section utility components built. Marketing layout composes Header + Breadcrumbs + main + Footer. Homepage exports metadata (title, description, openGraph) per CLAUDE.md and shows test content with alternating sections, scroll animations, and mid-page CTA. Type-check passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete layout shell</name>
  <what-built>Complete design system and layout shell: approved color palette applied to all components, sticky header with shrink-on-scroll, mega-menu navigation with service silos, full-screen mobile navigation overlay, breadcrumb trail, section wrappers with alternating dark tones, CTA banners, scroll reveal animations, and full sitemap footer. All wired into the marketing layout that wraps every page.</what-built>
  <how-to-verify>
    1. Run `pnpm build` to confirm production build succeeds (one-time final validation)
    2. Start the dev server: pnpm dev (opens at http://localhost:3000)
    3. Desktop (1024px+) checks:
       - Header is sticky at top with logo, nav links, phone number, and "Get Free Quote" button
       - Scroll down -- header shrinks smoothly from tall to compact
       - Hover "Residential Services" in nav -- mega-menu panel shows 4 service links
       - Hover "Commercial Services" -- panel shows 4 commercial service links
       - Hover "Service Areas" -- panel shows 12 municipality links
       - Tab through page -- focus rings visible on all interactive elements
       - Sections alternate between two dark tones (dominant/secondary)
       - Scroll animations fire smoothly (sections fade-in-up as entering viewport)
       - Mid-page CTA banner shows phone number and "Get Free Quote" button
       - Footer shows CTA banner strip, 4-column sitemap, certification badges, copyright
    4. Mobile (resize to less than 640px) checks:
       - Hamburger icon appears (Menu icon)
       - Tap hamburger -- full-screen overlay slides in from right
       - Phone number and CTA button prominent at top of overlay
       - Accordion sections expand/collapse for Residential, Commercial, Service Areas
       - Close button (X) works
       - Body scroll locked while overlay open
    5. Accessibility checks:
       - Tab through page -- all interactive elements get focus rings
       - Escape in mega-menu -- it closes
       - Enable "Reduce motion" in OS -- scroll animations disabled
    6. Typography: headings in Cormorant serif, body in Cormorant Garamond, no text smaller than 18px
    7. Navigate to /about -- breadcrumbs show "Home > About Us"
    8. Navigate to /services/residential/roof-repair -- breadcrumbs show "Home > Services > Residential Services > Roof Repair"
  </how-to-verify>
  <resume-signal>Type "approved" if the layout shell looks correct, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
- pnpm type-check passes (zero TypeScript errors)
- pnpm build succeeds (production build compiles all components — verified during checkpoint)
- pnpm lint passes (zero ESLint errors)
- Homepage renders complete layout shell (header, sections, CTA, footer)
- Homepage exports metadata with title, description, and openGraph
- Mega-menu populates from data registries
- Header shrinks on scroll
- Mobile nav opens/closes with body scroll lock
- Breadcrumbs auto-generate and inject JSON-LD
- Section alternation visible between dominant/secondary tones
- Scroll reveal animations fire on viewport entry
- User has visually approved the layout
</verification>

<success_criteria>
1. SectionWrapper applies alternating dark tones with responsive padding
2. CTABanner renders phone + quote button with correct copy
3. ScrollReveal animates with 600ms fade-in-up, respects prefers-reduced-motion
4. Marketing layout composes Header + Breadcrumbs + main + Footer on every page
5. Homepage test content proves all components work together
6. Homepage exports metadata (title, description, openGraph) per CLAUDE.md
7. pnpm type-check passes
8. pnpm build succeeds (verified during visual checkpoint)
9. User visually approves the complete layout shell in browser
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system-layout-shell/02-03-SUMMARY.md`
</output>