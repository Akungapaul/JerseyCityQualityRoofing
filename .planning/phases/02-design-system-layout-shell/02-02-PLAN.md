---
phase: 02-design-system-layout-shell
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/layout/header.tsx
  - src/components/layout/mega-menu.tsx
  - src/components/layout/mobile-nav.tsx
  - src/components/layout/footer.tsx
  - src/components/layout/breadcrumbs.tsx
  - src/lib/seo/json-ld.tsx
autonomous: true
requirements:
  - FNDN-05
  - FNDN-07
  - UX-03
  - UX-04
  - UX-05
  - UX-08
  - SEO-06
  - CRO-02
  - CRO-04

must_haves:
  truths:
    - "Sticky header displays company logo, mega-menu navigation, click-to-call phone number, and Get Free Quote CTA button at all scroll positions"
    - "Header shrinks from 80px to 56px on scroll past 100px with smooth Motion transition"
    - "Mega-menu flyout shows all services grouped by Residential and Commercial silos, plus all 12 locations"
    - "Full-screen mobile navigation overlay opens from hamburger with accordion silo sections and phone/CTA at top"
    - "Footer displays CTA banner strip above multi-column sitemap with all service links, location links, company links, and NAP data"
    - "Breadcrumbs auto-generate from current route and inject BreadcrumbList JSON-LD schema"
    - "All navigation uses next/link, all phone numbers use tel: links"
    - "All interactive elements are keyboard-accessible with visible focus rings"
  artifacts:
    - path: "src/components/layout/header.tsx"
      provides: "Sticky header with shrink-on-scroll, phone link, CTA button"
      exports: ["Header"]
    - path: "src/components/layout/mega-menu.tsx"
      provides: "Desktop flyout navigation with service silo panels"
      exports: ["MegaMenu"]
    - path: "src/components/layout/mobile-nav.tsx"
      provides: "Full-screen mobile navigation overlay with accordion sections"
      exports: ["MobileNav"]
    - path: "src/components/layout/footer.tsx"
      provides: "Full sitemap footer with CTA banner"
      exports: ["Footer"]
    - path: "src/components/layout/breadcrumbs.tsx"
      provides: "Route-aware breadcrumb trail with JSON-LD"
      exports: ["Breadcrumbs"]
  key_links:
    - from: "src/components/layout/header.tsx"
      to: "src/components/ui/button.tsx"
      via: "Button component for CTA and phone"
      pattern: "import.*Button.*from.*ui/button"
    - from: "src/components/layout/header.tsx"
      to: "src/data/business-info.ts"
      via: "BUSINESS_INFO for phone number"
      pattern: "import.*BUSINESS_INFO.*from.*business-info"
    - from: "src/components/layout/mega-menu.tsx"
      to: "src/data/services.ts"
      via: "getServicesByCategory for navigation links"
      pattern: "import.*getServicesByCategory.*from.*services"
    - from: "src/components/layout/mega-menu.tsx"
      to: "src/data/municipalities.ts"
      via: "getAllMunicipalities for location links"
      pattern: "import.*getAllMunicipalities.*from.*municipalities"
    - from: "src/components/layout/breadcrumbs.tsx"
      to: "src/lib/seo/json-ld.tsx"
      via: "buildBreadcrumbJsonLd for schema injection"
      pattern: "import.*buildBreadcrumbJsonLd.*from.*json-ld"
    - from: "src/components/layout/footer.tsx"
      to: "src/data/business-info.ts"
      via: "BUSINESS_INFO for NAP data"
      pattern: "import.*BUSINESS_INFO.*from.*business-info"
---

<objective>
Build all layout components: sticky Header with shrink-on-scroll behavior, MegaMenu desktop navigation, MobileNav full-screen overlay, Footer with CTA banner and full sitemap, and route-aware Breadcrumbs with BreadcrumbList JSON-LD.

Purpose: These components form the persistent layout shell that wraps every marketing page. The header provides always-visible conversion elements (phone + CTA). The footer provides secondary navigation and SEO internal linking. Breadcrumbs provide route context and rich snippet eligibility.

Output: Five layout components ready for composition in the marketing layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-design-system-layout-shell/02-CONTEXT.md
@.planning/phases/02-design-system-layout-shell/02-UI-SPEC.md
@.planning/phases/02-design-system-layout-shell/02-01-SUMMARY.md

@src/styles/globals.css
@src/lib/utils.ts
@src/lib/constants.ts
@src/data/business-info.ts
@src/data/services.ts
@src/data/municipalities.ts
@src/data/types.ts
@src/lib/seo/json-ld.tsx
@src/components/ui/button.tsx
@src/components/ui/badge.tsx

<interfaces>
From src/lib/constants.ts:
```typescript
export const SITE_NAME = "Jersey City Quality Roofing";
export const BASE_URL = "https://www.jerseycityqualityroofing.com";
export const PHONE_NUMBER = "(201) 555-0123";
export const PHONE_HREF = "tel:+12015550123";
```

From src/data/business-info.ts:
```typescript
export const BUSINESS_INFO: BusinessInfo = {
  name: "Jersey City Quality Roofing",
  phone: "(201) 555-0123",
  email: "info@jerseycityqualityroofing.com",
  address: { street: "123 Summit Avenue", city: "Jersey City", state: "NJ", zip: "07304" },
  serviceAreas: ["Jersey City", "Hoboken", "Bayonne", ...], // 12 cities
  foundedYear: 2003,
  licenseNumber: "NJ-HIC-13VH12345678",
  certifications: [
    { name: "GAF Master Elite Contractor", issuer: "GAF", year: 2020 },
    { name: "CertainTeed SELECT ShingleMaster", issuer: "CertainTeed", year: 2019 },
    { name: "OSHA 30-Hour Safety Certified", issuer: "OSHA", year: 2022 },
    { name: "EPA Lead-Safe Certified Firm", issuer: "EPA", year: 2021 },
  ],
  operatingHours: "Mon-Fri 7:00 AM - 6:00 PM, Sat 8:00 AM - 2:00 PM",
  emergencyAvailable: true,
};
```

From src/data/services.ts:
```typescript
export function getServicesByCategory(category: ServiceCategory): Service[]
// Residential: roof-repair, roof-replacement, roof-inspection, emergency-roofing
// Commercial: flat-roof-systems, roof-maintenance, commercial-repair, commercial-replacement
```

From src/data/municipalities.ts:
```typescript
export function getAllMunicipalities(): Municipality[]
// 12 municipalities, each has .name and .slug
```

From src/data/types.ts:
```typescript
export type ServiceCategory = "residential" | "commercial";
export interface Service { name: string; slug: string; category: ServiceCategory; shortDescription: string; ... }
export interface Municipality { name: string; slug: string; ... }
```

From src/lib/seo/json-ld.tsx:
```typescript
export function buildBreadcrumbJsonLd(items: Array<{ name: string; url: string }>): WithContext<BreadcrumbList>
export function JsonLd({ data }: { data: Record<string, unknown> }): JSX.Element
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Header with shrink-on-scroll, MegaMenu, and MobileNav</name>
  <files>src/components/layout/header.tsx, src/components/layout/mega-menu.tsx, src/components/layout/mobile-nav.tsx</files>
  <read_first>
    - src/components/ui/button.tsx (Button variants to use for CTA and phone)
    - src/styles/globals.css (color tokens)
    - src/data/services.ts (getServicesByCategory function signature and return type)
    - src/data/municipalities.ts (getAllMunicipalities function signature)
    - src/data/business-info.ts (BUSINESS_INFO shape for phone, certifications)
    - src/lib/constants.ts (PHONE_NUMBER, PHONE_HREF, SITE_NAME)
    - src/data/types.ts (Service, Municipality, ServiceCategory interfaces)
    - .planning/phases/02-design-system-layout-shell/02-UI-SPEC.md (interaction contracts for header shrink, mega-menu, mobile-nav)
  </read_first>
  <action>
Create 3 files. All are "use client" components because they require browser APIs (scroll events, hover/click state, body scroll lock).

**FILE 1: src/components/layout/header.tsx**

Imports: motion/react (motion, useScroll, useMotionValueEvent), react (useState), next/link (Link), lucide-react (Phone, Menu), @/components/ui/button (Button), @/lib/constants (SITE_NAME, PHONE_NUMBER, PHONE_HREF), @/components/layout/mega-menu (MegaMenu), @/components/layout/mobile-nav (MobileNav).

Structure: Skip-to-content link before header + sticky motion.header (top-0, z-50, bg-dominant) containing max-w-[1280px] centered container with px-4 sm:px-6 lg:px-8.

Inside the header flex row: (1) Logo as Link to "/" with SITE_NAME in font-heading font-bold text-text-primary. (2) Desktop nav (hidden lg:flex) containing MegaMenu. (3) Right side with phone link (a href={PHONE_HREF} with Phone icon + PHONE_NUMBER text hidden on mobile, visible sm+), CTA Button variant="primary" size="compact" linking to /contact with text "Get Free Quote" in full state and "Free Quote" in shrunk state, and hamburger button (lg:hidden, min-h-[44px] min-w-[44px], aria-label="Open navigation menu", Menu icon). MobileNav rendered below header with isOpen/onClose props.

Shrink behavior per D-09: Use useScroll() to get scrollY, useMotionValueEvent to detect scrollY > 100px and set isScrolled state. Full state: height 80px, full logo. Shrunk state: height 56px, ~75% logo scale. Animate via motion.header animate={{ height: isScrolled ? 56 : 80 }} transition={{ duration: 0.3, ease: "easeOut" }}. Phone and CTA visible in BOTH states (CRO-02, CRO-04).

Accessibility: Skip-to-content with className="skip-to-content" targeting #main-content. Semantic header element. nav aria-label="Main navigation". Hamburger aria-label="Open navigation menu". All buttons min-h-[44px] min-w-[44px] touch targets.

**FILE 2: src/components/layout/mega-menu.tsx**

Imports: react (useState, useRef, useCallback, useEffect), next/link (Link), motion/react (motion, AnimatePresence), lucide-react (ChevronDown), @/data/services (getServicesByCategory), @/data/municipalities (getAllMunicipalities).

Three top-level nav items: "Residential Services", "Commercial Services", "Service Areas". Each item is a button (not link) with aria-expanded, aria-haspopup="true", onMouseEnter/Leave/Focus/Click/KeyDown handlers. Text styled as font-body font-bold text-lg tracking-[0.02em] text-text-primary with ChevronDown indicator.

AnimatePresence wraps each panel. Panel animates with initial={{ opacity: 0, y: -8 }}, animate={{ opacity: 1, y: 0 }}, exit={{ opacity: 0, y: -8 }}, transition={{ duration: 0.2, ease: "easeOut" }}. Panel class: absolute left-0 top-full mt-2 bg-secondary rounded-lg shadow-xl p-6 min-w-[400px] z-50.

Panel content: Residential panel shows grid of services from getServicesByCategory('residential') with Link href=/services/residential/${slug}, service name + shortDescription snippet (first 80 chars). Commercial panel same pattern with getServicesByCategory('commercial') linking to /services/commercial/${slug}. Locations panel shows 3-column grid of all 12 municipalities from getAllMunicipalities() linking to /service-areas/${slug}.

Interaction: Hover opens with 200ms ease-out. 100ms delay before close on mouse-leave (setTimeout + useRef timer). Enter/Space opens panel. Tab cycles panel links. Escape closes and returns focus to trigger. Click toggles. Close on mouse leave (100ms delay), Escape, click outside (document click listener in useEffect), route change.

**FILE 3: src/components/layout/mobile-nav.tsx**

Props: interface MobileNavProps { isOpen: boolean; onClose: () => void; }

Imports: motion/react (motion, AnimatePresence), react (useState, useEffect, useRef), next/link (Link), lucide-react (X, ChevronDown, Phone), @/components/ui/button (Button), @/lib/constants (PHONE_NUMBER, PHONE_HREF), @/data/services (getServicesByCategory), @/data/municipalities (getAllMunicipalities).

AnimatePresence wraps the overlay. When isOpen: motion.div with initial={{ x: "100%" }}, animate={{ x: 0 }}, exit={{ x: "100%" }}, transition={{ duration: 0.3, ease: "easeOut" }}. Classes: fixed inset-0 z-[60] bg-dominant overflow-y-auto. role="dialog" aria-modal="true" aria-label="Navigation menu".

Layout: Top bar with "Menu" label + close button (aria-label="Close navigation menu", min-h-[44px] min-w-[44px], X icon). Phone + CTA section below with a href={PHONE_HREF} ("Call Now: {PHONE_NUMBER}") and full-width Button variant="primary" linking to /contact ("Get Free Quote"). Accordion nav sections for "Residential Services" (getServicesByCategory('residential'), links to /services/residential/${slug}), "Commercial Services" (getServicesByCategory('commercial'), links to /services/commercial/${slug}), "Service Areas" (getAllMunicipalities(), links to /service-areas/${slug}). Company links (About Us /about, Contact /contact) at bottom.

Accordion: Internal AccordionSection component (not exported). Tap title expands children (200ms motion animation). Only one section open at a time via useState. ChevronDown rotates 180deg when expanded. All link clicks call onClose.

Body scroll lock: useEffect adds overflow: hidden to document.body when isOpen, removes on close/unmount. Focus trap: focus close button on open, trap Tab/Shift+Tab within overlay, Escape calls onClose.
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/JerseyCityQualityRoofing && pnpm type-check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layout/header.tsx contains "use client"
    - src/components/layout/header.tsx contains useScroll from motion/react
    - src/components/layout/header.tsx contains aria-label="Open navigation menu"
    - src/components/layout/header.tsx contains skip-to-content
    - src/components/layout/header.tsx contains PHONE_HREF or PHONE_NUMBER import
    - src/components/layout/header.tsx contains sticky class
    - src/components/layout/header.tsx contains z-50
    - src/components/layout/header.tsx exports Header
    - src/components/layout/mega-menu.tsx contains getServicesByCategory import
    - src/components/layout/mega-menu.tsx contains getAllMunicipalities import
    - src/components/layout/mega-menu.tsx contains aria-expanded
    - src/components/layout/mega-menu.tsx contains aria-haspopup
    - src/components/layout/mega-menu.tsx contains AnimatePresence
    - src/components/layout/mega-menu.tsx contains Escape handling
    - src/components/layout/mega-menu.tsx exports MegaMenu
    - src/components/layout/mobile-nav.tsx contains aria-label="Close navigation menu"
    - src/components/layout/mobile-nav.tsx contains aria-modal="true"
    - src/components/layout/mobile-nav.tsx contains overflow body scroll lock logic
    - src/components/layout/mobile-nav.tsx contains PHONE_NUMBER or PHONE_HREF
    - src/components/layout/mobile-nav.tsx exports MobileNav
    - pnpm type-check passes with zero errors
  </acceptance_criteria>
  <done>Header renders with shrink-on-scroll, phone link, and CTA button always visible. MegaMenu shows residential/commercial/locations with keyboard support. MobileNav provides full-screen overlay with accordion sections, phone, and CTA. All use semantic HTML and meet WCAG touch target requirements.</done>
</task>

<task type="auto">
  <name>Task 2: Build Footer with CTA banner and Breadcrumbs with JSON-LD</name>
  <files>src/components/layout/footer.tsx, src/components/layout/breadcrumbs.tsx</files>
  <read_first>
    - src/components/ui/button.tsx (Button component for CTA banner)
    - src/components/ui/badge.tsx (Badge component for certification badges)
    - src/styles/globals.css (color tokens)
    - src/data/business-info.ts (full BUSINESS_INFO for NAP, certifications)
    - src/data/services.ts (getServicesByCategory for footer links)
    - src/data/municipalities.ts (getAllMunicipalities for footer links)
    - src/lib/constants.ts (PHONE_NUMBER, PHONE_HREF, SITE_NAME, BASE_URL)
    - src/lib/seo/json-ld.tsx (buildBreadcrumbJsonLd, JsonLd component)
    - src/data/types.ts (Service, Municipality, Certification interfaces)
    - .planning/phases/02-design-system-layout-shell/02-UI-SPEC.md (footer, breadcrumb, copywriting contract)
  </read_first>
  <action>
**FILE 1: src/components/layout/footer.tsx**

Server Component (no "use client" -- static content with links, no interactivity).

Imports: next/link (Link), lucide-react (Phone, Mail, MapPin), @/components/ui/button (Button), @/components/ui/badge (Badge), @/data/business-info (BUSINESS_INFO), @/data/services (getServicesByCategory), @/data/municipalities (getAllMunicipalities), @/lib/constants (PHONE_NUMBER, PHONE_HREF, SITE_NAME).

Structure: footer element with bg-dominant border-t border-secondary/30.

Section 1 - CTA Banner Strip (D-13): bg-secondary py-8 lg:py-10 container. Flex row (col on mobile, row on sm+). Left side: h2 "Ready to Protect Your Roof?" in font-heading font-bold text-[1.75rem] text-text-primary + p "Call now for a free inspection or request your no-obligation quote." in text-text-secondary text-lg. Right side: phone link (a href={PHONE_HREF} with Phone icon + PHONE_NUMBER) + Button variant="primary" linking to /contact "Get Free Quote". Min height 96px via padding.

Section 2 - Multi-column sitemap (D-12): Container with py-12 lg:py-16. Grid: 1 col mobile, 2 cols sm, 3 cols md, 4 cols lg with gap-8 lg:gap-12. Column 1: "Residential Services" h3 + ul of getServicesByCategory('residential') as Link href=/services/residential/${slug}. Column 2: "Commercial Services" h3 + ul of getServicesByCategory('commercial') as Link href=/services/commercial/${slug}. Column 3: "Service Areas" h3 + ul of getAllMunicipalities() as Link href=/service-areas/${slug}. Column 4: "Contact Us" h3 + address (not-italic) with MapPin + full address, Phone + PHONE_HREF link, Mail + email link, then company links (About Us /about, Contact /contact, All Service Areas /service-areas).

All link text: text-text-secondary hover:text-accent text-lg transition-colors duration-150. All links use next/link. Phone uses a href={PHONE_HREF}. Heading labels use font-heading font-bold text-lg text-text-primary mb-4.

Section 3 - Certification badges: flex flex-wrap gap-2 justify-center. Map BUSINESS_INFO.certifications with Badge variant="certification". Add Badge variant="status" for "Licensed: {licenseNumber}" and "Insured".

Section 4 - Copyright bar: border-t border-secondary/30 py-6. Text: "{foundedYear}--{new Date().getFullYear()} {SITE_NAME}. All rights reserved." in text-text-secondary text-lg text-center.

Key rules: CTA heading uses h2 (not h1 -- layout never renders h1 per SEO-11). All text 18px minimum. NAP from single BUSINESS_INFO (SEO-14 prep). All links use next/link (UX-08). Phone uses tel: (UX-08).

**FILE 2: src/components/layout/breadcrumbs.tsx**

"use client" component (needs usePathname from next/navigation).

Imports: next/navigation (usePathname), next/link (Link), lucide-react (ChevronRight), @/lib/seo/json-ld (buildBreadcrumbJsonLd, JsonLd), @/lib/constants (BASE_URL).

SEGMENT_LABELS constant (Record<string, string>): "services" -> "Services", "residential" -> "Residential Services", "commercial" -> "Commercial Services", "service-areas" -> "Service Areas", "about" -> "About Us", "contact" -> "Contact", "blog" -> "Blog".

Logic: (1) Get pathname from usePathname(). (2) If pathname === "/" return null (no breadcrumbs on homepage). (3) Split pathname into segments. (4) For each segment, look up SEGMENT_LABELS; if not found, title-case from slug ("roof-repair" -> "Roof Repair", "jersey-city" -> "Jersey City"). (5) Build items array: [{ name: "Home", url: BASE_URL }, ...segments with cumulative URLs]. (6) Last item is current page (span with aria-current="page"), ancestors are Link.

Structure: nav aria-label="Breadcrumb" with max-w-[1280px] mx-auto px-4 sm:px-6 lg:px-8 py-3. JsonLd component at top injecting buildBreadcrumbJsonLd(items) cast as Record<string, unknown>. ol with flex items-center gap-1 text-text-secondary text-lg font-body font-medium. Each li has ChevronRight separator (aria-hidden="true", text-text-secondary/50, size 16, strokeWidth 1.5) except first. Ancestor items are Link with hover:text-accent transition-colors duration-150. Current page is span with aria-current="page" text-text-primary.
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/JerseyCityQualityRoofing && pnpm type-check 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layout/footer.tsx does NOT contain "use client"
    - src/components/layout/footer.tsx contains "Ready to Protect Your Roof?"
    - src/components/layout/footer.tsx contains BUSINESS_INFO import
    - src/components/layout/footer.tsx contains getServicesByCategory import
    - src/components/layout/footer.tsx contains getAllMunicipalities import
    - src/components/layout/footer.tsx contains PHONE_HREF or tel:
    - src/components/layout/footer.tsx contains footer element
    - src/components/layout/footer.tsx contains foundedYear
    - src/components/layout/footer.tsx contains certification badge usage
    - src/components/layout/footer.tsx exports Footer
    - src/components/layout/breadcrumbs.tsx contains "use client"
    - src/components/layout/breadcrumbs.tsx contains usePathname import
    - src/components/layout/breadcrumbs.tsx contains buildBreadcrumbJsonLd import
    - src/components/layout/breadcrumbs.tsx contains JsonLd usage
    - src/components/layout/breadcrumbs.tsx contains aria-label="Breadcrumb"
    - src/components/layout/breadcrumbs.tsx contains aria-current="page"
    - src/components/layout/breadcrumbs.tsx contains ChevronRight
    - src/components/layout/breadcrumbs.tsx exports Breadcrumbs
    - pnpm type-check passes with zero errors
  </acceptance_criteria>
  <done>Footer renders CTA banner strip with "Ready to Protect Your Roof?" heading, phone link, quote button, then 4-column sitemap with all service links, location links, NAP data, and certification badges. Breadcrumbs auto-generate from route pathname, render BreadcrumbList JSON-LD, and skip rendering on homepage.</done>
</task>

</tasks>

<verification>
- pnpm type-check passes (TypeScript compiles all 5 layout components)
- pnpm lint passes (no ESLint errors)
- Header uses motion/react for scroll-based shrink
- MegaMenu populates from data registries (8 services, 12 municipalities)
- MobileNav locks body scroll and traps focus
- Footer sources NAP from BUSINESS_INFO (single source of truth)
- Breadcrumbs inject BreadcrumbList JSON-LD
- All interactive elements have aria attributes and keyboard handlers
- All text 18px minimum
- All links use next/link, phone links use tel:
</verification>

<success_criteria>
1. Header sticks to top with phone and CTA visible at all scroll positions
2. Header shrinks from 80px to 56px on scroll with Motion animation
3. MegaMenu shows 3 panels (Residential 4, Commercial 4, Locations 12) with keyboard support
4. MobileNav opens full-screen overlay with accordion sections and phone/CTA at top
5. Footer displays CTA banner, 4-column sitemap, certification badges, and copyright
6. Breadcrumbs generate from route path and emit BreadcrumbList JSON-LD
7. TypeScript compiles with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-design-system-layout-shell/02-02-SUMMARY.md`
</output>
