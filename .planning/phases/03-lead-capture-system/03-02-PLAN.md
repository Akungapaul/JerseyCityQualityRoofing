---
phase: 03-lead-capture-system
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/forms/form-input.tsx
  - src/components/forms/form-select.tsx
  - src/components/forms/form-textarea.tsx
  - src/components/forms/submit-button.tsx
  - src/components/forms/turnstile-widget.tsx
  - src/components/forms/compact-quote-form.tsx
  - src/components/forms/quote-form.tsx
  - src/app/actions/submit-quote.ts
  - src/components/layout/header.tsx
autonomous: true
requirements:
  - CRO-01
  - CRO-10
  - CRO-11

must_haves:
  truths:
    - "Compact form (3 fields: name, phone, service type) validates on blur and submits via Server Action"
    - "Full form (6 fields: name, phone, email, service type, address, message) validates on blur and submits via Server Action"
    - "Both forms show inline success message replacing the form after successful submission"
    - "Both forms show inline error message above the form with phone fallback on failure, preserving form data"
    - "Cloudflare Turnstile invisible widget obtains token before submission and resets after each attempt"
    - "Honeypot hidden field is invisible to users and screen readers, rejects bots server-side"
    - "Server Action validates with Zod, checks honeypot, verifies Turnstile token, then sends email"
    - "Header CTA smooth-scrolls to compact form section or navigates to /contact if no form on page"
    - "Service type dropdown auto-selects based on defaultServiceType prop when provided"
  artifacts:
    - path: "src/components/forms/form-input.tsx"
      provides: "Reusable text input with label, error, cva variants"
      exports: ["FormInput"]
    - path: "src/components/forms/form-select.tsx"
      provides: "Reusable select dropdown with label, error, cva variants"
      exports: ["FormSelect"]
    - path: "src/components/forms/form-textarea.tsx"
      provides: "Reusable textarea with label, error, cva variants"
      exports: ["FormTextarea"]
    - path: "src/components/forms/submit-button.tsx"
      provides: "Button with loading spinner and disabled state"
      exports: ["SubmitButton"]
    - path: "src/components/forms/turnstile-widget.tsx"
      provides: "Cloudflare Turnstile invisible captcha React wrapper"
      exports: ["TurnstileWidget"]
    - path: "src/components/forms/compact-quote-form.tsx"
      provides: "3-field inline quote form with horizontal desktop layout"
      exports: ["CompactQuoteForm"]
    - path: "src/components/forms/quote-form.tsx"
      provides: "6-field stacked quote form with 2-column desktop layout"
      exports: ["QuoteForm"]
    - path: "src/app/actions/submit-quote.ts"
      provides: "Server Action: validate, honeypot check, Turnstile verify, send email"
      exports: ["submitQuote"]
    - path: "src/components/layout/header.tsx"
      provides: "Updated header with scroll-to-form CTA behavior"
      contains: "quote-form"
  key_links:
    - from: "src/components/forms/compact-quote-form.tsx"
      to: "src/app/actions/submit-quote.ts"
      via: "import submitQuote, call from handleSubmit onSubmit"
      pattern: "submitQuote"
    - from: "src/components/forms/quote-form.tsx"
      to: "src/app/actions/submit-quote.ts"
      via: "import submitQuote, call from handleSubmit onSubmit"
      pattern: "submitQuote"
    - from: "src/app/actions/submit-quote.ts"
      to: "src/lib/turnstile.ts"
      via: "import verifyTurnstileToken"
      pattern: "verifyTurnstileToken"
    - from: "src/app/actions/submit-quote.ts"
      to: "src/lib/email.ts"
      via: "import sendLeadNotification"
      pattern: "sendLeadNotification"
    - from: "src/app/actions/submit-quote.ts"
      to: "src/lib/schemas/quote-form.ts"
      via: "import schemas for server-side re-validation"
      pattern: "compactQuoteSchema|fullQuoteSchema"
    - from: "src/components/layout/header.tsx"
      to: "#quote-form"
      via: "scrollIntoView on click, fallback to /contact navigation"
      pattern: "quote-form"
---

<objective>
Build all form UI components, the Server Action, Turnstile widget, and wire the header CTA to scroll to the form section.

Purpose: This completes the lead capture system. After this plan, both the compact and full quote forms are functional, validated, spam-protected, and connected to email delivery. The forms are reusable components ready for embedding on any page in future phases.

Output: 7 form components (FormInput, FormSelect, FormTextarea, SubmitButton, TurnstileWidget, CompactQuoteForm, QuoteForm), 1 Server Action (submitQuote), 1 updated header with scroll-to-form behavior.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-lead-capture-system/03-CONTEXT.md
@.planning/phases/03-lead-capture-system/03-RESEARCH.md
@.planning/phases/03-lead-capture-system/03-UI-SPEC.md
@.planning/phases/03-lead-capture-system/03-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan implements against -->

From src/lib/schemas/quote-form.ts (created in Plan 01):
```typescript
export const compactQuoteSchema: z.ZodObject<{
  name: z.ZodString;
  phone: z.ZodPipeline<...>; // string -> transform -> refine -> 10 digits
  serviceType: z.ZodString;
  honeypot: z.ZodOptional<z.ZodString>;
  turnstileToken: z.ZodString;
}>;
export const fullQuoteSchema: z.ZodObject<{
  // compactQuoteSchema fields plus:
  email: z.ZodOptional<z.ZodString> | z.ZodLiteral<"">;
  address: z.ZodOptional<z.ZodString>;
  message: z.ZodOptional<z.ZodString>;
}>;
export type CompactQuoteFormData = z.infer<typeof compactQuoteSchema>;
export type FullQuoteFormData = z.infer<typeof fullQuoteSchema>;
```

From src/types/form.ts (created in Plan 01):
```typescript
export interface ActionResult {
  success: boolean;
  error?: string;
}
export interface LeadNotificationPayload {
  name: string;
  phone: string;
  serviceType: string;
  email?: string;
  address?: string;
  message?: string;
  sourceUrl: string;
  pageTitle: string;
  timestamp: string;
  formVariant: "compact" | "full";
}
```

From src/lib/turnstile.ts (created in Plan 01):
```typescript
export async function verifyTurnstileToken(token: string): Promise<boolean>;
```

From src/lib/email.ts (created in Plan 01):
```typescript
export async function sendLeadNotification(payload: LeadNotificationPayload): Promise<{ success: boolean; error?: string }>;
```

From src/components/ui/button-variants.ts (Phase 2):
```typescript
export const buttonVariants = cva([...], {
  variants: {
    variant: { primary: [...], secondary: [...], phone: [...] },
    size: { default: "px-6 py-3 text-lg", compact: "px-4 py-2 text-lg" },
  },
  defaultVariants: { variant: "primary", size: "default" },
});
export type ButtonVariantProps = VariantProps<typeof buttonVariants>;
```

From src/components/sections/section-wrapper.tsx (Phase 2):
```typescript
export function SectionWrapper({ children, tone, id, className }: {
  children: React.ReactNode;
  tone?: "dominant" | "secondary";
  id?: string;
  className?: string;
});
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string;
```

From src/lib/constants.ts:
```typescript
export const PHONE_NUMBER = "(201) 555-0123";
export const PHONE_HREF = "tel:+12015550123";
```

From src/components/layout/header.tsx (will be modified):
```typescript
// Current: <Link href="/contact" className={cn(buttonVariants({...}))}>
// Lines 77-84 contain the CTA button to modify
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build form primitives (FormInput, FormSelect, FormTextarea, SubmitButton) and TurnstileWidget</name>
  <files>src/components/forms/form-input.tsx, src/components/forms/form-select.tsx, src/components/forms/form-textarea.tsx, src/components/forms/submit-button.tsx, src/components/forms/turnstile-widget.tsx</files>
  <read_first>
    - src/components/ui/button-variants.ts (cva pattern to replicate for form inputs)
    - src/components/ui/button.tsx (component structure pattern with forwardRef)
    - src/lib/utils.ts (cn() import path)
    - src/styles/globals.css (color tokens, animation tokens, font families)
    - .planning/phases/03-lead-capture-system/03-UI-SPEC.md (Form Input Visual Contract section, Honeypot Field section, Turnstile Widget section, Interaction States section for SubmitButton)
    - .planning/phases/03-lead-capture-system/03-RESEARCH.md (Pattern 4: Turnstile Custom Hook, anti-patterns)
    - .planning/phases/03-lead-capture-system/03-CONTEXT.md (D-18, D-20, D-21)
  </read_first>
  <action>
Delete the `.gitkeep` file in `src/components/forms/` before creating components.

**File 1: `src/components/forms/form-input.tsx`** -- `"use client"` component

Create a cva-based text input following the Phase 2 cva+cn() pattern. Use `forwardRef` so React Hook Form `register()` can attach refs.

**cva variants definition** (named `inputVariants`):
- Base classes: `"w-full rounded-md bg-dominant border font-body text-lg text-text-primary placeholder:text-text-secondary/60 transition-colors duration-[--duration-fast] outline-none disabled:opacity-50 disabled:cursor-not-allowed"`
- Variant `state`:
  - `default`: `"border-secondary/50 hover:border-accent/50 focus:border-accent focus:ring-2 focus:ring-accent focus:ring-offset-1 focus:ring-offset-dominant"`
  - `error`: `"border-destructive focus:border-destructive focus:ring-2 focus:ring-destructive focus:ring-offset-1 focus:ring-offset-dominant"`
- Variant `inputSize`:
  - `default`: `"px-4 py-3"` (48px height target)
  - `compact`: `"px-3 py-2"`
- defaultVariants: `{ state: "default", inputSize: "default" }`

**Component props:** Extend `InputHTMLAttributes<HTMLInputElement>` plus: `label: string`, `error?: string`, `required?: boolean`, `inputSize?: "default" | "compact"`. Use `forwardRef`.

**Render structure:**
1. `<div className="flex flex-col gap-1">` wrapper
2. `<label htmlFor={id} className="font-body text-lg font-medium text-text-primary">` with the label text. If `required`, append `<span aria-hidden="true" className="text-accent ml-1">*</span>`.
3. `<input>` with inputVariants applied, `id` and `aria-describedby={error ? errorId : undefined}`, `aria-invalid={!!error}`.
4. If error: `<AnimatePresence>` wrapping `<motion.p>` with `id={errorId}`, `role="alert"`, `className="text-sm text-destructive font-body"`. Animation: initial `{ opacity: 0, y: -4 }`, animate `{ opacity: 1, y: 0 }`, exit `{ opacity: 0 }`, transition `{ duration: 0.2 }`.

Generate unique `id` via `useId()` hook if no `id` prop provided.

**File 2: `src/components/forms/form-select.tsx`** -- `"use client"` component

Same cva pattern as FormInput but for `<select>`. Add `appearance-none` to base classes. Add `pr-10` for custom dropdown arrow space. Include a Lucide `ChevronDown` icon (16px, `text-text-secondary`) absolutely positioned right inside the wrapper.

Props extend `SelectHTMLAttributes<HTMLSelectElement>` plus: `label: string`, `error?: string`, `required?: boolean`, `options: Array<{ value: string; label: string }>`, `placeholder?: string`, `inputSize?: "default" | "compact"`. Use `forwardRef`.

Render the placeholder option as `<option value="" disabled>{placeholder}</option>`. Map `options` to `<option value={opt.value}>{opt.label}</option>`.

**File 3: `src/components/forms/form-textarea.tsx`** -- `"use client"` component

Same cva pattern as FormInput but for `<textarea>`. Add `min-h-[120px] resize-y` to base classes. Props extend `TextareaHTMLAttributes<HTMLTextAreaElement>` plus: `label: string`, `error?: string`, `required?: boolean`. Use `forwardRef`.

**File 4: `src/components/forms/submit-button.tsx`** -- `"use client"` component

Wraps the existing `buttonVariants` from `@/components/ui/button-variants`. Props: `isSubmitting: boolean`, `children: React.ReactNode`, `className?: string`, `variant?: ButtonVariantProps["variant"]`, `size?: ButtonVariantProps["size"]`.

When `isSubmitting`:
- Replace children with: Lucide `Loader2` icon (20px) with `className="animate-spin"` + "Submitting..." text
- Set `disabled` attribute
- Add `opacity-80` to className

When not submitting: render children normally.

Use `<button type="submit">` with `buttonVariants({ variant, size })` applied via cn().

**File 5: `src/components/forms/turnstile-widget.tsx`** -- `"use client"` component

Custom Turnstile widget per RESEARCH Pattern 4.

Declare global `Window.turnstile` interface with methods: `render`, `reset`, `remove`, `getResponse`, `isExpired`.

Props: `onVerify: (token: string) => void`, `onExpire?: () => void`, `onError?: () => void`.

Component:
1. `containerRef = useRef<HTMLDivElement>(null)`
2. `widgetIdRef = useRef<string | null>(null)`
3. `useEffect` that calls `window.turnstile.render(containerRef.current, { sitekey: process.env.NEXT_PUBLIC_TURNSTILE_SITE_KEY, appearance: "interaction-only", callback: onVerify, "expired-callback": onExpire, "error-callback": onError })` when Turnstile script is loaded. Store returned widgetId. Cleanup: `window.turnstile.remove(widgetId)`.
4. Export a `reset()` method via `useImperativeHandle` + `forwardRef` that calls `window.turnstile?.reset(widgetIdRef.current)`. This allows parent forms to reset after each submission.
5. Handle script not yet loaded: poll for `window.turnstile` with a small interval (100ms, max 50 attempts = 5 seconds), then render once available.
6. Render: `<div ref={containerRef} />` -- zero visual footprint per UI-SPEC.

Also add a `<Script>` import from `next/script` to load the Turnstile API. The script URL: `https://challenges.cloudflare.com/turnstile/v0/api.js?render=explicit`. Strategy: `afterInteractive`. Place the Script tag in the component so it loads when any form mounts.
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/JerseyCityQualityRoofing && pnpm exec tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/components/forms/form-input.tsx contains '"use client"'
    - src/components/forms/form-input.tsx contains "forwardRef"
    - src/components/forms/form-input.tsx contains "inputVariants" (cva definition)
    - src/components/forms/form-input.tsx contains 'role="alert"' (error announcement)
    - src/components/forms/form-input.tsx contains "aria-invalid" (error state)
    - src/components/forms/form-input.tsx contains "aria-describedby" (error association)
    - src/components/forms/form-input.tsx contains "AnimatePresence" (error animation)
    - src/components/forms/form-input.tsx contains "aria-hidden" on the required asterisk
    - src/components/forms/form-select.tsx contains '"use client"'
    - src/components/forms/form-select.tsx contains "forwardRef"
    - src/components/forms/form-select.tsx contains "ChevronDown" (custom arrow icon)
    - src/components/forms/form-select.tsx contains "appearance-none"
    - src/components/forms/form-textarea.tsx contains '"use client"'
    - src/components/forms/form-textarea.tsx contains "forwardRef"
    - src/components/forms/form-textarea.tsx contains "min-h-[120px]"
    - src/components/forms/form-textarea.tsx contains "resize-y"
    - src/components/forms/submit-button.tsx contains "Loader2" (spinning icon)
    - src/components/forms/submit-button.tsx contains "isSubmitting"
    - src/components/forms/submit-button.tsx contains "Submitting..."
    - src/components/forms/submit-button.tsx contains "buttonVariants"
    - src/components/forms/turnstile-widget.tsx contains '"use client"'
    - src/components/forms/turnstile-widget.tsx contains "interaction-only" (invisible mode)
    - src/components/forms/turnstile-widget.tsx contains "NEXT_PUBLIC_TURNSTILE_SITE_KEY"
    - src/components/forms/turnstile-widget.tsx contains "useImperativeHandle" (reset method)
    - src/components/forms/turnstile-widget.tsx contains "challenges.cloudflare.com/turnstile" (script URL)
    - src/components/forms/.gitkeep does NOT exist (deleted)
    - `pnpm exec tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Five form primitive components created: FormInput (text input with label, error, cva variants, forwardRef, aria attributes, AnimatePresence error animation), FormSelect (select dropdown with custom ChevronDown arrow, placeholder option, forwardRef), FormTextarea (textarea with 120px min height, vertical resize, forwardRef), SubmitButton (loading spinner with Loader2, disabled state, buttonVariants), TurnstileWidget (invisible Turnstile with explicit rendering, onVerify/onExpire callbacks, reset via imperative handle, next/script loader). All use the established cva+cn() pattern. TypeScript compiles cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Build CompactQuoteForm, QuoteForm, Server Action, and wire header CTA scroll</name>
  <files>src/components/forms/compact-quote-form.tsx, src/components/forms/quote-form.tsx, src/app/actions/submit-quote.ts, src/components/layout/header.tsx</files>
  <read_first>
    - src/components/forms/form-input.tsx (just created -- verify exports and props interface)
    - src/components/forms/form-select.tsx (just created -- verify exports and props interface)
    - src/components/forms/form-textarea.tsx (just created -- verify exports and props interface)
    - src/components/forms/submit-button.tsx (just created -- verify exports and props interface)
    - src/components/forms/turnstile-widget.tsx (just created -- verify exports, ref type for reset)
    - src/lib/schemas/quote-form.ts (schemas from Plan 01)
    - src/types/form.ts (ActionResult, LeadNotificationPayload from Plan 01)
    - src/lib/turnstile.ts (verifyTurnstileToken from Plan 01)
    - src/lib/email.ts (sendLeadNotification from Plan 01)
    - src/components/sections/section-wrapper.tsx (SectionWrapper for form section wrapping)
    - src/components/layout/header.tsx (current header -- lines 77-84 have the CTA Link to modify)
    - src/data/services.ts (lines 1-10 for SERVICES object keys to populate dropdown options)
    - src/lib/constants.ts (PHONE_NUMBER, PHONE_HREF for error message fallback)
    - .planning/phases/03-lead-capture-system/03-UI-SPEC.md (Form Layout Contract, Interaction States Contract, Scroll-to-Form Behavior, Copywriting Contract, Accessibility Contract, Animation Contract sections)
    - .planning/phases/03-lead-capture-system/03-CONTEXT.md (D-01 through D-22)
    - .planning/phases/03-lead-capture-system/03-RESEARCH.md (Pattern 2: RHF+Zod v4 no generic, Pattern 3: Server Action, anti-patterns)
  </read_first>
  <action>
**File 1: `src/app/actions/submit-quote.ts`** -- Server Action per D-22, RESEARCH Pattern 3

Create the `src/app/actions/` directory first.

```typescript
"use server";
```

Import `compactQuoteSchema`, `fullQuoteSchema` from `@/lib/schemas/quote-form`. Import `verifyTurnstileToken` from `@/lib/turnstile`. Import `sendLeadNotification` from `@/lib/email`. Import `ActionResult`, `LeadNotificationPayload` from `@/types/form`.

Export function signature:
```typescript
export async function submitQuote(
  formVariant: "compact" | "full",
  data: Record<string, unknown>,
  metadata: { sourceUrl: string; pageTitle: string },
): Promise<ActionResult>
```

Implementation flow:
1. **Honeypot check** (D-21): If `data.honeypot` is truthy (non-empty string), return `{ success: true }` (silent success to not reveal detection).
2. **Server-side Zod validation**: Select schema based on `formVariant`. Call `schema.safeParse(data)`. If `!result.success`, return `{ success: false, error: "Please check your form and try again." }`.
3. **Turnstile verification** (D-22): Call `verifyTurnstileToken(result.data.turnstileToken)`. If `!turnstileValid`, return `{ success: false, error: "Security verification failed. Please try again." }`.
4. **Build payload**: Construct `LeadNotificationPayload` from `result.data` plus `metadata.sourceUrl`, `metadata.pageTitle`, `new Date().toISOString()` for timestamp, and `formVariant`.
5. **Send email** (D-11): Call `sendLeadNotification(payload)`. If `!emailResult.success`, return `{ success: false, error: "Something went wrong. Please try again or call us." }`.
6. **Return success**: `{ success: true }`.

**File 2: `src/components/forms/compact-quote-form.tsx`** -- `"use client"` per D-01, D-02, D-04

Props: `defaultServiceType?: string`.

React Hook Form setup -- CRITICAL: Do NOT pass generic to useForm (Zod v4 type fix):
```typescript
const { register, handleSubmit, formState: { errors, isSubmitting }, reset } = useForm({
  resolver: zodResolver(compactQuoteSchema),
  mode: "onBlur",
  defaultValues: { serviceType: defaultServiceType ?? "", name: "", phone: "", honeypot: "" },
});
```

State: `formStatus: "idle" | "success" | "error"` via useState. `turnstileToken: string` via useState. `turnstileRef` via useRef for reset.

Service options: Import `SERVICES` from `@/data/services` and map Object.values to `{ value: service.name, label: service.name }` for FormSelect options.

**Layout per UI-SPEC Compact Form Layout Contract:**
- Wrapped in `<SectionWrapper tone="secondary" id="quote-form">` (this is the scroll target for header CTA per D-05)
- Heading: `<h2 className="font-heading text-[1.75rem] font-bold text-text-primary">Get Your Free Quote</h2>`
- Subtext: `<p className="text-lg text-text-secondary mt-2">Quick response -- we'll call you back within 24 hours.</p>`
- Form grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-[1fr_1fr_1fr_auto] gap-3` with submit button in the last column
- Mobile: single column, submit button full-width
- Container: `bg-secondary rounded-lg p-4 sm:p-6`

**Form fields:** 3 fields using FormInput (name), FormInput (phone), FormSelect (serviceType) with `inputSize="compact"`. Register each with `register("fieldName")`. Pass `error={errors.fieldName?.message}`.

**Honeypot field** (D-21): Hidden input per UI-SPEC Honeypot section:
```tsx
<input
  type="text"
  {...register("honeypot")}
  aria-hidden="true"
  tabIndex={-1}
  autoComplete="off"
  className="absolute -left-[9999px] -top-[9999px] h-0 w-0 overflow-hidden"
  name="website"
/>
```

**TurnstileWidget:** Place inside form, pass `onVerify={(token) => setValue("turnstileToken", token)}`. Store ref for reset.

**SubmitButton:** Text "Get Free Quote", variant="primary", size="compact".

**onSubmit handler:**
1. Call `submitQuote("compact", data, { sourceUrl: window.location.href, pageTitle: document.title })`.
2. On success: set `formStatus` to "success".
3. On error: set `formStatus` to "error", reset Turnstile widget via `turnstileRef.current?.reset()`.
4. On success OR error: Turnstile reset (tokens are single-use per Pitfall 5).

**Success state** (D-08, D-09): Wrap form and success message in `<AnimatePresence mode="wait">`. When `formStatus === "success"`, render success container replacing form:
- Container: `bg-secondary rounded-lg p-6 border-l-4 border-success`
- Icon: Lucide `CheckCircle` (32px, `text-success`) centered
- Heading: "Thank You!" in 28px Cormorant bold
- Body: "We've received your request and will contact you within 24 hours."
- Phone fallback: "Need immediate help? Call us at " + `<a href={PHONE_HREF} className="text-accent hover:underline">{PHONE_NUMBER}</a>`
- Animation: fade-in + scale 0.95->1.0, duration 300ms
- Focus management: container has `tabIndex={-1}` and `ref` that calls `.focus()` on mount

**Error state** (D-10): When `formStatus === "error"`, render error banner ABOVE the form (form remains visible, data preserved):
- Container: `bg-destructive/10 rounded-md p-4 border-l-4 border-destructive`
- Icon: Lucide `AlertCircle` (20px, `text-destructive`) inline
- Copy: "Something went wrong. Please try again or call us at " + phone link
- Animation: slide down from y:-16px + fade-in, duration 300ms

**File 3: `src/components/forms/quote-form.tsx`** -- `"use client"` per D-01, D-03, D-06

Props: `defaultServiceType?: string`.

Same React Hook Form + Zod pattern as CompactQuoteForm but uses `fullQuoteSchema`.

Default values: `{ serviceType: defaultServiceType ?? "", name: "", phone: "", email: "", address: "", message: "", honeypot: "" }`.

**Layout per UI-SPEC Full Form Layout Contract:**
- Wrapped in `<SectionWrapper tone="secondary" id="full-quote-form">`
- Heading: `<h2>Request a Detailed Quote</h2>` (same heading style as compact)
- Subtext: "Tell us about your project and we'll prepare a comprehensive estimate."
- Form grid: `grid grid-cols-1 sm:grid-cols-2 gap-4`
  - Row 1: name (col 1) + phone (col 2)
  - Row 2: email (col 1) + service type (col 2)
  - Row 3: address (full width via `sm:col-span-2`)
  - Row 4: message textarea (full width via `sm:col-span-2`)
  - Row 5: submit button (left-aligned, NOT full width on desktop)
- Container: `bg-secondary rounded-lg p-4 sm:p-6 lg:p-8`

**Fields:** 6 fields: FormInput (name, required), FormInput (phone, required), FormInput (email, type="email"), FormSelect (serviceType, required), FormInput (address), FormTextarea (message with label "Tell Us About Your Project").

**SubmitButton:** Text "Request Free Quote", variant="primary", size="default" (NOT compact).

Same honeypot, Turnstile, success/error state, and submission flow as CompactQuoteForm. Same success and error UI (reuse the same patterns -- consider extracting a FormSuccessMessage and FormErrorBanner component or just duplicate the JSX since there are only 2 forms).

**File 4: `src/components/layout/header.tsx`** -- Modify existing header per D-05

The current header CTA (lines 77-84) is a `<Link href="/contact">`. Change it to a `<button>` that:

1. On click: check if `document.getElementById("quote-form")` exists.
2. If exists: call `element.scrollIntoView({ behavior: "smooth", block: "start" })`. Account for sticky header height by using `window.scrollTo({ top: element.offsetTop - headerHeight, behavior: "smooth" })` where headerHeight is `isScrolled ? 56 : 80` (matching the header animate values).
3. If not exists: call `router.push("/contact")` using `useRouter` from `next/navigation`.

Change the element from `<Link>` to `<button>` and apply the same `buttonVariants({ variant: "primary", size: "compact" })` className. The visual appearance stays identical.

Add `import { useRouter } from "next/navigation"` at top and `const router = useRouter()` in the component.

The handler function:
```typescript
const handleQuoteCTA = () => {
  const formEl = document.getElementById("quote-form");
  if (formEl) {
    const headerHeight = isScrolled ? 56 : 80;
    const top = formEl.getBoundingClientRect().top + window.scrollY - headerHeight;
    window.scrollTo({ top, behavior: "smooth" });
  } else {
    router.push("/contact");
  }
};
```
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/JerseyCityQualityRoofing && pnpm exec tsc --noEmit 2>&1 | head -30 && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/actions/submit-quote.ts contains '"use server"' as first line
    - src/app/actions/submit-quote.ts contains "export async function submitQuote"
    - src/app/actions/submit-quote.ts contains "compactQuoteSchema" and "fullQuoteSchema" (server re-validation)
    - src/app/actions/submit-quote.ts contains "verifyTurnstileToken" (Turnstile check)
    - src/app/actions/submit-quote.ts contains "sendLeadNotification" (email send)
    - src/app/actions/submit-quote.ts contains "data.honeypot" or "honeypot" check (bot detection)
    - src/components/forms/compact-quote-form.tsx contains '"use client"'
    - src/components/forms/compact-quote-form.tsx contains "zodResolver(compactQuoteSchema)" (Zod v4 resolver)
    - src/components/forms/compact-quote-form.tsx does NOT contain "useForm<" (no generic -- Zod v4 type fix)
    - src/components/forms/compact-quote-form.tsx contains 'mode: "onBlur"' (D-18 validation timing)
    - src/components/forms/compact-quote-form.tsx contains 'id="quote-form"' (scroll target)
    - src/components/forms/compact-quote-form.tsx contains "Get Your Free Quote" (heading per UI-SPEC)
    - src/components/forms/compact-quote-form.tsx contains "submitQuote" (Server Action call)
    - src/components/forms/compact-quote-form.tsx contains 'aria-hidden="true"' (honeypot hiding)
    - src/components/forms/compact-quote-form.tsx contains "tabIndex={-1}" (honeypot tab removal)
    - src/components/forms/compact-quote-form.tsx contains "Thank You!" (success message per D-08)
    - src/components/forms/compact-quote-form.tsx contains "AnimatePresence" (success/error animations)
    - src/components/forms/compact-quote-form.tsx contains "CheckCircle" (success icon)
    - src/components/forms/compact-quote-form.tsx contains "AlertCircle" (error icon)
    - src/components/forms/compact-quote-form.tsx contains "PHONE_NUMBER" or "(201) 555-0123" (error fallback per D-10)
    - src/components/forms/quote-form.tsx contains '"use client"'
    - src/components/forms/quote-form.tsx contains "zodResolver(fullQuoteSchema)"
    - src/components/forms/quote-form.tsx does NOT contain "useForm<" (no generic -- Zod v4 type fix)
    - src/components/forms/quote-form.tsx contains 'mode: "onBlur"'
    - src/components/forms/quote-form.tsx contains 'id="full-quote-form"' (scroll target)
    - src/components/forms/quote-form.tsx contains "Request a Detailed Quote" (heading per UI-SPEC)
    - src/components/forms/quote-form.tsx contains "defaultServiceType" (prop for auto-selection per D-15)
    - src/components/forms/quote-form.tsx contains "submitQuote"
    - src/components/forms/quote-form.tsx contains "Thank You!" (success message)
    - src/components/forms/quote-form.tsx contains 'role="alert"' somewhere (error announcement)
    - src/components/layout/header.tsx contains "quote-form" (scroll target reference)
    - src/components/layout/header.tsx contains "scrollTo" or "scrollIntoView" (smooth scroll)
    - src/components/layout/header.tsx contains "useRouter" (fallback navigation)
    - src/components/layout/header.tsx contains "handleQuoteCTA" or similar handler function name
    - src/components/layout/header.tsx contains "<button" for CTA (changed from Link)
    - `pnpm exec tsc --noEmit` exits 0
    - `pnpm build` succeeds (no build errors)
  </acceptance_criteria>
  <done>Complete lead capture system functional. CompactQuoteForm (3-field, horizontal desktop layout, id="quote-form") and QuoteForm (6-field, 2-column desktop layout, id="full-quote-form") both validate via React Hook Form with Zod v4 on blur, submit via submitQuote Server Action, show inline success message replacing form on success, show error banner with phone fallback preserving form data on failure. Server Action validates server-side with Zod, checks honeypot, verifies Turnstile token, sends styled HTML email to business. Turnstile invisible widget obtains and refreshes tokens. Header CTA smooth-scrolls to #quote-form or navigates to /contact. Service type dropdown accepts defaultServiceType prop for page-context auto-selection. All animations use Motion with AnimatePresence. All form elements meet accessibility contract (labels, aria-invalid, aria-describedby, role="alert", focus management). Build succeeds.</done>
</task>

</tasks>

<verification>
- `pnpm exec tsc --noEmit` exits 0 -- all form components, Server Action, and updated header type-check
- `pnpm build` succeeds -- Server Action compiles, client components bundle correctly, no server/client boundary violations
- `grep -r "useForm<" src/components/forms/` returns NO matches (Zod v4 type fix enforced)
- `grep -r '"use server"' src/app/actions/submit-quote.ts` confirms Server Action directive
- `grep -r '"use client"' src/components/forms/` confirms all form components are client components
- `grep -r 'id="quote-form"' src/components/forms/compact-quote-form.tsx` confirms scroll target
- `grep -r 'id="full-quote-form"' src/components/forms/quote-form.tsx` confirms full form scroll target
- `grep -r 'aria-invalid' src/components/forms/form-input.tsx` confirms accessibility
- `grep -r 'role="alert"' src/components/forms/form-input.tsx` confirms error announcement
</verification>

<success_criteria>
1. CompactQuoteForm renders 3 fields (name, phone, service type) in horizontal desktop layout with id="quote-form"
2. QuoteForm renders 6 fields in 2-column desktop layout with id="full-quote-form"
3. Both forms validate on blur via React Hook Form + Zod v4 (no generic on useForm)
4. Both forms submit via submitQuote Server Action which validates, checks honeypot, verifies Turnstile, sends email
5. Success state: form replaced by inline "Thank You!" message with phone fallback (D-08, D-09)
6. Error state: banner above form with "try again or call us" message, form data preserved (D-10)
7. Turnstile invisible widget loads, obtains token, resets after each submission
8. Honeypot field hidden from users and screen readers, checked server-side
9. Header CTA smooth-scrolls to #quote-form or navigates to /contact
10. Service type dropdown pre-selects from defaultServiceType prop (D-15)
11. All form elements accessible: labels, aria-invalid, aria-describedby, role="alert", focus management
12. `pnpm build` succeeds with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-lead-capture-system/03-02-SUMMARY.md`
</output>
