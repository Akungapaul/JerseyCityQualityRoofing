---
phase: 15-navigation-seo-entity-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/mega-menu.tsx
  - src/components/layout/mobile-nav.tsx
  - src/components/layout/footer.tsx
  - src/components/layout/__tests__/mega-menu.test.tsx
  - src/components/layout/__tests__/mobile-nav.test.tsx
  - src/components/layout/__tests__/footer.test.tsx
autonomous: true
requirements: [UX-08, SEO-05]
must_haves:
  truths:
    - "Header MegaMenu includes a Resources dropdown panel with links to /blog, /guides, /problems"
    - "MobileNav includes a Resources accordion section with the same 3 links"
    - "Footer includes a Resources column with links to /blog, /guides, /problems, and /gallery"
    - "All navigation links use next/link (not raw <a> tags)"
    - "Content silos (/blog, /guides, /problems) are reachable from persistent navigation on every page"
  artifacts:
    - path: "src/components/layout/mega-menu.tsx"
      provides: "Resources dropdown panel in MegaMenu"
      contains: "resources"
    - path: "src/components/layout/mobile-nav.tsx"
      provides: "Resources accordion section in MobileNav"
      contains: "Resources"
    - path: "src/components/layout/footer.tsx"
      provides: "Resources column in Footer"
      contains: "grid-cols-5"
    - path: "src/components/layout/__tests__/mega-menu.test.tsx"
      provides: "Test for MegaMenu Resources panel links"
      contains: "/blog"
    - path: "src/components/layout/__tests__/mobile-nav.test.tsx"
      provides: "Test for MobileNav Resources accordion links"
      contains: "/blog"
    - path: "src/components/layout/__tests__/footer.test.tsx"
      provides: "Test for Footer Resources column links"
      contains: "/blog"
  key_links:
    - from: "src/components/layout/mega-menu.tsx"
      to: "/blog, /guides, /problems"
      via: "next/link href attributes in Resources panel"
      pattern: 'href="/blog"'
    - from: "src/components/layout/mobile-nav.tsx"
      to: "/blog, /guides, /problems"
      via: "next/link href attributes in Resources accordion"
      pattern: 'href="/blog"'
    - from: "src/components/layout/footer.tsx"
      to: "/blog, /guides, /problems, /gallery"
      via: "next/link href attributes in Resources column"
      pattern: 'href="/blog"'
---

<objective>
Add content navigation entry points to the site header (MegaMenu + MobileNav) and footer so that /blog, /guides, and /problems are discoverable from every page.

Purpose: 27 content pages across 3 silos are currently unreachable from persistent navigation. Adding these links makes them discoverable and strengthens the internal link graph (UX-08, SEO-05).

Output: Updated MegaMenu with Resources panel, MobileNav with Resources accordion, Footer with Resources column, plus test coverage for all three.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-navigation-seo-entity-polish/15-RESEARCH.md
@.planning/phases/15-navigation-seo-entity-polish/15-UI-SPEC.md
@.planning/phases/15-navigation-seo-entity-polish/15-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/layout/mega-menu.tsx:
```typescript
type PanelId = "residential" | "commercial" | "locations" | null;
// CHANGE TO: type PanelId = "residential" | "commercial" | "locations" | "resources" | null;

const navItems: Array<{ id: PanelId; label: string }> = [
  { id: "residential", label: "Residential Services" },
  { id: "commercial", label: "Commercial Services" },
  { id: "locations", label: "Service Areas" },
  // ADD: { id: "resources", label: "Resources" },
];
```

From src/components/layout/mobile-nav.tsx:
```typescript
// AccordionSection component (lines 24-63) -- reuse for Resources
interface AccordionSectionProps {
  title: string;
  isExpanded: boolean;
  onToggle: () => void;
  children: React.ReactNode;
}
// Existing sections: "residential", "commercial", "locations"
// ADD: "resources" section after "locations", before company links
```

From src/components/layout/footer.tsx:
```typescript
// Current grid: grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4
// CHANGE TO: lg:grid-cols-5
// Columns: Residential Services | Commercial Services | Service Areas | Resources (NEW) | Contact Us
```

From 15-UI-SPEC.md -- Copywriting contract:
```
MegaMenu panel heading: "Roofing Resources"
Blog link description: "Roofing tips, guides, and industry insights"
Guides link description: "Cost guides and material deep-dives"
Problems link description: "Solutions for ice dams, leaks, and storm damage"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Resources panel to MegaMenu and Resources accordion to MobileNav with test scaffolds</name>
  <files>src/components/layout/mega-menu.tsx, src/components/layout/mobile-nav.tsx, src/components/layout/__tests__/mega-menu.test.tsx, src/components/layout/__tests__/mobile-nav.test.tsx</files>
  <read_first>
    - src/components/layout/mega-menu.tsx (full file -- understand PanelId type at line 10, navItems array at lines 104-108, panel rendering blocks at lines 150-223, existing class patterns)
    - src/components/layout/mobile-nav.tsx (full file -- understand AccordionSection component at lines 24-63, accordion sections at lines 183-238, company links placement at lines 241-256)
    - .planning/phases/15-navigation-seo-entity-polish/15-UI-SPEC.md (Component Inventory sections 1 and 2, Copywriting Contract, Interaction Contracts, Accessibility Contract)
  </read_first>
  <action>
    **Part A: Modify MegaMenu (src/components/layout/mega-menu.tsx)**

    1. Update the PanelId type at line 10:
       ```typescript
       type PanelId = "residential" | "commercial" | "locations" | "resources" | null;
       ```

    2. Add "resources" to the navItems array at line 108 (after "locations"):
       ```typescript
       { id: "resources", label: "Resources" },
       ```

    3. Add a new panel rendering block after the `{item.id === "locations" && (...)}` block (after line 221, before the closing `</motion.div>`). Use the exact same class patterns as the residential panel:
       ```tsx
       {item.id === "resources" && (
         <div className="grid gap-4">
           <p className="font-heading font-bold text-lg text-accent mb-1">
             Roofing Resources
           </p>
           <Link
             href="/blog"
             className="group block rounded-md p-2 -mx-2 transition-colors duration-[--duration-fast] hover:bg-dominant-lighter"
             onClick={closePanel}
             role="menuitem"
           >
             <span className="block font-body font-bold text-lg text-text-primary group-hover:text-accent transition-colors duration-[--duration-fast]">
               Blog
             </span>
             <span className="block text-text-secondary text-lg leading-snug mt-0.5">
               Roofing tips, guides, and industry insights
             </span>
           </Link>
           <Link
             href="/guides"
             className="group block rounded-md p-2 -mx-2 transition-colors duration-[--duration-fast] hover:bg-dominant-lighter"
             onClick={closePanel}
             role="menuitem"
           >
             <span className="block font-body font-bold text-lg text-text-primary group-hover:text-accent transition-colors duration-[--duration-fast]">
               Roofing Guides
             </span>
             <span className="block text-text-secondary text-lg leading-snug mt-0.5">
               Cost guides and material deep-dives
             </span>
           </Link>
           <Link
             href="/problems"
             className="group block rounded-md p-2 -mx-2 transition-colors duration-[--duration-fast] hover:bg-dominant-lighter"
             onClick={closePanel}
             role="menuitem"
           >
             <span className="block font-body font-bold text-lg text-text-primary group-hover:text-accent transition-colors duration-[--duration-fast]">
               Common Problems
             </span>
             <span className="block text-text-secondary text-lg leading-snug mt-0.5">
               Solutions for ice dams, leaks, and storm damage
             </span>
           </Link>
         </div>
       )}
       ```

    **Part B: Modify MobileNav (src/components/layout/mobile-nav.tsx)**

    Insert a new AccordionSection after the "Service Areas" section (after line 238, before the company links `<div>` at line 241). Use the exact same link class pattern as the other accordion sections:

    ```tsx
    <AccordionSection
      title="Resources"
      isExpanded={expandedSection === "resources"}
      onToggle={() => toggleSection("resources")}
    >
      <div className="space-y-1">
        <Link
          href="/blog"
          className="block rounded-md px-3 py-2 font-body text-lg text-text-secondary hover:text-accent hover:bg-secondary transition-colors duration-[--duration-fast] min-h-[44px] flex items-center"
          onClick={onClose}
        >
          Blog
        </Link>
        <Link
          href="/guides"
          className="block rounded-md px-3 py-2 font-body text-lg text-text-secondary hover:text-accent hover:bg-secondary transition-colors duration-[--duration-fast] min-h-[44px] flex items-center"
          onClick={onClose}
        >
          Roofing Guides
        </Link>
        <Link
          href="/problems"
          className="block rounded-md px-3 py-2 font-body text-lg text-text-secondary hover:text-accent hover:bg-secondary transition-colors duration-[--duration-fast] min-h-[44px] flex items-center"
          onClick={onClose}
        >
          Common Problems
        </Link>
      </div>
    </AccordionSection>
    ```

    **Part C: Create test file for MegaMenu**

    Create directory `src/components/layout/__tests__/` if it does not exist. Create `src/components/layout/__tests__/mega-menu.test.tsx`:

    ```typescript
    import { describe, it, expect, vi } from 'vitest';
    import { createElement } from 'react';
    import { renderToStaticMarkup } from 'react-dom/server';

    // Mock motion/react to avoid client-side animation issues in test
    vi.mock('motion/react', () => ({
      motion: {
        div: ({ children, ...props }: Record<string, unknown>) =>
          createElement('div', props, children as React.ReactNode),
      },
      AnimatePresence: ({ children }: { children: React.ReactNode }) =>
        createElement('div', null, children),
    }));

    // Mock lucide-react
    vi.mock('lucide-react', () => ({
      ChevronDown: () => createElement('svg', { 'data-testid': 'chevron-down' }),
    }));

    import { MegaMenu } from '../mega-menu';

    describe('MegaMenu', () => {
      it('renders without crashing', () => {
        const html = renderToStaticMarkup(createElement(MegaMenu));
        expect(html).toBeDefined();
      });

      it('contains Resources trigger button', () => {
        const html = renderToStaticMarkup(createElement(MegaMenu));
        expect(html).toContain('Resources');
      });

      it('includes 4 navigation trigger buttons', () => {
        const html = renderToStaticMarkup(createElement(MegaMenu));
        expect(html).toContain('Residential Services');
        expect(html).toContain('Commercial Services');
        expect(html).toContain('Service Areas');
        expect(html).toContain('Resources');
      });
    });
    ```

    NOTE: Since MegaMenu is a "use client" component with useState, the renderToStaticMarkup approach may produce initial state (all panels closed). The test verifies trigger buttons render. Panel content is hidden until interaction (useState), so we only verify triggers are present.

    **Part D: Create test file for MobileNav**

    Create `src/components/layout/__tests__/mobile-nav.test.tsx`:

    ```typescript
    import { describe, it, expect, vi } from 'vitest';
    import { createElement } from 'react';
    import { renderToStaticMarkup } from 'react-dom/server';

    // Mock motion/react
    vi.mock('motion/react', () => ({
      motion: {
        div: ({ children, ...props }: Record<string, unknown>) =>
          createElement('div', props, children as React.ReactNode),
      },
      AnimatePresence: ({ children }: { children: React.ReactNode }) =>
        createElement('div', null, children),
    }));

    // Mock lucide-react
    vi.mock('lucide-react', () => ({
      ChevronDown: () => createElement('svg', { 'data-testid': 'chevron-down' }),
      X: () => createElement('svg', { 'data-testid': 'x-icon' }),
      Phone: () => createElement('svg', { 'data-testid': 'phone-icon' }),
    }));

    // Mock Button component
    vi.mock('@/components/ui/button', () => ({
      Button: ({ children, ...props }: Record<string, unknown>) =>
        createElement('button', props, children as React.ReactNode),
    }));

    import { MobileNav } from '../mobile-nav';

    describe('MobileNav', () => {
      const defaultProps = { isOpen: true, onClose: vi.fn() };

      it('renders when isOpen is true', () => {
        const html = renderToStaticMarkup(createElement(MobileNav, defaultProps));
        expect(html).toBeDefined();
        expect(html.length).toBeGreaterThan(0);
      });

      it('contains Resources accordion section', () => {
        const html = renderToStaticMarkup(createElement(MobileNav, defaultProps));
        expect(html).toContain('Resources');
      });

      it('contains all 4 accordion section titles', () => {
        const html = renderToStaticMarkup(createElement(MobileNav, defaultProps));
        expect(html).toContain('Residential Services');
        expect(html).toContain('Commercial Services');
        expect(html).toContain('Service Areas');
        expect(html).toContain('Resources');
      });
    });
    ```
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/JerseyCityQualityRoofing && npx vitest --run src/components/layout/__tests__/</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layout/mega-menu.tsx contains `type PanelId = "residential" | "commercial" | "locations" | "resources" | null`
    - src/components/layout/mega-menu.tsx contains `{ id: "resources", label: "Resources" }`
    - src/components/layout/mega-menu.tsx contains `href="/blog"` within the resources panel block
    - src/components/layout/mega-menu.tsx contains `href="/guides"` within the resources panel block
    - src/components/layout/mega-menu.tsx contains `href="/problems"` within the resources panel block
    - src/components/layout/mega-menu.tsx contains `Roofing Resources` as panel heading text
    - src/components/layout/mega-menu.tsx contains `Roofing tips, guides, and industry insights`
    - src/components/layout/mega-menu.tsx contains `Cost guides and material deep-dives`
    - src/components/layout/mega-menu.tsx contains `Solutions for ice dams, leaks, and storm damage`
    - src/components/layout/mobile-nav.tsx contains `title="Resources"` in an AccordionSection
    - src/components/layout/mobile-nav.tsx contains `toggleSection("resources")`
    - src/components/layout/mobile-nav.tsx contains `href="/blog"` within the Resources accordion
    - src/components/layout/mobile-nav.tsx contains `href="/guides"` within the Resources accordion
    - src/components/layout/mobile-nav.tsx contains `href="/problems"` within the Resources accordion
    - src/components/layout/__tests__/mega-menu.test.tsx exists and contains `describe('MegaMenu'`
    - src/components/layout/__tests__/mobile-nav.test.tsx exists and contains `describe('MobileNav'`
    - `npx vitest --run src/components/layout/__tests__/` exits 0
  </acceptance_criteria>
  <done>MegaMenu has a 4th dropdown panel "Resources" with links to /blog, /guides, /problems using the exact same class patterns and interaction model as existing panels. MobileNav has a 4th accordion section "Resources" with the same 3 links. Tests verify both components render the Resources section.</done>
</task>

<task type="auto">
  <name>Task 2: Add Resources column to Footer with test scaffold</name>
  <files>src/components/layout/footer.tsx, src/components/layout/__tests__/footer.test.tsx</files>
  <read_first>
    - src/components/layout/footer.tsx (full file -- understand current 4-column grid at line 55, column patterns at lines 56-191, grid classes at line 55)
    - .planning/phases/15-navigation-seo-entity-polish/15-UI-SPEC.md (Component Inventory section 3 for Footer, Responsive Breakpoints section)
  </read_first>
  <action>
    **Part A: Modify Footer (src/components/layout/footer.tsx)**

    1. Change the grid class at line 55 from `lg:grid-cols-4` to `lg:grid-cols-5`:
       ```tsx
       <div className="grid grid-cols-1 gap-8 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-5 lg:gap-12">
       ```

    2. Insert a new column between the "Service Areas" column (ending at line 111) and the "Contact Us" column (starting at line 113). Add the Resources column:
       ```tsx
       {/* Column 4: Resources */}
       <div>
         <h3 className="font-heading font-bold text-lg text-text-primary mb-4">
           Resources
         </h3>
         <ul className="space-y-2">
           <li>
             <Link
               href="/blog"
               className="text-text-secondary hover:text-accent text-lg transition-colors duration-[--duration-fast]"
             >
               Blog
             </Link>
           </li>
           <li>
             <Link
               href="/guides"
               className="text-text-secondary hover:text-accent text-lg transition-colors duration-[--duration-fast]"
             >
               Roofing Guides
             </Link>
           </li>
           <li>
             <Link
               href="/problems"
               className="text-text-secondary hover:text-accent text-lg transition-colors duration-[--duration-fast]"
             >
               Common Problems
             </Link>
           </li>
           <li>
             <Link
               href="/gallery"
               className="text-text-secondary hover:text-accent text-lg transition-colors duration-[--duration-fast]"
             >
               Project Gallery
             </Link>
           </li>
         </ul>
       </div>
       ```

    3. Update the comment on the Contact Us column from `{/* Column 4: Contact Us */}` to `{/* Column 5: Contact Us */}`.

    **Part B: Create Footer test file**

    Create `src/components/layout/__tests__/footer.test.tsx`. Footer is a Server Component (no "use client"), so renderToStaticMarkup works directly:

    ```typescript
    import { describe, it, expect } from 'vitest';
    import { createElement } from 'react';
    import { renderToStaticMarkup } from 'react-dom/server';
    import { Footer } from '../footer';

    describe('Footer', () => {
      it('renders without crashing', () => {
        const html = renderToStaticMarkup(createElement(Footer));
        expect(html).toBeDefined();
        expect(html.length).toBeGreaterThan(0);
      });

      it('contains Resources column heading', () => {
        const html = renderToStaticMarkup(createElement(Footer));
        expect(html).toContain('Resources');
      });

      it('contains link to /blog', () => {
        const html = renderToStaticMarkup(createElement(Footer));
        expect(html).toContain('href="/blog"');
      });

      it('contains link to /guides', () => {
        const html = renderToStaticMarkup(createElement(Footer));
        expect(html).toContain('href="/guides"');
      });

      it('contains link to /problems', () => {
        const html = renderToStaticMarkup(createElement(Footer));
        expect(html).toContain('href="/problems"');
      });

      it('contains link to /gallery', () => {
        const html = renderToStaticMarkup(createElement(Footer));
        expect(html).toContain('href="/gallery"');
      });

      it('uses 5-column grid layout on large screens', () => {
        const html = renderToStaticMarkup(createElement(Footer));
        expect(html).toContain('lg:grid-cols-5');
      });

      it('contains all 5 column headings', () => {
        const html = renderToStaticMarkup(createElement(Footer));
        expect(html).toContain('Residential Services');
        expect(html).toContain('Commercial Services');
        expect(html).toContain('Service Areas');
        expect(html).toContain('Resources');
        expect(html).toContain('Contact Us');
      });
    });
    ```
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/JerseyCityQualityRoofing && npx vitest --run src/components/layout/__tests__/footer.test.tsx && npx vitest --run</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layout/footer.tsx contains `lg:grid-cols-5` (changed from lg:grid-cols-4)
    - src/components/layout/footer.tsx contains the heading text `Resources` in an h3 element
    - src/components/layout/footer.tsx contains `href="/blog"` in the Resources column
    - src/components/layout/footer.tsx contains `href="/guides"` in the Resources column
    - src/components/layout/footer.tsx contains `href="/problems"` in the Resources column
    - src/components/layout/footer.tsx contains `href="/gallery"` in the Resources column
    - src/components/layout/footer.tsx contains `{/* Column 5: Contact Us */}`
    - src/components/layout/__tests__/footer.test.tsx exists and contains `describe('Footer'`
    - `npx vitest --run src/components/layout/__tests__/footer.test.tsx` exits 0
    - `npx vitest --run` exits 0 (full suite green)
  </acceptance_criteria>
  <done>Footer has a 5th "Resources" column with links to /blog, /guides, /problems, /gallery using the exact same link class pattern as other footer columns. Grid layout updated from 4 to 5 columns on large screens. Tests verify all resource links are present. Full test suite passes.</done>
</task>

</tasks>

<verification>
1. `npx vitest --run src/components/layout/__tests__/` -- all layout component tests pass
2. `npx vitest --run` -- full test suite green (629+ tests)
3. Verify grep: `grep -c 'href="/blog"' src/components/layout/mega-menu.tsx` returns 1
4. Verify grep: `grep -c 'href="/blog"' src/components/layout/mobile-nav.tsx` returns 1
5. Verify grep: `grep -c 'href="/blog"' src/components/layout/footer.tsx` returns 1
6. Verify grep: `grep 'grid-cols-5' src/components/layout/footer.tsx` shows the updated grid class
7. `pnpm build` succeeds without errors
</verification>

<success_criteria>
- MegaMenu has 4th dropdown panel "Resources" with 3 links (/blog, /guides, /problems) per UX-08
- MobileNav has 4th accordion section "Resources" with same 3 links per UX-08
- Footer has 5th column "Resources" with 4 links (/blog, /guides, /problems, /gallery) per SEO-05
- All links use next/link (not raw <a> tags) per UX-08
- 27 content pages reachable from persistent navigation on every page
- All tests pass including new layout component tests
</success_criteria>

<output>
After completion, create `.planning/phases/15-navigation-seo-entity-polish/15-02-SUMMARY.md`
</output>
