---
phase: 05-residential-service-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/service-hero.tsx
  - src/components/sections/process-timeline.tsx
  - src/components/sections/material-cards.tsx
  - src/components/sections/cost-factors-section.tsx
  - src/components/sections/warning-signs-section.tsx
  - src/components/sections/service-content-section.tsx
autonomous: true
requirements:
  - CONT-07

must_haves:
  truths:
    - "ServiceHero renders headline, subtitle, dual CTA, and inline CompactQuoteForm in 60/40 desktop split"
    - "ProcessTimeline renders vertical numbered steps with connector lines, duration badges, and accent markers"
    - "MaterialCards renders 2-col desktop grid with expandable pros/cons using Motion animation"
    - "CostFactorsSection renders divided list with impact badges (low/moderate/high)"
    - "WarningSignsSection renders 2-col grid of icon + title + description cards"
    - "ServiceContentSection renders long-form prose with proper heading hierarchy"
  artifacts:
    - path: "src/components/sections/service-hero.tsx"
      provides: "Standard service page hero with headline, subtitle, dual CTA, compact form"
      contains: "export function ServiceHero"
    - path: "src/components/sections/process-timeline.tsx"
      provides: "Vertical timeline with numbered steps, connector lines, duration badges"
      contains: "export function ProcessTimeline"
    - path: "src/components/sections/material-cards.tsx"
      provides: "2-col grid of material comparison cards with expandable pros/cons"
      contains: "export function MaterialCards"
    - path: "src/components/sections/cost-factors-section.tsx"
      provides: "Cost factor list with impact badges"
      contains: "export function CostFactorsSection"
    - path: "src/components/sections/warning-signs-section.tsx"
      provides: "Warning signs grid with Lucide icons"
      contains: "export function WarningSignsSection"
    - path: "src/components/sections/service-content-section.tsx"
      provides: "Long-form prose renderer for service intros"
      contains: "export function ServiceContentSection"
  key_links:
    - from: "src/components/sections/service-hero.tsx"
      to: "src/components/forms/compact-quote-form.tsx"
      via: "import { CompactQuoteForm }"
      pattern: "import.*CompactQuoteForm"
    - from: "src/components/sections/material-cards.tsx"
      to: "src/data/types.ts"
      via: "import type { Material }"
      pattern: "import.*Material.*from.*types"
    - from: "src/components/sections/process-timeline.tsx"
      to: "src/data/types.ts"
      via: "import type { ProcessStep }"
      pattern: "import.*ProcessStep.*from.*types"
---

<objective>
Build 6 standard-template section components that render structured service data and content for the 3 non-emergency service pages (Roof Repair, Replacement, Inspection).

Purpose: These components render the core section flow per D-02: ServiceHero, ProcessTimeline, MaterialCards, CostFactorsSection, WarningSignsSection, and ServiceContentSection. They receive typed props from data registries and content files -- no inline content.

Output: 6 new component files in src/components/sections/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-residential-service-pages/05-CONTEXT.md
@.planning/phases/05-residential-service-pages/05-UI-SPEC.md

@src/data/types.ts
@src/lib/constants.ts
@src/components/sections/section-wrapper.tsx
@src/components/sections/scroll-reveal.tsx
@src/components/sections/faq-accordion.tsx
@src/components/sections/hero-section.tsx
@src/components/forms/compact-quote-form.tsx
@src/styles/globals.css

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/data/types.ts:
```typescript
export interface ProcessStep {
  step: number;
  title: string;
  description: string;
  duration: string;
}

export interface Material {
  name: string;
  description: string;
  lifespan: string;
  priceRange: string;
  pros: string[];
  cons: string[];
}

export interface CostFactor {
  factor: string;
  description: string;
  impact: "low" | "moderate" | "high";
}

// New types from Plan 01 Task 1:
export interface WarningSign {
  icon: string;        // Lucide icon name
  title: string;
  description: string;
}
```

From src/lib/constants.ts:
```typescript
export const PHONE_NUMBER = "(201) 555-0123";
export const PHONE_HREF = "tel:+12015550123";
```

From src/components/forms/compact-quote-form.tsx:
```typescript
interface CompactQuoteFormProps {
  defaultServiceType?: string;
}
export function CompactQuoteForm({ defaultServiceType }: CompactQuoteFormProps);
```

From src/components/sections/section-wrapper.tsx:
```typescript
// SectionWrapper provides consistent padding and max-width container
// tone="dominant" = dark bg, tone="secondary" = slightly lighter bg
```

From src/styles/globals.css @theme:
```css
--color-dominant: #2a2e22;
--color-secondary: #3e4435;
--color-secondary-lighter: #4a5040;
--color-accent: #c89640;
--color-text-primary: #f0ede6;
--color-text-secondary: #b0ae9e;
--color-success: #34d399;
--color-destructive: #ef4444;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ServiceHero, ProcessTimeline, and ServiceContentSection components</name>
  <files>src/components/sections/service-hero.tsx, src/components/sections/process-timeline.tsx, src/components/sections/service-content-section.tsx</files>
  <read_first>
    - src/components/sections/hero-section.tsx (existing hero pattern -- ServiceHero is different layout but follow same padding/accessibility patterns)
    - src/components/forms/compact-quote-form.tsx (imported into ServiceHero, understand its props interface)
    - src/components/sections/section-wrapper.tsx (wrap each section component)
    - src/components/sections/scroll-reveal.tsx (wrap section content)
    - src/styles/globals.css (theme variables for colors and spacing)
    - src/lib/constants.ts (PHONE_NUMBER, PHONE_HREF constants)
    - .planning/phases/05-residential-service-pages/05-UI-SPEC.md (visual specs for ServiceHero, ProcessTimeline, ServiceContentSection)
  </read_first>
  <action>
**Component 1: src/components/sections/service-hero.tsx** (Server Component -- NO "use client")

Props interface:
```typescript
interface ServiceHeroProps {
  headline: string;
  subtitle: string;
  serviceName: string;
}
```

Layout per UI-SPEC ServiceHero spec:
- Full-width dominant background via SectionWrapper tone="dominant"
- Vertical padding: `py-16 sm:py-20 lg:py-24`
- Inner layout: `flex flex-col lg:flex-row gap-8 lg:gap-12`
- Left column (60%): `lg:w-3/5`
  - h1: `text-[2.5rem] lg:text-[3rem] font-heading font-bold text-text-primary leading-[1.1]` -- renders `headline` prop
  - p: `text-lg text-text-secondary mt-4 leading-relaxed` -- renders `subtitle` prop
  - CTA row: `flex flex-wrap items-center gap-4 mt-8`
    - "Get Your Free Quote" button: Link to `#quote-form` using anchor, styled with `bg-accent hover:bg-accent-hover text-dominant font-bold px-6 py-3 rounded-lg min-h-[44px] transition-colors duration-[150ms] focus-ring inline-flex items-center`
    - Phone link: `<a href={PHONE_HREF}>` with `text-accent hover:text-accent-hover flex items-center gap-2 text-lg font-bold min-h-[44px]` containing Phone icon (lucide-react, size=20) + `PHONE_NUMBER`
- Right column (40%): `lg:w-2/5`
  - `<CompactQuoteForm defaultServiceType={serviceName} />`

Import PHONE_NUMBER and PHONE_HREF from `@/lib/constants`. Import Phone icon from `lucide-react`. Import CompactQuoteForm from `@/components/forms/compact-quote-form`. Do NOT wrap in SectionWrapper -- the page component will handle section wrapping.

**Component 2: src/components/sections/process-timeline.tsx** (Server Component)

Props interface:
```typescript
interface ProcessTimelineProps {
  steps: readonly ProcessStep[];
  accentColor?: string; // CSS class for step markers, defaults to 'bg-accent'
}
```

Layout per UI-SPEC ProcessTimeline spec:
- Vertical layout with `<div>` for each step
- For each step, flex row: `flex gap-4`
  - Left column (48px wide): `w-12 shrink-0 flex flex-col items-center`
    - Circle marker: `w-8 h-8 rounded-full flex items-center justify-center text-sm font-bold text-dominant` with `accentColor ?? 'bg-accent'`
    - Connector line: `flex-1 w-0.5 bg-secondary-lighter mt-2` -- hidden on last step using conditional `{index < steps.length - 1 && <div className="flex-1 w-0.5 bg-secondary-lighter mt-2" />}`
  - Right column: `flex-1 pb-8` (no pb on last step)
    - Step title row: `flex flex-wrap items-center gap-3`
      - h3: `text-lg font-heading font-bold text-text-primary` -- step.title
      - Duration badge: `text-sm text-accent bg-secondary px-3 py-1 rounded-full` -- step.duration
    - Description: `text-lg text-text-secondary mt-2 leading-relaxed` -- step.description

Import ProcessStep type from `@/data/types`.

**Component 3: src/components/sections/service-content-section.tsx** (Server Component)

Props interface:
```typescript
interface ServiceContentSectionProps {
  heading: string;
  content: string;
  headingId?: string; // optional id for anchor links
}
```

Simple prose renderer:
- h2: `text-[1.75rem] font-heading font-bold text-text-primary` with optional `id={headingId}`
- Content rendered as paragraphs: Split `content` on double-newline (`\n\n`) and render each as `<p className="text-lg text-text-secondary leading-relaxed mt-4">`
- If content has no double-newlines, render as single paragraph

No SectionWrapper inside -- the page component handles section wrapping.
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/JerseyCityQualityRoofing && pnpm type-check 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/sections/service-hero.tsx contains `export function ServiceHero`
    - src/components/sections/service-hero.tsx contains `CompactQuoteForm`
    - src/components/sections/service-hero.tsx contains `PHONE_NUMBER`
    - src/components/sections/service-hero.tsx contains `PHONE_HREF`
    - src/components/sections/service-hero.tsx contains `h1` (exactly one h1)
    - src/components/sections/service-hero.tsx does NOT contain `"use client"`
    - src/components/sections/process-timeline.tsx contains `export function ProcessTimeline`
    - src/components/sections/process-timeline.tsx contains `ProcessStep`
    - src/components/sections/process-timeline.tsx contains `bg-accent`
    - src/components/sections/process-timeline.tsx contains `rounded-full`
    - src/components/sections/service-content-section.tsx contains `export function ServiceContentSection`
    - src/components/sections/service-content-section.tsx contains `<h2`
    - pnpm type-check exits 0
  </acceptance_criteria>
  <done>ServiceHero renders headline + subtitle + dual CTA + CompactQuoteForm in 60/40 layout. ProcessTimeline renders vertical numbered steps with connector lines and duration badges. ServiceContentSection renders prose paragraphs with h2 heading.</done>
</task>

<task type="auto">
  <name>Task 2: Build MaterialCards, CostFactorsSection, and WarningSignsSection components</name>
  <files>src/components/sections/material-cards.tsx, src/components/sections/cost-factors-section.tsx, src/components/sections/warning-signs-section.tsx</files>
  <read_first>
    - src/data/types.ts (Material, CostFactor, WarningSign interfaces)
    - src/components/sections/faq-accordion.tsx (reference for Motion expand/collapse animation pattern -- MaterialCards uses same pattern for pros/cons toggle)
    - src/styles/globals.css (theme variables)
    - .planning/phases/05-residential-service-pages/05-UI-SPEC.md (visual specs for MaterialCards, CostFactorsSection, WarningSignsSection)
  </read_first>
  <action>
**Component 1: src/components/sections/material-cards.tsx** ("use client" -- has interactive expand/collapse)

Props interface:
```typescript
interface MaterialCardsProps {
  materials: readonly Material[];
}
```

Layout per UI-SPEC MaterialCards spec:
- Grid: `grid grid-cols-1 md:grid-cols-2 gap-4`
- Each card: `bg-secondary rounded-lg p-6 border border-secondary-lighter`
  - h3: `text-lg font-heading font-bold text-text-primary` -- material.name
  - p: `text-lg text-text-secondary mt-2` -- material.description
  - Badge row: `flex gap-2 mt-4`
    - Lifespan badge: `bg-dominant text-accent text-sm px-3 py-1 rounded-full font-bold` -- material.lifespan
    - Price badge: `bg-dominant text-text-secondary text-sm px-3 py-1 rounded-full` -- material.priceRange
  - Expand/collapse toggle: `mt-4`
    - Button: `text-accent text-sm underline min-h-[44px] inline-flex items-center focus-ring` with text "Show Pros & Cons" / "Hide Pros & Cons"
    - Use `useState<Set<number>>` (set of expanded card indices) for INDEPENDENT multi-card expansion
    - Wrap expanded content in `AnimatePresence` + `motion.div` with `initial={{ height: 0, opacity: 0 }} animate={{ height: 'auto', opacity: 1 }} exit={{ height: 0, opacity: 0 }} transition={{ duration: 0.3, ease: 'easeOut' }}` and `overflow-hidden`
    - Pros list: `mt-2` with each item as `flex items-start gap-2 py-1` -- CheckCircle icon (lucide-react, size=16, `text-success shrink-0 mt-1`) + `text-lg text-text-secondary`
    - Cons list: `mt-2` with each item as `flex items-start gap-2 py-1` -- XCircle icon (lucide-react, size=16, `text-destructive shrink-0 mt-1`) + `text-lg text-text-secondary`

Import Material from `@/data/types`. Import `motion, AnimatePresence` from `motion/react`. Import `CheckCircle, XCircle` from `lucide-react`. Import `useState` from `react`.

**Component 2: src/components/sections/cost-factors-section.tsx** (Server Component)

Props interface:
```typescript
interface CostFactorsSectionProps {
  factors: readonly CostFactor[];
  serviceName: string;
}
```

Layout per UI-SPEC CostFactorsSection spec:
- h2: `text-[1.75rem] font-heading font-bold text-text-primary` -- text: `What Affects Your ${serviceName} Cost`
- List container: `divide-y divide-secondary-lighter mt-6`
- Each factor: `flex items-start justify-between gap-4 py-4`
  - Content area: `flex-1`
    - h3: `text-lg font-heading font-bold text-text-primary` -- factor.factor
    - p: `text-lg text-text-secondary mt-1` -- factor.description
  - Impact badge: `self-start ml-4 shrink-0 text-sm px-3 py-1 rounded-full uppercase`
    - low: `bg-dominant text-text-secondary border border-secondary-lighter`
    - moderate: `bg-dominant text-accent`
    - high: `bg-dominant text-accent font-bold`

Import CostFactor from `@/data/types`.

**Component 3: src/components/sections/warning-signs-section.tsx** (Server Component)

Props interface:
```typescript
interface WarningSectionProps {
  signs: readonly WarningSign[];
  serviceName: string;
  intro: string;
}
```

Layout per UI-SPEC WarningSignsSection spec:
- h2: `text-[1.75rem] font-heading font-bold text-text-primary` -- text: `Warning Signs You Need ${serviceName}`
- p: `text-lg text-text-secondary mt-2` -- renders `intro` prop
- Grid: `grid grid-cols-1 md:grid-cols-2 gap-4 mt-6`
- Each sign card: `bg-secondary rounded-lg p-6 flex gap-4`
  - Icon container: `shrink-0 w-10 h-10 bg-accent/10 rounded-full flex items-center justify-center`
    - Render Lucide icon dynamically: Use a mapping object from icon name strings to Lucide icon components. Map these icon names: `AlertTriangle`, `Droplets`, `Eye`, `Thermometer`, `Wind`, `CloudRain`, `Home`, `DollarSign`, `Sun`, `Snowflake`, `Shield`, `Clock`, `Search`, `Wrench`, `Zap`, `Camera`, `FileText`. Default to `AlertTriangle` for unrecognized names. Render: `<IconComponent size={20} className="text-accent" aria-hidden="true" />`
  - Content: `flex-1`
    - h3: `text-lg font-heading font-bold text-text-primary` -- sign.title
    - p: `text-lg text-text-secondary mt-1` -- sign.description

Import WarningSign from `@/data/types`. Import all needed Lucide icons.
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/JerseyCityQualityRoofing && pnpm type-check 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/sections/material-cards.tsx contains `"use client"`
    - src/components/sections/material-cards.tsx contains `export function MaterialCards`
    - src/components/sections/material-cards.tsx contains `AnimatePresence`
    - src/components/sections/material-cards.tsx contains `motion/react`
    - src/components/sections/material-cards.tsx contains `CheckCircle`
    - src/components/sections/material-cards.tsx contains `Show Pros & Cons`
    - src/components/sections/material-cards.tsx contains `Hide Pros & Cons`
    - src/components/sections/cost-factors-section.tsx contains `export function CostFactorsSection`
    - src/components/sections/cost-factors-section.tsx contains `What Affects Your`
    - src/components/sections/cost-factors-section.tsx contains impact badge classes for low, moderate, high
    - src/components/sections/warning-signs-section.tsx contains `export function WarningSignsSection`
    - src/components/sections/warning-signs-section.tsx contains `Warning Signs You Need`
    - src/components/sections/warning-signs-section.tsx contains `AlertTriangle`
    - pnpm type-check exits 0
  </acceptance_criteria>
  <done>MaterialCards renders 2-col grid with expandable pros/cons animation. CostFactorsSection renders divided list with impact badges. WarningSignsSection renders icon cards in 2-col grid. All components pass type-check.</done>
</task>

</tasks>

<verification>
Run after both tasks complete:
```bash
pnpm type-check && pnpm lint
```
No type errors, no lint errors. All 6 components export correctly typed functions.
</verification>

<success_criteria>
- 6 new section components in src/components/sections/
- ServiceHero: h1, dual CTA, CompactQuoteForm, 60/40 layout
- ProcessTimeline: numbered vertical steps, connector lines, duration badges
- MaterialCards: 2-col grid, expandable pros/cons with Motion animation
- CostFactorsSection: divided list, impact badges (low/moderate/high)
- WarningSignsSection: 2-col icon card grid
- ServiceContentSection: prose paragraph renderer with h2
- pnpm type-check passes, pnpm lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-residential-service-pages/05-02-SUMMARY.md`
</output>
