---
phase: 05-residential-service-pages
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/seo/json-ld.tsx
  - src/lib/__tests__/json-ld.test.ts
  - src/app/api/og/route.tsx
  - src/styles/globals.css
autonomous: true
requirements:
  - SEO-02

must_haves:
  truths:
    - "buildServicePageJsonLd returns valid Service schema with provider, areaServed, hasOfferCatalog"
    - "FAQPage schema works with combined FAQ arrays (base + extended)"
    - "OG image route generates PNG with service name and company branding"
    - "Emergency accent CSS variable declared in globals.css @theme block"
  artifacts:
    - path: "src/lib/seo/json-ld.tsx"
      provides: "buildServicePageJsonLd function added alongside existing builders"
      contains: "export function buildServicePageJsonLd"
    - path: "src/lib/__tests__/json-ld.test.ts"
      provides: "Tests for buildServicePageJsonLd"
      contains: "buildServicePageJsonLd"
    - path: "src/app/api/og/route.tsx"
      provides: "Dynamic OG image generation endpoint"
      contains: "export async function GET"
    - path: "src/styles/globals.css"
      provides: "Emergency accent CSS variable"
      contains: "--color-emergency-accent"
  key_links:
    - from: "src/lib/seo/json-ld.tsx"
      to: "src/data/business-info.ts"
      via: "import { BUSINESS_INFO }"
      pattern: "import.*BUSINESS_INFO.*from.*business-info"
    - from: "src/app/api/og/route.tsx"
      to: "src/data/services.ts"
      via: "import { getService }"
      pattern: "import.*getService.*from.*services"
---

<objective>
Add Service JSON-LD schema builder, extend JSON-LD tests, create dynamic OG image route, and add emergency accent CSS variable.

Purpose: SEO-02 requires Service schema + FAQ schema on all service pages. The OG image route (D-17) generates unique social cards per service. The emergency accent variable (D-14) enables Tailwind utility classes for emergency page components.

Output: 1 modified source file (json-ld.tsx), 1 modified test file, 1 new API route, 1 modified CSS file
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-residential-service-pages/05-CONTEXT.md
@.planning/phases/05-residential-service-pages/05-RESEARCH.md
@.planning/phases/05-residential-service-pages/05-UI-SPEC.md

@src/lib/seo/json-ld.tsx
@src/lib/__tests__/json-ld.test.ts
@src/data/services.ts
@src/data/business-info.ts
@src/data/types.ts
@src/lib/constants.ts
@src/styles/globals.css

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/seo/json-ld.tsx (existing exports):
```typescript
export function buildRoofingContractorJsonLd(): WithContext<RoofingContractor>;
export function buildBreadcrumbJsonLd(items: Array<{ name: string; url: string }>): WithContext<BreadcrumbList>;
export function buildAggregateRatingJsonLd(testimonials: readonly Testimonial[]): WithContext<RoofingContractor>;
export function buildFaqPageJsonLd(faqs: Array<{ question: string; answer: string }>): WithContext<FAQPage>;
export function buildContactPageJsonLd(): WithContext<RoofingContractor>;
export function JsonLd({ data }: { data: Record<string, unknown> }): JSX.Element;
```

From src/data/types.ts:
```typescript
export interface Service {
  name: string;
  slug: string;
  shortDescription: string;
  fullDescription: string;
  // ...
}
```

From src/data/business-info.ts:
```typescript
export const BUSINESS_INFO: BusinessInfo = {
  name: "Jersey City Quality Roofing",
  phone: "(201) 555-0123",
  address: { street: "123 Summit Avenue", city: "Jersey City", state: "NJ", zip: "07304" },
  serviceAreas: ["Jersey City", "Hoboken", "Bayonne", ...12 cities],
  // ...
};
```

From src/lib/constants.ts:
```typescript
export const BASE_URL = "https://www.jerseycityqualityroofing.com";
```

schema-dts types available:
```typescript
import type { Service, WithContext, OfferCatalog } from 'schema-dts';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add buildServicePageJsonLd to json-ld.tsx and extend tests</name>
  <files>src/lib/seo/json-ld.tsx, src/lib/__tests__/json-ld.test.ts</files>
  <read_first>
    - src/lib/seo/json-ld.tsx (existing builders -- append new function, follow same patterns and import style)
    - src/lib/__tests__/json-ld.test.ts (existing test structure -- add new describe block following same cast pattern)
    - src/data/business-info.ts (BUSINESS_INFO used in schema builder)
    - src/data/services.ts (Service interface fields, getService function)
    - src/lib/constants.ts (BASE_URL for canonical URL)
  </read_first>
  <action>
**1. Add buildServicePageJsonLd function to src/lib/seo/json-ld.tsx**

Add this import at the top alongside existing schema-dts imports:
```typescript
import type { RoofingContractor, WithContext, BreadcrumbList, FAQPage, Service as ServiceSchema } from 'schema-dts';
```

Note: Rename the schema-dts `Service` import to `ServiceSchema` to avoid conflict with the data `Service` type.

Add this import for the data Service type:
```typescript
import type { Service as ServiceData } from '@/data/types';
```

Add this function after `buildContactPageJsonLd`:

```typescript
export function buildServicePageJsonLd(
  service: ServiceData,
  canonicalUrl: string
): WithContext<ServiceSchema> {
  return {
    '@context': 'https://schema.org',
    '@type': 'Service',
    name: service.name,
    description: service.fullDescription,
    serviceType: service.name,
    url: canonicalUrl,
    provider: {
      '@type': 'RoofingContractor',
      name: BUSINESS_INFO.name,
      telephone: BUSINESS_INFO.phone,
      address: {
        '@type': 'PostalAddress',
        streetAddress: BUSINESS_INFO.address.street,
        addressLocality: BUSINESS_INFO.address.city,
        addressRegion: BUSINESS_INFO.address.state,
        postalCode: BUSINESS_INFO.address.zip,
        addressCountry: 'US',
      },
    },
    areaServed: BUSINESS_INFO.serviceAreas.map((area) => ({
      '@type': 'City' as const,
      name: area,
    })),
    hasOfferCatalog: {
      '@type': 'OfferCatalog',
      name: `${service.name} Services`,
      itemListElement: [{
        '@type': 'Offer',
        itemOffered: {
          '@type': 'Service',
          name: service.name,
          description: service.shortDescription,
        },
      }],
    },
  };
}
```

**2. Extend src/lib/__tests__/json-ld.test.ts**

Add `buildServicePageJsonLd` to the import statement at line 5:
```typescript
import {
  buildRoofingContractorJsonLd,
  buildBreadcrumbJsonLd,
  buildAggregateRatingJsonLd,
  buildFaqPageJsonLd,
  buildContactPageJsonLd,
  buildServicePageJsonLd,
} from '@/lib/seo/json-ld';
```

Add this import:
```typescript
import { getService } from '@/data/services';
```

Add this new describe block after the existing `buildContactPageJsonLd` describe block:

```typescript
describe('buildServicePageJsonLd', () => {
  const service = getService('roof-repair')!;
  const canonicalUrl = 'https://www.jerseycityqualityroofing.com/services/residential/roof-repair';

  it('returns Service schema with @context and @type', () => {
    const schema = buildServicePageJsonLd(service, canonicalUrl) as unknown as Record<string, unknown>;
    expect(schema['@context']).toBe('https://schema.org');
    expect(schema['@type']).toBe('Service');
  });

  it('includes service name and description', () => {
    const schema = buildServicePageJsonLd(service, canonicalUrl) as unknown as Record<string, unknown>;
    expect(schema.name).toBe('Roof Repair');
    expect(schema.description).toBe(service.fullDescription);
  });

  it('includes serviceType matching service name', () => {
    const schema = buildServicePageJsonLd(service, canonicalUrl) as unknown as Record<string, unknown>;
    expect(schema.serviceType).toBe('Roof Repair');
  });

  it('includes canonical URL', () => {
    const schema = buildServicePageJsonLd(service, canonicalUrl) as unknown as Record<string, unknown>;
    expect(schema.url).toBe(canonicalUrl);
  });

  it('includes provider with @type RoofingContractor', () => {
    const schema = buildServicePageJsonLd(service, canonicalUrl) as unknown as Record<string, unknown>;
    const provider = schema.provider as Record<string, unknown>;
    expect(provider['@type']).toBe('RoofingContractor');
    expect(provider.name).toBe(BUSINESS_INFO.name);
    expect(provider.telephone).toBe(BUSINESS_INFO.phone);
  });

  it('includes provider PostalAddress', () => {
    const schema = buildServicePageJsonLd(service, canonicalUrl) as unknown as Record<string, unknown>;
    const provider = schema.provider as Record<string, unknown>;
    const address = provider.address as Record<string, unknown>;
    expect(address['@type']).toBe('PostalAddress');
    expect(address.streetAddress).toBe(BUSINESS_INFO.address.street);
  });

  it('includes areaServed with 12 cities', () => {
    const schema = buildServicePageJsonLd(service, canonicalUrl) as unknown as Record<string, unknown>;
    expect(Array.isArray(schema.areaServed)).toBe(true);
    expect((schema.areaServed as unknown[]).length).toBe(12);
  });

  it('includes hasOfferCatalog with OfferCatalog type', () => {
    const schema = buildServicePageJsonLd(service, canonicalUrl) as unknown as Record<string, unknown>;
    const catalog = schema.hasOfferCatalog as Record<string, unknown>;
    expect(catalog['@type']).toBe('OfferCatalog');
    expect(catalog.name).toBe('Roof Repair Services');
  });

  it('OfferCatalog has itemListElement with Offer', () => {
    const schema = buildServicePageJsonLd(service, canonicalUrl) as unknown as Record<string, unknown>;
    const catalog = schema.hasOfferCatalog as Record<string, unknown>;
    const items = catalog.itemListElement as Array<Record<string, unknown>>;
    expect(items).toHaveLength(1);
    expect(items[0]['@type']).toBe('Offer');
    const offered = items[0].itemOffered as Record<string, unknown>;
    expect(offered['@type']).toBe('Service');
    expect(offered.name).toBe('Roof Repair');
  });
});
```
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/JerseyCityQualityRoofing && pnpm vitest run src/lib/__tests__/json-ld.test.ts 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/seo/json-ld.tsx contains `export function buildServicePageJsonLd`
    - src/lib/seo/json-ld.tsx contains `'@type': 'Service'`
    - src/lib/seo/json-ld.tsx contains `hasOfferCatalog`
    - src/lib/seo/json-ld.tsx contains `'@type': 'RoofingContractor'` inside the provider block
    - src/lib/seo/json-ld.tsx contains `Service as ServiceSchema` (renamed import)
    - src/lib/seo/json-ld.tsx contains `Service as ServiceData` (data type import)
    - src/lib/__tests__/json-ld.test.ts contains `describe('buildServicePageJsonLd'`
    - src/lib/__tests__/json-ld.test.ts contains `getService('roof-repair')`
    - All json-ld tests pass (pnpm vitest run src/lib/__tests__/json-ld.test.ts exits 0)
  </acceptance_criteria>
  <done>buildServicePageJsonLd produces valid Service schema with provider (RoofingContractor), areaServed (12 cities), hasOfferCatalog. All 30+ json-ld tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create dynamic OG image route and add emergency accent CSS variable</name>
  <files>src/app/api/og/route.tsx, src/styles/globals.css</files>
  <read_first>
    - src/styles/globals.css (existing @theme block -- add emergency accent variables inside it)
    - src/data/services.ts (getService function used in OG route)
    - src/lib/constants.ts (BASE_URL for fallback text)
    - .planning/phases/05-residential-service-pages/05-RESEARCH.md (OG image pattern section, font loading approach)
    - .planning/phases/05-residential-service-pages/05-UI-SPEC.md (OG image design spec: dark bg, gold text, 1200x630)
  </read_first>
  <action>
**1. Create src/app/api/og/route.tsx**

This is a Next.js Route Handler that generates dynamic Open Graph images using next/og ImageResponse.

```typescript
import { ImageResponse } from 'next/og';
import { getService } from '@/data/services';

export const runtime = 'edge';

export async function GET(request: Request) {
  const { searchParams } = new URL(request.url);
  const serviceSlug = searchParams.get('service');
  const service = serviceSlug ? getService(serviceSlug) : null;

  // Load Cormorant Bold font for the OG image
  const fontData = await fetch(
    new URL('https://fonts.gstatic.com/s/cormorant/v21/H4c2BXOCl9bbnla_nHIq75u9.ttf')
  ).then((res) => res.arrayBuffer());

  const title = service?.name ?? 'Professional Roofing Services';
  const subtitle = service?.shortDescription ?? 'Licensed, insured, and locally trusted across Hudson County.';

  return new ImageResponse(
    (
      <div
        style={{
          width: '100%',
          height: '100%',
          display: 'flex',
          flexDirection: 'column',
          justifyContent: 'center',
          alignItems: 'center',
          backgroundColor: '#2a2e22',
          padding: '60px 80px',
        }}
      >
        {/* Gold accent line at top */}
        <div
          style={{
            position: 'absolute',
            top: 0,
            left: 0,
            right: 0,
            height: '6px',
            backgroundColor: '#c89640',
          }}
        />

        {/* Service name */}
        <div
          style={{
            fontFamily: 'Cormorant',
            fontSize: '56px',
            fontWeight: 700,
            color: '#c89640',
            textAlign: 'center',
            lineHeight: 1.1,
            maxWidth: '900px',
          }}
        >
          {title}
        </div>

        {/* Short description */}
        <div
          style={{
            fontFamily: 'Cormorant',
            fontSize: '24px',
            fontWeight: 500,
            color: '#b0ae9e',
            textAlign: 'center',
            marginTop: '24px',
            maxWidth: '800px',
            lineHeight: 1.4,
          }}
        >
          {subtitle}
        </div>

        {/* Company name */}
        <div
          style={{
            fontFamily: 'Cormorant',
            fontSize: '28px',
            fontWeight: 700,
            color: '#f0ede6',
            marginTop: '48px',
          }}
        >
          Jersey City Quality Roofing
        </div>

        {/* Phone number */}
        <div
          style={{
            fontFamily: 'Cormorant',
            fontSize: '20px',
            fontWeight: 500,
            color: '#b0ae9e',
            marginTop: '12px',
          }}
        >
          (201) 555-0123
        </div>

        {/* Gold accent line at bottom */}
        <div
          style={{
            position: 'absolute',
            bottom: 0,
            left: 0,
            right: 0,
            height: '6px',
            backgroundColor: '#c89640',
          }}
        />
      </div>
    ),
    {
      width: 1200,
      height: 630,
      fonts: [
        {
          name: 'Cormorant',
          data: fontData,
          weight: 700,
          style: 'normal',
        },
      ],
    }
  );
}
```

**2. Add emergency accent CSS variables to src/styles/globals.css**

Inside the existing `@theme { ... }` block, after the `--color-success: #34d399;` line (line 18), add:

```css
  /* Emergency page accent (Phase 5) */
  --color-emergency-accent: #d4782f;
  --color-emergency-accent-hover: #e08a3f;
```

This makes `bg-emergency-accent`, `text-emergency-accent`, `hover:bg-emergency-accent-hover`, etc. available as Tailwind utility classes. Add these two lines INSIDE the @theme block, NOT outside it.

Do NOT modify any other existing CSS rules.
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/JerseyCityQualityRoofing && pnpm type-check 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/app/api/og/route.tsx contains `export const runtime = 'edge'`
    - src/app/api/og/route.tsx contains `export async function GET`
    - src/app/api/og/route.tsx contains `ImageResponse`
    - src/app/api/og/route.tsx contains `next/og`
    - src/app/api/og/route.tsx contains `getService`
    - src/app/api/og/route.tsx contains `1200` and `630` (dimensions)
    - src/app/api/og/route.tsx contains `#2a2e22` (dark background)
    - src/app/api/og/route.tsx contains `#c89640` (gold accent)
    - src/app/api/og/route.tsx contains `Cormorant` (font name)
    - src/styles/globals.css contains `--color-emergency-accent: #d4782f`
    - src/styles/globals.css contains `--color-emergency-accent-hover: #e08a3f`
    - Emergency accent variables are INSIDE the @theme block
    - pnpm type-check exits 0
  </acceptance_criteria>
  <done>OG image route at /api/og generates dark-bg gold-text PNG images per service. Emergency accent CSS variables declared in @theme block enabling Tailwind utility classes.</done>
</task>

</tasks>

<verification>
Run after both tasks complete:
```bash
pnpm vitest run src/lib/__tests__/json-ld.test.ts && pnpm type-check && pnpm lint
```
All JSON-LD tests pass, no type errors, no lint errors.
</verification>

<success_criteria>
- buildServicePageJsonLd produces Service schema with provider, areaServed, hasOfferCatalog
- JSON-LD tests extended with 9+ new tests for service schema, all passing
- /api/og route generates 1200x630 PNG with Cormorant font, dark bg, gold text
- globals.css has --color-emergency-accent: #d4782f and --color-emergency-accent-hover: #e08a3f in @theme block
</success_criteria>

<output>
After completion, create `.planning/phases/05-residential-service-pages/05-04-SUMMARY.md`
</output>
