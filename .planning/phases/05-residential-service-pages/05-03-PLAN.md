---
phase: 05-residential-service-pages
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/emergency-hero.tsx
  - src/components/sections/what-to-do-section.tsx
  - src/components/sections/insurance-claims-section.tsx
  - src/components/sections/storm-damage-types.tsx
  - src/components/sections/mid-page-cta.tsx
  - src/components/sections/related-services-row.tsx
autonomous: true
requirements:
  - RESI-04

must_haves:
  truths:
    - "EmergencyHero renders oversized phone number, 24/7 badge, CALL NOW button with emergency accent color"
    - "WhatToDoSection renders numbered emergency steps with emergency accent markers"
    - "InsuranceClaimsSection renders two-column checklist layout"
    - "StormDamageTypes renders grid of storm damage category cards"
    - "MidPageCTA renders compact CTA strip with phone number and estimate button"
    - "RelatedServicesRow renders 2-3 linked cards to related residential services"
  artifacts:
    - path: "src/components/sections/emergency-hero.tsx"
      provides: "Crisis-mode hero with oversized phone number and emergency accent"
      contains: "export function EmergencyHero"
    - path: "src/components/sections/what-to-do-section.tsx"
      provides: "Emergency steps checklist"
      contains: "export function WhatToDoSection"
    - path: "src/components/sections/insurance-claims-section.tsx"
      provides: "Two-column insurance claims help layout"
      contains: "export function InsuranceClaimsSection"
    - path: "src/components/sections/storm-damage-types.tsx"
      provides: "Storm damage category grid"
      contains: "export function StormDamageTypes"
    - path: "src/components/sections/mid-page-cta.tsx"
      provides: "Compact inline CTA strip"
      contains: "export function MidPageCTA"
    - path: "src/components/sections/related-services-row.tsx"
      provides: "Related service card links"
      contains: "export function RelatedServicesRow"
  key_links:
    - from: "src/components/sections/emergency-hero.tsx"
      to: "src/lib/constants.ts"
      via: "import { PHONE_NUMBER, PHONE_HREF }"
      pattern: "import.*PHONE_NUMBER.*from.*constants"
    - from: "src/components/sections/related-services-row.tsx"
      to: "src/data/services.ts"
      via: "import { getService }"
      pattern: "import.*getService.*from.*services"
    - from: "src/components/sections/mid-page-cta.tsx"
      to: "src/lib/constants.ts"
      via: "import { PHONE_NUMBER, PHONE_HREF }"
      pattern: "import.*PHONE_NUMBER.*from.*constants"
---

<objective>
Build 6 section components for the emergency template and shared CTA/linking sections used across all 4 service pages.

Purpose: EmergencyHero, WhatToDoSection, InsuranceClaimsSection, and StormDamageTypes are unique to the emergency page per D-12/D-13. MidPageCTA (D-03) and RelatedServicesRow (D-08) are shared across all 4 service pages.

Output: 6 new component files in src/components/sections/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-residential-service-pages/05-CONTEXT.md
@.planning/phases/05-residential-service-pages/05-UI-SPEC.md

@src/data/types.ts
@src/data/services.ts
@src/data/business-info.ts
@src/lib/constants.ts
@src/styles/globals.css
@src/components/sections/cta-banner.tsx
@src/components/sections/section-wrapper.tsx

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/data/types.ts (new types from Plan 01):
```typescript
export interface EmergencyStep {
  title: string;
  description: string;
}

export interface StormDamageType {
  icon: string;        // Lucide icon name
  name: string;
  description: string;
}

export interface InsuranceClaimsContent {
  intro: string;
  whatWeHandle: string[];
  whatToDocument: string[];
}
```

From src/data/types.ts (existing):
```typescript
export interface Service {
  name: string;
  slug: string;
  category: ServiceCategory;
  shortDescription: string;
  relatedServices: string[];
  // ...
}
```

From src/data/services.ts:
```typescript
export function getService(slug: string): Service | undefined;
```

From src/lib/constants.ts:
```typescript
export const PHONE_NUMBER = "(201) 555-0123";
export const PHONE_HREF = "tel:+12015550123";
```

Emergency accent color (to be added to globals.css by Plan 04):
```
--color-emergency-accent: #d4782f
```
Until the CSS variable is added, use the hex value `#d4782f` directly in emergency components via Tailwind arbitrary values: `bg-[#d4782f]`, `text-[#d4782f]`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build EmergencyHero, WhatToDoSection, and StormDamageTypes components</name>
  <files>src/components/sections/emergency-hero.tsx, src/components/sections/what-to-do-section.tsx, src/components/sections/storm-damage-types.tsx</files>
  <read_first>
    - src/components/sections/hero-section.tsx (existing hero -- EmergencyHero is fundamentally different but follow accessibility patterns)
    - src/components/sections/cta-banner.tsx (reference for CTA button styling patterns)
    - src/lib/constants.ts (PHONE_NUMBER, PHONE_HREF)
    - src/styles/globals.css (existing theme variables -- emergency accent not yet added, use hex directly)
    - .planning/phases/05-residential-service-pages/05-UI-SPEC.md (EmergencyHero, WhatToDoSection, StormDamageTypes visual specs)
  </read_first>
  <action>
**Component 1: src/components/sections/emergency-hero.tsx** (Server Component -- NO "use client")

Props interface:
```typescript
interface EmergencyHeroProps {
  phoneNumber: string;
  phoneHref: string;
}
```

Layout per UI-SPEC EmergencyHero spec and D-12:
- Full-width dominant background with bottom border: `border-b-4 border-[#d4782f]`
- Vertical padding: `py-20 sm:py-24 lg:py-28`
- Container: `max-w-5xl mx-auto px-4 sm:px-6 lg:px-8 text-center`
- 24/7 badge: `inline-flex items-center bg-[#d4782f] text-dominant px-4 py-2 rounded-full text-sm font-bold uppercase tracking-wide` -- text: "24/7 EMERGENCY SERVICE"
- h1: `text-[2.5rem] sm:text-[3rem] lg:text-[4rem] font-heading font-bold text-text-primary mt-6 leading-[1.1]` -- text: "24/7 Emergency Roof Repair"
- Subtitle: `text-lg text-text-secondary mt-4 max-w-2xl mx-auto` -- text: "Storm damage? Roof leak? We respond immediately."
- Phone display: `mt-8`
  - `<a href={phoneHref}>` with `text-[2.5rem] lg:text-[3rem] font-heading font-bold text-[#d4782f] hover:opacity-80 transition-opacity` -- renders `phoneNumber`
- CALL NOW button: `mt-6 inline-flex items-center justify-center bg-[#d4782f] hover:bg-[#e08a3f] text-dominant font-bold text-xl px-10 py-4 min-h-[56px] rounded-lg transition-colors duration-[150ms] focus-ring`
  - `<a href={phoneHref}>` -- text: "Call Now" with Phone icon (lucide-react, size=24) to the left
- Scroll prompt: `text-sm text-text-secondary mt-6` -- text: "Or scroll down to request emergency service online"

Import Phone icon from `lucide-react`.

**Component 2: src/components/sections/what-to-do-section.tsx** (Server Component)

Props interface:
```typescript
interface WhatToDoSectionProps {
  steps: readonly EmergencyStep[];
  phoneNumber: string;
  phoneHref: string;
}
```

Layout per UI-SPEC WhatToDoSection spec:
- h2: `text-[1.75rem] font-heading font-bold text-text-primary` -- text: "What To Do Right Now"
- p: `text-lg text-text-secondary mt-2` -- text: "If you have an active roof emergency, follow these steps:"
- Numbered list container: `mt-6 space-y-3`
- Each step: `flex items-start gap-4 py-3`
  - Number circle: `w-8 h-8 rounded-full bg-[#d4782f] text-dominant font-bold flex items-center justify-center shrink-0 text-sm` -- step index + 1
  - Content: `flex-1`
    - h3: `text-lg font-heading font-bold text-text-primary` -- step.title
    - p: `text-lg text-text-secondary mt-1` -- step.description
- Emergency CTA below steps: `mt-6 text-center`
  - p: `text-lg text-text-primary` -- "Don't wait -- call us now:"
  - Phone display: `text-[1.75rem] font-heading text-[#d4782f] font-bold mt-2`
    - `<a href={phoneHref}>` -- renders `phoneNumber`
  - CALL NOW button: same styling as EmergencyHero but slightly smaller: `mt-4 inline-flex items-center justify-center bg-[#d4782f] hover:bg-[#e08a3f] text-dominant font-bold px-8 py-3 min-h-[44px] rounded-lg transition-colors duration-[150ms] focus-ring`

Import EmergencyStep from `@/data/types`. Import Phone icon from `lucide-react`.

**Component 3: src/components/sections/storm-damage-types.tsx** (Server Component)

Props interface:
```typescript
interface StormDamageTypesProps {
  damageTypes: readonly StormDamageType[];
}
```

Layout per UI-SPEC StormDamageTypes spec:
- h2: `text-[1.75rem] font-heading font-bold text-text-primary` -- text: "Types of Storm Damage We Repair"
- Grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 mt-6`
- Each card: `bg-secondary rounded-lg p-6`
  - Icon: Use the SAME Lucide icon mapping object pattern as WarningSignsSection (Plan 02). Map icon name string to Lucide component. Include: `Wind`, `CloudRain`, `Snowflake`, `Zap`, `TreePine`, `CloudLightning`, `Haze`, `AlertTriangle` (default). Render with `text-[#d4782f] mb-2` and `size={24} aria-hidden="true"`
  - h3: `text-lg font-heading font-bold text-text-primary mt-2` -- damageType.name
  - p: `text-lg text-text-secondary mt-1` -- damageType.description

Import StormDamageType from `@/data/types`. Import needed Lucide icons.
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/JerseyCityQualityRoofing && pnpm type-check 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/sections/emergency-hero.tsx contains `export function EmergencyHero`
    - src/components/sections/emergency-hero.tsx contains `24/7 EMERGENCY SERVICE`
    - src/components/sections/emergency-hero.tsx contains `Call Now`
    - src/components/sections/emergency-hero.tsx contains `#d4782f`
    - src/components/sections/emergency-hero.tsx contains `min-h-[56px]` (2x standard button)
    - src/components/sections/emergency-hero.tsx does NOT contain `"use client"`
    - src/components/sections/what-to-do-section.tsx contains `export function WhatToDoSection`
    - src/components/sections/what-to-do-section.tsx contains `What To Do Right Now`
    - src/components/sections/what-to-do-section.tsx contains `#d4782f`
    - src/components/sections/storm-damage-types.tsx contains `export function StormDamageTypes`
    - src/components/sections/storm-damage-types.tsx contains `Types of Storm Damage We Repair`
    - pnpm type-check exits 0
  </acceptance_criteria>
  <done>EmergencyHero renders phone-first crisis hero with oversized phone, 24/7 badge, and CALL NOW button in emergency accent. WhatToDoSection renders numbered emergency steps. StormDamageTypes renders damage category grid.</done>
</task>

<task type="auto">
  <name>Task 2: Build InsuranceClaimsSection, MidPageCTA, and RelatedServicesRow components</name>
  <files>src/components/sections/insurance-claims-section.tsx, src/components/sections/mid-page-cta.tsx, src/components/sections/related-services-row.tsx</files>
  <read_first>
    - src/components/sections/cta-banner.tsx (MidPageCTA mirrors this layout with different copy)
    - src/data/services.ts (getService function for RelatedServicesRow to resolve service slugs to names)
    - src/lib/constants.ts (PHONE_NUMBER, PHONE_HREF)
    - src/data/types.ts (InsuranceClaimsContent type)
    - .planning/phases/05-residential-service-pages/05-UI-SPEC.md (InsuranceClaimsSection, MidPageCTA, RelatedServicesRow visual specs)
  </read_first>
  <action>
**Component 1: src/components/sections/insurance-claims-section.tsx** (Server Component)

Props interface:
```typescript
interface InsuranceClaimsSectionProps {
  content: InsuranceClaimsContent;
}
```

Layout per UI-SPEC InsuranceClaimsSection spec:
- h2: `text-[1.75rem] font-heading font-bold text-text-primary` -- text: "Insurance Claims Help"
- p: `text-lg text-text-secondary mt-2` -- content.intro
- Two-column grid: `grid grid-cols-1 lg:grid-cols-2 gap-6 mt-6`
  - Column 1: "What We Handle"
    - h3: `text-lg font-heading font-bold text-text-primary mb-3` -- "What We Handle"
    - Each item: `flex items-start gap-3 py-2`
      - CheckCircle icon: `text-success shrink-0 mt-1` size=16
      - `text-lg text-text-secondary` -- item text
  - Column 2: "What To Document"
    - h3: `text-lg font-heading font-bold text-text-primary mb-3` -- "What To Document"
    - Each item: `flex items-start gap-3 py-2`
      - Camera icon for photo items, FileText icon for document items -- alternate or use Camera for all: `text-accent shrink-0 mt-1` size=16
      - `text-lg text-text-secondary` -- item text

Import InsuranceClaimsContent from `@/data/types`. Import CheckCircle, Camera from `lucide-react`.

**Component 2: src/components/sections/mid-page-cta.tsx** (Server Component)

Props interface:
```typescript
interface MidPageCTAProps {
  heading?: string;
  phoneNumber?: string;
  phoneHref?: string;
  accentColor?: string; // For emergency page: 'bg-[#d4782f] hover:bg-[#e08a3f]'
}
```

Layout per UI-SPEC MidPageCTA spec and D-03:
- Full-width: `bg-secondary py-8 lg:py-10`
- Container: `max-w-7xl mx-auto px-4 sm:px-6 lg:px-8`
- Inner flex: `flex flex-col sm:flex-row items-center justify-between gap-4`
  - Left (text): `text-center sm:text-left`
    - p: `text-[1.75rem] font-heading font-bold text-text-primary` -- `heading ?? "Ready to Get Started?"`
    - p: `text-lg text-text-secondary mt-1` -- "Free estimates with no obligation."
  - Right (actions): `flex items-center gap-3 shrink-0`
    - Phone link: `text-accent flex items-center gap-2 text-lg font-bold min-h-[44px]` -- Phone icon (size=20) + `phoneNumber ?? PHONE_NUMBER`
    - CTA button: `<a href="#quote-form">` with classes `${accentColor ?? 'bg-accent hover:bg-accent-hover'} text-dominant font-bold px-6 py-3 rounded-lg min-h-[44px] transition-colors duration-[150ms] focus-ring inline-flex items-center` -- text: "Get Your Free Estimate"

Import PHONE_NUMBER, PHONE_HREF from `@/lib/constants`. Import Phone icon from `lucide-react`.

**Component 3: src/components/sections/related-services-row.tsx** (Server Component)

Props interface:
```typescript
interface RelatedServicesRowProps {
  serviceSlugs: readonly string[];
}
```

Layout per UI-SPEC RelatedServicesRow spec and D-08:
- h2: `text-[1.75rem] font-heading font-bold text-text-primary` -- text: "Related Services"
- Grid: `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4 mt-6`
- For each slug, resolve via `getService(slug)` -- skip if undefined
- Each card: `<Link href={/services/residential/${service.slug}}>` wrapping a div with classes `bg-secondary rounded-lg p-6 group hover:bg-secondary-lighter transition-colors duration-[150ms]`
  - Icon: Map service slugs to Lucide icons:
    - `roof-repair` -> `Wrench`
    - `roof-replacement` -> `Home`
    - `roof-inspection` -> `Search`
    - `emergency-roofing` -> `AlertTriangle`
    - default -> `Hammer`
  - Render icon: `text-accent mb-2` size=24, aria-hidden="true"
  - h3: `text-lg font-heading font-bold text-text-primary` -- service.name
  - p: `text-lg text-text-secondary mt-1 line-clamp-2` -- service.shortDescription
  - Arrow row: `mt-4 flex items-center gap-1 text-accent text-sm`
    - Text: `View ${service.name}`
    - ArrowRight icon: size=16, `group-hover:translate-x-1 transition-transform duration-[150ms]`

Import Link from `next/link`. Import getService from `@/data/services`. Import Wrench, Home, Search, AlertTriangle, Hammer, ArrowRight from `lucide-react`.
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/JerseyCityQualityRoofing && pnpm type-check 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - src/components/sections/insurance-claims-section.tsx contains `export function InsuranceClaimsSection`
    - src/components/sections/insurance-claims-section.tsx contains `Insurance Claims Help`
    - src/components/sections/insurance-claims-section.tsx contains `What We Handle`
    - src/components/sections/insurance-claims-section.tsx contains `What To Document`
    - src/components/sections/insurance-claims-section.tsx contains `CheckCircle`
    - src/components/sections/mid-page-cta.tsx contains `export function MidPageCTA`
    - src/components/sections/mid-page-cta.tsx contains `Ready to Get Started?`
    - src/components/sections/mid-page-cta.tsx contains `Get Your Free Estimate`
    - src/components/sections/mid-page-cta.tsx contains `PHONE_NUMBER`
    - src/components/sections/related-services-row.tsx contains `export function RelatedServicesRow`
    - src/components/sections/related-services-row.tsx contains `Related Services`
    - src/components/sections/related-services-row.tsx contains `next/link`
    - src/components/sections/related-services-row.tsx contains `getService`
    - src/components/sections/related-services-row.tsx contains `ArrowRight`
    - pnpm type-check exits 0
  </acceptance_criteria>
  <done>InsuranceClaimsSection renders two-column checklist. MidPageCTA renders compact CTA strip with phone and button. RelatedServicesRow renders linked service cards with hover animation. All pass type-check.</done>
</task>

</tasks>

<verification>
Run after both tasks complete:
```bash
pnpm type-check && pnpm lint
```
No type errors, no lint errors. All 6 components export correctly typed functions.
</verification>

<success_criteria>
- 6 new section components in src/components/sections/
- EmergencyHero: oversized phone, 24/7 badge, CALL NOW 2x button, emergency accent #d4782f
- WhatToDoSection: numbered emergency steps, emergency accent markers, phone CTA
- StormDamageTypes: 3-col grid of damage type cards
- InsuranceClaimsSection: two-column checklist layout
- MidPageCTA: compact CTA strip with phone + estimate button
- RelatedServicesRow: linked service cards with hover arrow animation
- pnpm type-check passes, pnpm lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-residential-service-pages/05-03-SUMMARY.md`
</output>
