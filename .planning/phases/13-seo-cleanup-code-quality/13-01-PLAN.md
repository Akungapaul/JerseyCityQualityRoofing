---
phase: 13-seo-cleanup-code-quality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(marketing)/services/page.tsx
  - src/app/(marketing)/services/residential/page.tsx
  - src/app/(marketing)/services/commercial/page.tsx
  - src/app/(marketing)/services/residential/[service]/[city]/page.tsx
  - src/app/(marketing)/services/commercial/[service]/[city]/page.tsx
  - src/app/(marketing)/service-areas/[city]/page.tsx
  - src/app/(marketing)/blog/page.tsx
  - src/app/(marketing)/blog/[slug]/page.tsx
  - src/app/(marketing)/guides/page.tsx
  - src/app/(marketing)/guides/cost/[slug]/page.tsx
  - src/app/(marketing)/guides/materials/[slug]/page.tsx
  - src/app/(marketing)/problems/page.tsx
  - src/app/(marketing)/problems/[slug]/page.tsx
  - src/app/sitemap.ts
  - src/lib/__tests__/sitemap.test.ts
  - src/app/(marketing)/testimonials/page.tsx
autonomous: true
requirements:
  - SEO-06

must_haves:
  truths:
    - "Each page emits exactly one BreadcrumbList JSON-LD script (from layout Breadcrumbs component), not two"
    - "All non-breadcrumb JSON-LD (RoofingContractor, FAQPage, CollectionPage, BlogPosting, Service) remains intact on every page"
    - "Testimonials stub page is excluded from sitemap XML output"
    - "Testimonials page has noindex metadata as defense-in-depth"
  artifacts:
    - path: "src/app/(marketing)/services/page.tsx"
      provides: "Services hub page without duplicate BreadcrumbList JSON-LD"
      contains: "buildRoofingContractorJsonLd"
    - path: "src/app/sitemap.ts"
      provides: "Sitemap without testimonials entry"
    - path: "src/lib/__tests__/sitemap.test.ts"
      provides: "Sitemap test asserting testimonials exclusion"
      contains: "testimonials"
  key_links:
    - from: "src/app/(marketing)/blog/page.tsx"
      to: "src/lib/seo/json-ld"
      via: "import (minus buildBreadcrumbJsonLd)"
      pattern: "buildCollectionPageJsonLd|buildRoofingContractorJsonLd"
---

<objective>
Remove duplicate BreadcrumbList JSON-LD from 13 page files and exclude testimonials stub from sitemap.

Purpose: The layout-level Breadcrumbs component already emits BreadcrumbList JSON-LD on every non-homepage page. The 13 page-level buildBreadcrumbJsonLd calls create duplicate structured data that Google Search Console flags as unnecessary. The testimonials page is a stub with only an h1 and placeholder text -- including it in the sitemap risks thin-content signals.

Output: 13 page files cleaned of duplicate BreadcrumbList JSON-LD, sitemap without testimonials entry, testimonials page with noindex metadata, updated sitemap test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-seo-cleanup-code-quality/13-RESEARCH.md

<interfaces>
<!-- Key imports the executor needs to understand. -->

From src/lib/seo/json-ld (the shared JSON-LD module):
- buildBreadcrumbJsonLd -- REMOVE from page-level imports (keep in breadcrumbs.tsx only)
- buildRoofingContractorJsonLd -- KEEP in all files that use it
- buildCollectionPageJsonLd -- KEEP in hub pages
- buildFaqPageJsonLd -- KEEP in pages with FAQs
- buildBlogPostingJsonLd -- KEEP in blog pages
- buildServicePageJsonLd -- KEEP in service pages
- buildCityServiceJsonLd -- KEEP in service-in-city pages
- JsonLd -- KEEP if any other JSON-LD remains in the file

From src/components/layout/breadcrumbs.tsx:
- Client component using usePathname() that renders BOTH visual breadcrumbs AND BreadcrumbList JSON-LD script
- This is the SOLE source of BreadcrumbList JSON-LD after cleanup
- SEGMENT_LABELS covers: services, residential, commercial, service-areas, about, contact, blog
- slugToTitle fallback handles: guides, cost, materials, problems, gallery, testimonials (all produce correct labels)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove duplicate BreadcrumbList JSON-LD from 13 page files</name>
  <files>
    src/app/(marketing)/services/page.tsx
    src/app/(marketing)/services/residential/page.tsx
    src/app/(marketing)/services/commercial/page.tsx
    src/app/(marketing)/services/residential/[service]/[city]/page.tsx
    src/app/(marketing)/services/commercial/[service]/[city]/page.tsx
    src/app/(marketing)/service-areas/[city]/page.tsx
    src/app/(marketing)/blog/page.tsx
    src/app/(marketing)/blog/[slug]/page.tsx
    src/app/(marketing)/guides/page.tsx
    src/app/(marketing)/guides/cost/[slug]/page.tsx
    src/app/(marketing)/guides/materials/[slug]/page.tsx
    src/app/(marketing)/problems/page.tsx
    src/app/(marketing)/problems/[slug]/page.tsx
  </files>
  <read_first>
    src/app/(marketing)/services/page.tsx
    src/app/(marketing)/services/residential/page.tsx
    src/app/(marketing)/services/commercial/page.tsx
    src/app/(marketing)/services/residential/[service]/[city]/page.tsx
    src/app/(marketing)/services/commercial/[service]/[city]/page.tsx
    src/app/(marketing)/service-areas/[city]/page.tsx
    src/app/(marketing)/blog/page.tsx
    src/app/(marketing)/blog/[slug]/page.tsx
    src/app/(marketing)/guides/page.tsx
    src/app/(marketing)/guides/cost/[slug]/page.tsx
    src/app/(marketing)/guides/materials/[slug]/page.tsx
    src/app/(marketing)/problems/page.tsx
    src/app/(marketing)/problems/[slug]/page.tsx
    src/components/layout/breadcrumbs.tsx
  </read_first>
  <action>
For each of the 13 page files, perform these 3 removals:

**A. Remove `buildBreadcrumbJsonLd` from the import statement.**
- In the `import { ... } from "@/lib/seo/json-ld"` line, remove `buildBreadcrumbJsonLd` from the destructured imports.
- Keep `JsonLd` and ALL other imports (`buildRoofingContractorJsonLd`, `buildCollectionPageJsonLd`, `buildFaqPageJsonLd`, `buildBlogPostingJsonLd`, `buildServicePageJsonLd`, `buildCityServiceJsonLd`, etc.).
- If `buildBreadcrumbJsonLd` is the last item before the closing brace, remove the trailing comma from the previous import.
- Also remove the `BASE_URL` import from `@/lib/constants` ONLY if it was exclusively used for breadcrumb items. In most files BASE_URL is also used for other JSON-LD or metadata, so CHECK before removing.

**B. Remove the `<JsonLd data={buildBreadcrumbJsonLd(...)} />` JSX block.**
- Each file has exactly one `<JsonLd>` element that wraps `buildBreadcrumbJsonLd(...)`. Remove the entire `<JsonLd data={...} />` element.
- For inline breadcrumb arrays: the entire `<JsonLd data={ buildBreadcrumbJsonLd([ ... ]) as unknown as Record<string, unknown> } />` block.
- For variable-based breadcrumbs: the `<JsonLd data={ buildBreadcrumbJsonLd(breadcrumbItems) as unknown as Record<string, unknown> } />` block.
- Keep the adjacent `<JsonLd>` elements for other schema types.

**C. Remove `breadcrumbItems` variable declarations (3 files only).**
These 3 files have a `const breadcrumbItems = [...]` array used only by the removed breadcrumb JSON-LD:
1. `src/app/(marketing)/service-areas/[city]/page.tsx` -- line ~128: remove the entire `const breadcrumbItems = [...]` declaration
2. `src/app/(marketing)/services/residential/[service]/[city]/page.tsx` -- line ~284: remove the entire `const breadcrumbItems = [...]` declaration
3. `src/app/(marketing)/services/commercial/[service]/[city]/page.tsx` -- line ~283: remove the entire `const breadcrumbItems = [...]` declaration

**Also update or remove JSON-LD comments** that mention "Breadcrumb" in the JSX comment blocks. For example, `{/* JSON-LD: CollectionPage + RoofingContractor + Breadcrumb */}` should become `{/* JSON-LD: CollectionPage + RoofingContractor */}`.

Per SEO-06: The layout-level Breadcrumbs component already satisfies this requirement. This task removes the DUPLICATE page-level JSON-LD.
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/JerseyCityQualityRoofing && grep -r "buildBreadcrumbJsonLd" src/app/ --include="*.tsx" | wc -l</automated>
    Expected output: 0 (no page files import or use buildBreadcrumbJsonLd)
    Additionally: `grep -r "breadcrumbItems" src/app/ --include="*.tsx" | wc -l` should output 0
  </verify>
  <acceptance_criteria>
    - `grep -r "buildBreadcrumbJsonLd" src/app/ --include="*.tsx" | wc -l` outputs 0
    - `grep -r "breadcrumbItems" src/app/ --include="*.tsx" | wc -l` outputs 0
    - `grep -r "buildRoofingContractorJsonLd" src/app/ --include="*.tsx" | wc -l` outputs a number greater than 0 (other JSON-LD preserved)
    - `grep -r "JsonLd" src/app/ --include="*.tsx" | wc -l` outputs a number greater than 0 (JsonLd component still used)
    - `pnpm type-check` passes with 0 errors in the 13 modified files (no unused imports)
    - `pnpm lint` passes with 0 unused-import errors in the 13 modified files
  </acceptance_criteria>
  <done>All 13 page files emit zero BreadcrumbList JSON-LD. Layout Breadcrumbs component remains the sole BreadcrumbList source. All other JSON-LD (RoofingContractor, FAQ, Collection, BlogPosting, Service) intact.</done>
</task>

<task type="auto">
  <name>Task 2: Exclude testimonials from sitemap and add noindex metadata</name>
  <files>
    src/app/sitemap.ts
    src/lib/__tests__/sitemap.test.ts
    src/app/(marketing)/testimonials/page.tsx
  </files>
  <read_first>
    src/app/sitemap.ts
    src/lib/__tests__/sitemap.test.ts
    src/app/(marketing)/testimonials/page.tsx
  </read_first>
  <action>
**A. Remove testimonials from sitemap.ts:**
In `src/app/sitemap.ts`, remove line 21:
```
{ url: `${BASE_URL}/testimonials`, changeFrequency: 'monthly', priority: 0.6 },
```
This reduces the static pages array from 12 entries to 11.

**B. Add noindex to testimonials page metadata:**
In `src/app/(marketing)/testimonials/page.tsx`, add `robots: { index: false, follow: true }` to the metadata export:
```typescript
export const metadata: Metadata = {
  ...generatePageMetadata({
    title: 'Customer Testimonials',
    description:
      'Read reviews from homeowners and businesses across Hudson County who trust Jersey City Quality Roofing for their roofing needs.',
    path: '/testimonials',
  }),
  robots: { index: false, follow: true },
};
```

**C. Update sitemap test:**
In `src/lib/__tests__/sitemap.test.ts`:

1. Update the total URL count test: Change `expect(entries).toHaveLength(155)` to `expect(entries).toHaveLength(154)` (one fewer URL since testimonials removed). Update the comment to reflect the new breakdown: `// 8 static + 3 silo index + 4 residential + 4 commercial + 12 city + 48 res-city + 48 com-city + 8 blog + 8 cost guides + 6 material guides + 5 problems = 154`

2. Add a new test asserting testimonials exclusion:
```typescript
it('excludes testimonials stub page from sitemap', () => {
  const entries = sitemap();
  const urls = entries.map((e) => e.url);
  expect(urls).not.toContain(`${BASE_URL}/testimonials`);
});
```
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/JerseyCityQualityRoofing && pnpm vitest run src/lib/__tests__/sitemap.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - `grep "testimonials" src/app/sitemap.ts | wc -l` outputs 0
    - `src/lib/__tests__/sitemap.test.ts` contains `not.toContain` and `testimonials`
    - `src/lib/__tests__/sitemap.test.ts` contains `toHaveLength(154)`
    - `src/app/(marketing)/testimonials/page.tsx` contains `robots:` and `index: false`
    - `pnpm vitest run src/lib/__tests__/sitemap.test.ts -x` passes (exit code 0)
    - `pnpm type-check` passes for all 3 modified files
  </acceptance_criteria>
  <done>Testimonials stub excluded from sitemap XML. Testimonials page has noindex robots directive. Sitemap test updated with correct count (154) and explicit testimonials exclusion assertion.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `grep -r "buildBreadcrumbJsonLd" src/app/ --include="*.tsx" | wc -l` outputs 0
2. `grep "testimonials" src/app/sitemap.ts | wc -l` outputs 0
3. `pnpm type-check` passes with 0 errors
4. `pnpm lint` passes with 0 errors
5. `pnpm vitest run src/lib/__tests__/sitemap.test.ts -x` passes
6. `pnpm test` full suite passes
</verification>

<success_criteria>
- Zero duplicate BreadcrumbList JSON-LD on any page (only layout Breadcrumbs component emits it)
- All other JSON-LD schemas intact across all pages
- Testimonials excluded from sitemap and marked noindex
- All tests, type-check, and lint pass
</success_criteria>

<output>
After completion, create `.planning/phases/13-seo-cleanup-code-quality/13-01-SUMMARY.md`
</output>
