---
phase: 09-blog-supporting-content
plan: 05
type: execute
wave: 3
depends_on: [01, 02, 03, 04]
files_modified:
  - src/app/(marketing)/blog/page.tsx
  - src/app/(marketing)/blog/[slug]/page.tsx
  - src/app/(marketing)/guides/page.tsx
  - src/app/(marketing)/guides/cost/[slug]/page.tsx
  - src/app/(marketing)/guides/materials/[slug]/page.tsx
  - src/app/(marketing)/problems/page.tsx
  - src/app/(marketing)/problems/[slug]/page.tsx
  - src/lib/internal-links.ts
  - src/app/sitemap.ts
  - src/data/__tests__/blog-content.test.ts
  - src/data/__tests__/cost-guide-content.test.ts
  - src/data/__tests__/material-guide-content.test.ts
  - src/data/__tests__/problem-content.test.ts
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-05
  - CONT-06
  - SEO-05

must_haves:
  truths:
    - "Blog article page template renders BlogHero, BadgeStrip, TableOfContents, ArticleBody sections, RelatedGuides, FaqAccordion, RelatedArticles, AuthorBio, QuoteForm, CTABanner with SiloPillarLink for silo articles"
    - "Cost guide page template renders GuideHero, CostTable, LocationPricingSection, FaqAccordion, RelatedGuides, QuoteForm, CTABanner"
    - "Material guide page template renders GuideHero, MaterialComparison pros/cons, FaqAccordion, QuoteForm, CTABanner"
    - "Problem page template renders GuideHero, IdentificationSignsList, ProblemSolutionCTA, FaqAccordion, QuoteForm, CTABanner"
    - "Hub pages (/blog/, /guides/, /problems/) render article/guide/problem cards in grids with intro prose"
    - "initializeContentRegistry() in internal-links.ts populates the registry from all content files"
    - "sitemap.ts includes dynamic entries for all blog articles, cost guides, material guides, and problem pages"
    - "All Wave 0 test scaffolds are activated (describe.skip -> describe) and pass"
  artifacts:
    - path: "src/app/(marketing)/blog/[slug]/page.tsx"
      provides: "Full blog article page with silo linking"
      contains: "BlogHero"
    - path: "src/app/(marketing)/guides/cost/[slug]/page.tsx"
      provides: "Full cost guide page with pricing table"
      contains: "CostTable"
    - path: "src/app/(marketing)/guides/materials/[slug]/page.tsx"
      provides: "Full material guide page with pros/cons"
      contains: "MaterialComparison"
    - path: "src/app/(marketing)/problems/[slug]/page.tsx"
      provides: "Full problem-solution page with service CTAs"
      contains: "ProblemSolutionCTA"
  key_links:
    - from: "src/app/(marketing)/blog/[slug]/page.tsx"
      to: "src/data/content/blog/index.ts"
      via: "import { getBlogArticle, ALL_BLOG_ARTICLES } from '@/data/content/blog'"
      pattern: "getBlogArticle"
    - from: "src/lib/internal-links.ts"
      to: "src/data/content/blog/index.ts"
      via: "initializeContentRegistry imports all content registries"
      pattern: "ALL_BLOG_ARTICLES"
    - from: "src/app/sitemap.ts"
      to: "src/data/content/blog/index.ts"
      via: "import ALL_BLOG_ARTICLES for sitemap generation"
      pattern: "ALL_BLOG_ARTICLES"
---

<objective>
Rewrite all page templates from stubs to full implementations, wire the internal linking registry, update the sitemap, and activate all Wave 0 test scaffolds.

Purpose: This plan connects all content data (from Plans 03-04) to user-facing pages. After this plan, every blog article, guide, and problem page renders with full design, JSON-LD, metadata, and automated internal linking.
Output: 7 rewritten page templates, populated internal-links registry, extended sitemap, and all tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-blog-supporting-content/09-RESEARCH.md
@.planning/phases/09-blog-supporting-content/09-UI-SPEC.md

<interfaces>
From Plans 03-04 content registries:
```typescript
// src/data/content/blog/index.ts
export const ALL_BLOG_ARTICLES: BlogArticle[];
export function getBlogArticle(slug: string): BlogArticle | undefined;
export function getSiloSupportingArticles(): BlogArticle[];
export function getStandaloneArticles(): BlogArticle[];

// src/data/content/cost-guides/index.ts
export const ALL_COST_GUIDES: CostGuide[];
export function getCostGuide(slug: string): CostGuide | undefined;
export function getCostGuideByService(serviceSlug: string): CostGuide | undefined;

// src/data/content/material-guides/index.ts
export const ALL_MATERIAL_GUIDES: MaterialGuide[];
export function getMaterialGuide(slug: string): MaterialGuide | undefined;

// src/data/content/problems/index.ts
export const ALL_PROBLEMS: ProblemSolution[];
export function getProblem(slug: string): ProblemSolution | undefined;
```

From Plan 01:
```typescript
// src/lib/internal-links.ts
export function initializeContentRegistry(): void; // empty body, to be wired
export function getRelatedBlogArticles(currentSlug: string, limit?: number): InternalLink[];
export function getRelatedGuides(currentSlug: string, limit?: number): InternalLink[];
export function getProblemRelatedServices(problemSlug: string): InternalLink[];
// ... all other link functions

// src/lib/seo/json-ld.tsx
export function buildBlogPostingJsonLd(article: {...}): WithContext<BlogPosting>;
export function buildCollectionPageJsonLd(hub: {...}): WithContext<CollectionPage>;
export function buildRoofingContractorJsonLd(): WithContext<RoofingContractor>;
export function buildBreadcrumbJsonLd(items: ...): WithContext<BreadcrumbList>;
export function buildFaqPageJsonLd(faqs: ...): WithContext<FAQPage>;
```

From Plan 02 — new components:
```
blog-hero.tsx, guide-hero.tsx, blog-article-body.tsx, table-of-contents.tsx,
related-articles.tsx, related-guides.tsx, cost-table.tsx, material-comparison.tsx,
problem-solution-cta.tsx, location-pricing-section.tsx, identification-signs-list.tsx,
blog-card.tsx, guide-card.tsx, reading-time-badge.tsx, author-bio.tsx, silo-pillar-link.tsx
```

Existing reusable: SectionWrapper, ScrollReveal, BadgeStrip, CTABanner, FaqAccordion, MidPageCTA, ServiceContentSection, QuoteForm
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite blog page templates (hub + article page)</name>
  <files>
    src/app/(marketing)/blog/page.tsx,
    src/app/(marketing)/blog/[slug]/page.tsx
  </files>
  <read_first>
    src/app/(marketing)/blog/page.tsx,
    src/app/(marketing)/blog/[slug]/page.tsx,
    src/app/(marketing)/services/residential/[service]/page.tsx,
    src/components/sections/section-wrapper.tsx,
    src/components/sections/scroll-reveal.tsx,
    src/lib/seo/json-ld.tsx,
    src/lib/seo/metadata.ts,
    src/lib/constants.ts,
    src/data/business-info.ts,
    .planning/phases/09-blog-supporting-content/09-UI-SPEC.md
  </read_first>
  <action>
**Rewrite `src/app/(marketing)/blog/page.tsx` (Blog Hub Page)**

Replace stub with full hub page per UI-SPEC Hub Page Template:

1. JsonLd: CollectionPage + RoofingContractor + Breadcrumb (Home > Blog)
2. GuideHero: H1 "Roofing Blog — Expert Tips & Guides", subtitle, categoryBadgeLabel "ROOFING BLOG"
3. BadgeStrip
4. SectionWrapper tone="dominant": Intro prose (200-300 words)
5. SectionWrapper tone="secondary" + ScrollReveal: Grid of BlogCards sorted by publishDate descending
6. CTABanner

Do NOT wrap in `<main>` — marketing layout provides it.

**Rewrite `src/app/(marketing)/blog/[slug]/page.tsx` (Blog Article Page)**

Replace stub with full article template per UI-SPEC Blog Article Page Template:

- `dynamicParams = false`, `generateStaticParams` from ALL_BLOG_ARTICLES
- `generateMetadata` with title, description, ogType 'article'
- Call `initializeContentRegistry()` before using link functions
- Page wrapped in `<article>` (NOT `<main>`)
- Section alternation: BlogHero > BadgeStrip > Intro (ServiceContentSection) > TableOfContents > ArticleSections (BlogArticleBody, alternating tones) > SiloPillarLink (silo articles only) > RelatedGuides > FaqAccordion > RelatedArticles > AuthorBio > QuoteForm > CTABanner
- SiloPillarLink: label "FROM OUR SERVICE LIBRARY", linkText "Read More About {serviceName}", href = parentPillarLink
- headingId: slugified heading text (lowercase, spaces to hyphens)
- JSON-LD: BlogPosting + FAQPage + RoofingContractor + Breadcrumb
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/JerseyCityQualityRoofing && npx tsc --noEmit --pretty 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(marketing)/blog/page.tsx contains `ALL_BLOG_ARTICLES` and `BlogCard`
    - src/app/(marketing)/blog/page.tsx does NOT contain `This page is under construction`
    - src/app/(marketing)/blog/[slug]/page.tsx contains `<article` and `BlogHero`
    - src/app/(marketing)/blog/[slug]/page.tsx contains `TableOfContents` and `BlogArticleBody`
    - src/app/(marketing)/blog/[slug]/page.tsx contains `SiloPillarLink` and `AuthorBio`
    - src/app/(marketing)/blog/[slug]/page.tsx contains `buildBlogPostingJsonLd`
    - src/app/(marketing)/blog/[slug]/page.tsx does NOT contain `<main>`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Blog hub and article pages fully implemented with silo linking, JSON-LD, and section alternation.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite guide page templates (guides hub + cost guide + material guide)</name>
  <files>
    src/app/(marketing)/guides/page.tsx,
    src/app/(marketing)/guides/cost/[slug]/page.tsx,
    src/app/(marketing)/guides/materials/[slug]/page.tsx
  </files>
  <read_first>
    src/app/(marketing)/guides/page.tsx,
    src/app/(marketing)/guides/cost/[slug]/page.tsx,
    src/app/(marketing)/guides/materials/[slug]/page.tsx,
    src/app/(marketing)/services/residential/[service]/page.tsx,
    src/lib/seo/json-ld.tsx,
    src/lib/seo/metadata.ts,
    .planning/phases/09-blog-supporting-content/09-UI-SPEC.md
  </read_first>
  <action>
**Guides Hub** — two grids: Cost Guides (DollarSign icon) and Material Guides (Layers icon). Uses GuideCard.

**Cost Guide Page** — per UI-SPEC Cost Guide Template:
Section order: GuideHero ("COST GUIDE") > BadgeStrip > Intro > CostTable > CostFactors > LocationPricingSection > SavingStrategies > MidPageCTA > WhenToInvest > FaqAccordion > RelatedGuides > QuoteForm > CTABanner
SiloPillarLink after saving strategies: "RELATED SERVICE" linking to `/services/${serviceCategory}/${serviceSlug}`
JSON-LD: Article + FAQPage + RoofingContractor + Breadcrumb (Home > Guides > Cost Guides > Title)

**Material Guide Page** — per UI-SPEC Material Guide Template:
Section order: GuideHero ("MATERIAL GUIDE") > BadgeStrip > Intro > MaterialProperties > LifespanDurability > CostAnalysis > MaterialComparison (pros/cons) > MidPageCTA > BestApplications > HudsonCountySuitability > InstallationProcess > MaintenanceRequirements > FaqAccordion > RelatedGuides > QuoteForm > CTABanner
JSON-LD: Article + FAQPage + RoofingContractor + Breadcrumb (Home > Guides > Material Guides > Title)
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/JerseyCityQualityRoofing && npx tsc --noEmit --pretty 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(marketing)/guides/page.tsx contains `ALL_COST_GUIDES` and `ALL_MATERIAL_GUIDES` and `GuideCard`
    - src/app/(marketing)/guides/cost/[slug]/page.tsx contains `CostTable` and `LocationPricingSection` and `SiloPillarLink`
    - src/app/(marketing)/guides/materials/[slug]/page.tsx contains `MaterialComparison` and `ALL_MATERIAL_GUIDES.map`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Guides hub, cost guide, and material guide pages fully implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite problem page templates (problems hub + problem page)</name>
  <files>
    src/app/(marketing)/problems/page.tsx,
    src/app/(marketing)/problems/[slug]/page.tsx
  </files>
  <read_first>
    src/app/(marketing)/problems/page.tsx,
    src/app/(marketing)/problems/[slug]/page.tsx,
    src/app/(marketing)/services/residential/[service]/page.tsx,
    src/lib/seo/json-ld.tsx,
    .planning/phases/09-blog-supporting-content/09-UI-SPEC.md
  </read_first>
  <action>
**Problems Hub** — grid of GuideCards with AlertTriangle icon. Import ALL_PROBLEMS.

**Problem Page** — per UI-SPEC Problem-Solution Template:
Section order: GuideHero ("COMMON PROBLEM") > BadgeStrip > Intro > IdentificationSignsList > CausesNarrative > DIYvsProfessional > MidPageCTA > ProblemSolutionCTA (with getProblemRelatedServices) > PreventionStrategies > HudsonCountyContext > FaqAccordion > RelatedGuides > QuoteForm > CTABanner
JSON-LD: Article + FAQPage + RoofingContractor + Breadcrumb (Home > Common Roofing Problems > Title)
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/JerseyCityQualityRoofing && npx tsc --noEmit --pretty 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(marketing)/problems/page.tsx contains `ALL_PROBLEMS` and `GuideCard` and `AlertTriangle`
    - src/app/(marketing)/problems/[slug]/page.tsx contains `IdentificationSignsList` and `ProblemSolutionCTA`
    - src/app/(marketing)/problems/[slug]/page.tsx contains `getProblemRelatedServices`
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Problems hub and problem-solution pages fully implemented.</done>
</task>

<task type="auto">
  <name>Task 4: Wire internal-links registry, update sitemap, and activate Wave 0 tests</name>
  <files>
    src/lib/internal-links.ts,
    src/app/sitemap.ts,
    src/data/__tests__/blog-content.test.ts,
    src/data/__tests__/cost-guide-content.test.ts,
    src/data/__tests__/material-guide-content.test.ts,
    src/data/__tests__/problem-content.test.ts
  </files>
  <read_first>
    src/lib/internal-links.ts,
    src/app/sitemap.ts,
    src/data/__tests__/blog-content.test.ts,
    src/data/__tests__/cost-guide-content.test.ts,
    src/data/__tests__/material-guide-content.test.ts,
    src/data/__tests__/problem-content.test.ts,
    src/data/content/blog/index.ts,
    src/data/content/cost-guides/index.ts,
    src/data/content/material-guides/index.ts,
    src/data/content/problems/index.ts
  </read_first>
  <action>
**Part A: Wire `initializeContentRegistry()` in `src/lib/internal-links.ts`**

Import all 4 content registries. Add `let initialized = false;` flag. Fill `initializeContentRegistry()`:
- Blog articles: registerContent with type 'blog', path `/blog/${slug}`
- Cost guides: registerContent with type 'cost-guide', path `/guides/cost/${slug}`
- Material guides: registerContent with type 'material-guide', path `/guides/materials/${slug}`
- Problems: registerContent with type 'problem', path `/problems/${slug}`
- Set `initialized = true` after registration
- Add `export function resetRegistry()` that clears array and sets initialized=false (for tests)

**Part B: Update `src/app/sitemap.ts`**

Import ALL_BLOG_ARTICLES, ALL_COST_GUIDES, ALL_MATERIAL_GUIDES, ALL_PROBLEMS. Add 4 new page arrays:
- blogPages: priority 0.6, changeFrequency 'monthly'
- costGuidePages: priority 0.7, changeFrequency 'monthly'
- materialGuidePages: priority 0.7, changeFrequency 'monthly'
- problemPages: priority 0.6, changeFrequency 'monthly'

Spread all 4 into the returned array.

**Part C: Activate Wave 0 test scaffolds**

In all 4 test files:
1. Change `describe.skip(` to `describe(`
2. Replace TODO import comments with real imports from content registries
3. Wire test assertions to use imported data arrays
  </action>
  <verify>
    <automated>cd /Users/akungapaul/Desktop/N8NProjects/JerseyCityQualityRoofing && pnpm test 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/internal-links.ts contains `import { ALL_BLOG_ARTICLES }` and `let initialized = false`
    - src/app/sitemap.ts contains `ALL_BLOG_ARTICLES` and `blogPages` and `costGuidePages`
    - src/data/__tests__/blog-content.test.ts does NOT contain `describe.skip`
    - src/data/__tests__/cost-guide-content.test.ts does NOT contain `describe.skip`
    - src/data/__tests__/material-guide-content.test.ts does NOT contain `describe.skip`
    - src/data/__tests__/problem-content.test.ts does NOT contain `describe.skip`
    - pnpm test passes with 0 failures
    - pnpm build succeeds
  </acceptance_criteria>
  <done>Internal linking registry wired. Sitemap updated. All Wave 0 tests activated and passing.</done>
</task>

</tasks>

<verification>
- `pnpm test` passes with all content validation tests green
- `pnpm build` succeeds with all pages statically generated
- `npx tsc --noEmit` reports no type errors
- All 7 page templates rewritten from stubs
- Blog hub shows article grid, guides hub shows cost + material grids, problems hub shows problem grid
- Sitemap includes entries for all new pages
- Internal linking registry populated with all content
</verification>

<success_criteria>
- Blog article template renders full section alternation with silo pillar linking
- Cost guide template renders pricing table with location-specific data
- Material guide template renders pros/cons comparison
- Problem template renders identification signs and service CTAs
- Hub pages render content grids with proper categorization
- Automated internal linking connects content across and within silos
- Sitemap updated with all dynamic entries
- All Wave 0 test scaffolds activated and passing
- `pnpm build` generates all pages statically
</success_criteria>

<output>
After completion, create `.planning/phases/09-blog-supporting-content/09-05-SUMMARY.md`
</output>
