---
phase: 08-service-in-city-pages
plan: 15
type: execute
wave: 5
depends_on:
  - "08-01"
  - "08-02"
  - "08-03"
  - "08-04"
  - "08-05"
  - "08-06"
  - "08-07"
  - "08-08"
  - "08-09"
  - "08-10"
  - "08-11"
  - "08-12"
  - "08-13"
  - "08-14"
files_modified:
  - src/app/(marketing)/services/residential/[service]/[city]/page.tsx
  - src/app/(marketing)/services/commercial/[service]/[city]/page.tsx
  - src/app/(marketing)/services/residential/[service]/page.tsx
  - src/app/(marketing)/services/commercial/[service]/page.tsx
  - src/data/__tests__/service-city-uniqueness.test.ts
  - src/data/__tests__/service-city-content-data.test.ts
autonomous: true
requirements:
  - LOC-02
  - LOC-03
  - LOC-04
  - SEO-16
must_haves:
  truths:
    - "All 96 service-in-city pages render at their correct URLs"
    - "Each page displays 15 sections in the correct tone alternation order"
    - "Each page has Service + FAQPage + BreadcrumbList JSON-LD"
    - "Emergency service pages swap hero to emergency variant"
    - "Content uniqueness tests pass (unskipped and green)"
    - "pnpm build completes successfully generating all 96 static pages"
    - "Each parent service page links down to its city variants via a Cities We Serve section"
  artifacts:
    - path: "src/app/(marketing)/services/residential/[service]/[city]/page.tsx"
      provides: "Residential service-in-city page template"
      contains: "CityServiceHero"
      min_lines: 100
    - path: "src/app/(marketing)/services/commercial/[service]/[city]/page.tsx"
      provides: "Commercial service-in-city page template"
      contains: "CityServiceHero"
      min_lines: 100
    - path: "src/app/(marketing)/services/residential/[service]/page.tsx"
      provides: "Updated residential service page with city downlinks"
      contains: "Cities We Serve"
    - path: "src/app/(marketing)/services/commercial/[service]/page.tsx"
      provides: "Updated commercial service page with city downlinks"
      contains: "Cities We Serve"
  key_links:
    - from: "src/app/(marketing)/services/residential/[service]/[city]/page.tsx"
      to: "src/data/content/service-cities/**/*.ts"
      via: "CONTENT_MAP lookup"
      pattern: "CONTENT_MAP"
    - from: "src/app/(marketing)/services/residential/[service]/[city]/page.tsx"
      to: "src/lib/seo/json-ld.tsx"
      via: "buildServiceInCityJsonLd import"
      pattern: "buildServiceInCityJsonLd"
    - from: "src/app/(marketing)/services/residential/[service]/page.tsx"
      to: "src/app/(marketing)/services/residential/[service]/[city]/page.tsx"
      via: "downward Links to city variants"
      pattern: "/services/residential/.*/"
    - from: "src/app/(marketing)/services/commercial/[service]/page.tsx"
      to: "src/app/(marketing)/services/commercial/[service]/[city]/page.tsx"
      via: "downward Links to city variants"
      pattern: "/services/commercial/.*/"
---

<objective>
Replace the stub page templates for both residential and commercial service-in-city routes with the full 15-section template. Wire all content data, section components, JSON-LD schemas. Add downward links from parent service pages to city variants (per RESEARCH.md Pitfall 5). Unskip and activate Wave 0 tests. Verify complete build produces all 96 pages.

Purpose: This is the final assembly plan that connects all the pieces from Plans 01-14. After this plan, all 96 service-in-city pages are fully functional with unique content, complete SEO markup, proper internal linking (including bidirectional links between parent service pages and city variants), and the silo structure is complete.
Output: Two fully wired page templates, updated parent service pages with city downlinks, activated tests, successful build of all 96 static pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-service-in-city-pages/08-RESEARCH.md
@.planning/phases/08-service-in-city-pages/08-UI-SPEC.md
@.planning/phases/08-service-in-city-pages/08-01-SUMMARY.md
@.planning/phases/08-service-in-city-pages/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire residential and commercial page templates with full 15-section layout</name>
  <files>src/app/(marketing)/services/residential/[service]/[city]/page.tsx, src/app/(marketing)/services/commercial/[service]/[city]/page.tsx</files>
  <read_first>
    - src/app/(marketing)/services/residential/[service]/[city]/page.tsx
    - src/app/(marketing)/services/commercial/[service]/[city]/page.tsx
    - src/app/(marketing)/services/residential/[service]/page.tsx (CONTENT_MAP pattern reference)
    - src/app/(marketing)/service-areas/[city]/page.tsx (city CONTENT_MAP + breadcrumb reference)
    - src/data/content/service-cities/jersey-city/roof-repair.ts (verify export naming)
    - src/data/service-city-content.ts (getCityServiceContent resolver)
    - src/data/services.ts (getService, getResidentialServiceSlugs, getCommercialServiceSlugs)
    - src/data/municipalities.ts (getMunicipality, getAllMunicipalitySlugs)
    - src/data/testimonials.ts (getTestimonialsByCityAndService, TESTIMONIALS)
    - src/lib/seo/json-ld.tsx (buildServiceInCityJsonLd, buildFaqPageJsonLd, buildBreadcrumbJsonLd, JsonLd)
    - src/lib/constants.ts (BASE_URL)
    - .planning/phases/08-service-in-city-pages/08-UI-SPEC.md (section order and tone alternation)
  </read_first>
  <action>
**A. Replace residential `[city]/page.tsx`:**

COMPLETELY replace the stub. Structure:

1. Import all residential content files (4 services x 12 cities = 48 imports), all section components, SEO builders, data lookups.

2. Build nested CONTENT_MAP: `Record<string, Record<string, ServiceInCityContent>>` keyed by citySlug then serviceSlug. Each city gets 4 residential service entries.

3. Keep existing `generateStaticParams` and `generateMetadata` (already correct). Add OG image to metadata: `images: [{ url: \`/api/og?service=\${service.slug}&city=\${city.slug}\`, width: 1200, height: 630 }]`.

4. Page component resolves: service (getService), city (getMunicipality), content (CONTENT_MAP lookup), resolverContent (getCityServiceContent).

5. Compute: isEmergency (service.emergencyAvailable && serviceSlug === 'emergency-roofing'), mergedFaqs ([...resolverContent.uniqueFaqs, ...content.extendedFaqs]), displayTestimonials (getTestimonialsByCityAndService with 3+ threshold, fallback TESTIMONIALS), breadcrumbItems (5-item chain: Home > Services > Residential > [Service] > [City]).

6. Render 3 JSON-LD blocks then 15 sections per 08-UI-SPEC.md tone alternation:
   - 1. CityServiceHero (dominant, own wrapper)
   - 2. BadgeStrip (secondary, own wrapper)
   - 3. LocalServiceContext (dominant, SectionWrapper+ScrollReveal) -- heading like "[Service] in [City]: What Local Homeowners Need to Know", narrative=content.cityServiceNarrative, servicePageLink=`/services/residential/${serviceSlug}`, cityPageLink=`/service-areas/${citySlug}`
   - 4. ProcessTimeline (secondary, SectionWrapper+ScrollReveal) -- heading "Our [Service] Process in [City]", steps=service.processSteps, accentColor for emergency
   - 5. NeighborhoodServiceInsights (dominant, SectionWrapper+ScrollReveal)
   - 6. MaterialCards (secondary, SectionWrapper+ScrollReveal) -- heading "Roofing Materials for [City] [Service]", intro paragraph from content.cityMaterialsAdvice, then MaterialCards component
   - 7. CitySpecificConcerns (dominant, SectionWrapper+ScrollReveal) -- heading "Common [Service] Challenges in [City]", concerns=resolverContent.specificConcerns, intro=content.citySpecificProcess
   - 8. MidPageCTA (secondary, own wrapper) -- heading "Need [Service] in [City]?"
   - 9. CostFactorsSection (dominant, SectionWrapper+ScrollReveal) -- intro paragraph from content.cityCostContext above CostFactorsSection
   - 10. ServiceContentSection for localCaseScenario (secondary, SectionWrapper+ScrollReveal) -- heading "A Real [Service] Story in [City]"
   - 11. TestimonialCarousel (secondary, own wrapper)
   - 12. SiblingCitiesNav (dominant, SectionWrapper+ScrollReveal) -- serviceCategory='residential'
   - 13. FaqAccordion (secondary, SectionWrapper+ScrollReveal) -- heading "Frequently Asked Questions About [Service] in [City]"
   - 14. QuoteForm (dominant, id="quote-form")
   - 15. CTABanner (secondary, own wrapper) -- heading "Your Trusted [City] [Service] Experts"

**B. Replace commercial `[city]/page.tsx`:**

IDENTICAL structure with these differences:
- getCommercialServiceSlugs instead of getResidentialServiceSlugs
- Import commercial content files (flat-roof-systems, roof-maintenance, commercial-repair, commercial-replacement x 12 cities = 48)
- canonicalUrl uses `/services/commercial/`
- Breadcrumb: Home > Services > Commercial > [Service] > [City]
- SiblingCitiesNav serviceCategory='commercial'
- isEmergency is always false
- Page function: CommercialServiceCityPage

Do NOT create a barrel index.ts importing all 96 files.
  </action>
  <verify>
    <automated>pnpm type-check && pnpm build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Residential page.tsx contains CityServiceHero, buildServiceInCityJsonLd, buildBreadcrumbJsonLd, CONTENT_MAP, getCityServiceContent, SiblingCitiesNav
    - Commercial page.tsx contains CityServiceHero, buildServiceInCityJsonLd, CONTENT_MAP, serviceCategory matching 'commercial'
    - pnpm type-check exits 0
    - pnpm build exits 0 generating all 96 static pages
  </acceptance_criteria>
  <done>Both page templates fully wired with 15 sections, JSON-LD, content data, testimonials, and internal linking. Build generates all 96 pages.</done>
</task>

<task type="auto">
  <name>Task 2: Add city downlinks to parent service pages and activate Wave 0 tests</name>
  <files>src/app/(marketing)/services/residential/[service]/page.tsx, src/app/(marketing)/services/commercial/[service]/page.tsx, src/data/__tests__/service-city-uniqueness.test.ts, src/data/__tests__/service-city-content-data.test.ts</files>
  <read_first>
    - src/app/(marketing)/services/residential/[service]/page.tsx (existing service page template)
    - src/app/(marketing)/services/commercial/[service]/page.tsx (existing commercial page template)
    - src/data/municipalities.ts (getAllMunicipalities for city list)
    - src/data/__tests__/service-city-uniqueness.test.ts
    - src/data/__tests__/service-city-content-data.test.ts
    - src/data/content/service-cities/jersey-city/roof-repair.ts
    - src/data/content/service-cities/hoboken/roof-repair.ts
    - src/data/content/service-cities/bayonne/roof-repair.ts
    - src/data/content/service-cities/secaucus/roof-repair.ts
  </read_first>
  <action>
**A. Add "Cities We Serve" downlinks to residential service pages:**

In `src/app/(marketing)/services/residential/[service]/page.tsx`, add a new section AFTER the existing SiblingServicesRow and BEFORE the QuoteForm section. This addresses RESEARCH.md Pitfall 5 (parent service pages should link down to city variants).

Add a section wrapped in `<SectionWrapper tone="dominant"><ScrollReveal>`:
```tsx
<h2 className="font-heading font-bold text-[1.75rem] text-text-primary mb-4">
  {service.name} in Hudson County Cities
</h2>
<p className="text-lg text-text-secondary mb-6 leading-relaxed">
  We provide expert {service.name.toLowerCase()} services across all 12 Hudson County municipalities. Select your city for location-specific information, pricing, and FAQs.
</p>
<nav aria-label="Service available in these cities" className="flex flex-wrap gap-3">
  {getAllMunicipalities().map((city) => (
    <Link
      key={city.slug}
      href={`/services/residential/${serviceSlug}/${city.slug}`}
      prefetch={false}
      className="inline-flex items-center bg-secondary rounded-lg px-4 py-2 text-lg text-text-primary border border-[#4a5040] hover:border-accent transition-colors duration-[150ms] min-h-[44px]"
    >
      {city.name}
    </Link>
  ))}
</nav>
```

Import `getAllMunicipalities` from `@/data/municipalities` (add to existing imports).

**B. Add "Cities We Serve" downlinks to commercial service pages:**

Same pattern in `src/app/(marketing)/services/commercial/[service]/page.tsx` but with `/services/commercial/` prefix in Link hrefs.

**C. Update service-city-uniqueness.test.ts:**

1. Change `describe.skip` to `describe` (unskip)
2. Import roof-repair content from 4 representative cities (jersey-city, hoboken, bayonne, secaucus)
3. Test all 6 pairwise combinations for cityServiceNarrative Jaccard similarity < 0.30
4. Also test localCaseScenario for at least 2 pairs

**D. Update service-city-content-data.test.ts:**

1. Change `describe.skip` to `describe` (unskip)
2. Import 3-4 sample content files: jersey-city/roof-repair, hoboken/flat-roof-systems, secaucus/emergency-roofing, weehawken/commercial-repair
3. For each, test:
   - cityServiceNarrative word count >= 400
   - localCaseScenario word count >= 250
   - cityMaterialsAdvice word count >= 150
   - cityCostContext word count >= 100
   - citySpecificProcess word count >= 150
   - closingNarrative word count >= 100
   - neighborhoodServiceInsights length 3-5, each insight >= 50 words
   - extendedFaqs length 3-5, each answer >= 30 words
   - serviceSlug matches a valid slug (use getService to verify)
   - citySlug matches a valid slug (use getMunicipality to verify)
   - CUMULATIVE: totalProseWords(content) >= 2500 (the critical cumulative word count assertion)
  </action>
  <verify>
    <automated>pnpm test -- src/data/__tests__/service-city-uniqueness.test.ts src/data/__tests__/service-city-content-data.test.ts src/data/__tests__/service-city-seo.test.ts && grep -n "Cities We Serve" src/app/\(marketing\)/services/residential/\[service\]/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Residential service page contains "Cities We Serve" section with links to /services/residential/[service]/[city] for all 12 cities
    - Commercial service page contains "Cities We Serve" section with links to /services/commercial/[service]/[city] for all 12 cities
    - service-city-uniqueness.test.ts does NOT contain describe.skip
    - Tests at least 6 city pairs for Jaccard similarity < 0.30
    - service-city-content-data.test.ts does NOT contain describe.skip
    - Tests validate word counts, FAQ counts, slug validity, AND totalProseWords >= 2500 for 3+ sample files
    - All tests pass with pnpm test exit code 0
  </acceptance_criteria>
  <done>Parent service pages link down to all city variants. Wave 0 tests activated. Content uniqueness < 30% Jaccard. Structure validated including cumulative word count >= 2500. All tests green.</done>
</task>

</tasks>

<verification>
- pnpm type-check passes
- pnpm build completes generating all 96 pages
- pnpm test passes with all suites green
- Residential template: 15 sections, 3 JSON-LD blocks
- Commercial template: 15 sections, 3 JSON-LD blocks
- Each parent service page links to 12 city variants
- Bidirectional linking complete: parent service -> city variant -> parent service (via LocalServiceContext links)
</verification>

<success_criteria>
- All 96 pages render at correct URLs via pnpm build
- Each page has 15-section template with city-specific content
- JSON-LD: Service (city-scoped areaServed), FAQPage, BreadcrumbList (5 items)
- Emergency pages use emergency hero variant
- SiblingCitiesNav links to same service in 11 other cities
- Parent service pages link down to all 12 city variants (addresses RESEARCH.md Pitfall 5)
- Content uniqueness tests pass (Jaccard < 30%)
- Content structure tests pass (word counts, FAQ counts, cumulative >= 2500)
- Full build + full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/08-service-in-city-pages/08-15-SUMMARY.md`
</output>
